# Progress 2026-05-02

## Ringkasan Perubahan

- Rename folder backend dari `materialize-fastapi/app/repository/` menjadi `materialize-fastapi/app/repositories/`.
- Update seluruh import Python dari `app.repository...` menjadi `app.repositories...`.
- Update path SQL query dari `app/repository/...` menjadi `app/repositories/...`.
- Update dokumentasi struktur backend di `AGENTS.md` dan `materialize-fastapi/backend_agent.md`.

## File yang Diubah

- `AGENTS.md`
- `materialize-fastapi/backend_agent.md`
- `materialize-fastapi/app/dependencies/*.py` (file dependency yang mengimpor repository)
- `materialize-fastapi/app/services/*.py` (file service yang mengimpor repository/path SQL)
- `materialize-fastapi/app/job/*.py` (path SQL/query dan import repository)
- `materialize-fastapi/app/integrations/ceisa/*.py` (import repository terkait CEISA)
- `materialize-fastapi/app/repositories/` (hasil rename dari `app/repository/`)

## Hasil Verifikasi/Test

- Verifikasi referensi teks: tidak ada lagi `app.repository` dan `app/repository` di source backend.
- Verifikasi struktur folder: `materialize-fastapi/app/repositories/` sudah tersedia dan berisi file repository sebelumnya.
- Belum menjalankan test suite/lint penuh pada task ini.

## Gap / Risiko

- Perubahan import lintas banyak file berisiko memunculkan error runtime jika ada referensi dinamis yang tidak terdeteksi pattern pencarian.
- Dokumentasi lain di luar scope yang menyebut `repository` mungkin masih ada dan perlu audit lanjutan.

## Blocker

- Tidak ada blocker teknis saat eksekusi rename dan update referensi.

## Next Step

- Jalankan test backend dan smoke run FastAPI (`poetry run ...`) untuk memastikan seluruh import dan path query berjalan normal.

---

## Update Desktop Scaffold (2026-05-02)

### Ringkasan Perubahan

- Menambahkan scaffold awal aplikasi desktop C# .NET MAUI di `desktop-app/src/Mau.Desktop/`.
- Menyusun struktur MVVM + DI + Service Layer + API client sesuai `desktop-app/desktop_agent.md`.
- Menambahkan solusi `desktop-app/Mau.Desktop.sln`, project test dasar `desktop-app/tests/Mau.Desktop.Tests/`, konfigurasi contoh `desktop-app/appsettings.example.json`, dan dokumentasi desktop.
- Menambahkan `desktop-app/.gitignore` untuk mengabaikan artifact build/IDE/secrets lokal.

### File yang Diubah

- `desktop-app/.gitignore`
- `desktop-app/Mau.Desktop.sln`
- `desktop-app/README.md`
- `desktop-app/appsettings.example.json`
- `desktop-app/docs/README.md`
- `desktop-app/src/Mau.Desktop/` (App, Shell, MauiProgram, Core, Api, Services, Devices, ViewModels, Views, Models, Resources, Platforms)
- `desktop-app/tests/Mau.Desktop.Tests/Mau.Desktop.Tests.csproj`
- `desktop-app/tests/Mau.Desktop.Tests/ViewModels/ResultTests.cs`

### Hasil Verifikasi/Test

- Scaffold MAUI berhasil dibuat dengan `dotnet new maui`.
- Build project MAUI belum berhasil di environment ini karena restore package NuGet tidak tersedia (`NU1101`, source hanya offline packages).
- Tidak ada koneksi database langsung dari desktop; alur akses data diarahkan lewat layer API client ke backend.

### Gap / Risiko

- API endpoint yang dipanggil desktop (`/auth/login`, `/auth/me`, `/warehouse/weighing`) adalah kontrak awal dan perlu diselaraskan dengan endpoint backend aktual.
- Integrasi device (scanner/timbangan/printer) masih berupa stub dan perlu adapter vendor/driver pada iterasi berikutnya.

### Blocker

- Source NuGet online belum tersedia pada environment ini, sehingga restore/build penuh belum dapat divalidasi.

### Next Step

- Sinkronkan DTO + endpoint dengan backend FastAPI.
- Implementasi use case operasional awal (login, weighing submit, basic navigation guard) beserta unit test ViewModel/Service lebih lengkap.
- Jalankan build/test ulang setelah source NuGet dapat diakses.

---

## Update Desktop Test Compatibility (2026-05-02)

### Ringkasan Perubahan

- Perbaikan kompatibilitas test project untuk mengatasi `NU1201` saat referensi ke project MAUI.
- Memisahkan `Result<T>` ke class library baru `Mau.Desktop.Core` (target `net8.0`) agar bisa diuji tanpa mereferensikan project UI MAUI.
- Mengubah `Mau.Desktop.Tests` agar mereferensikan `Mau.Desktop.Core` alih-alih `Mau.Desktop`.
- Menambahkan `ProjectReference` dari `Mau.Desktop` ke `Mau.Desktop.Core`.

### File yang Diubah

- `desktop-app/src/Mau.Desktop/Mau.Desktop.csproj`
- `desktop-app/tests/Mau.Desktop.Tests/Mau.Desktop.Tests.csproj`
- `desktop-app/src/Mau.Desktop.Core/Mau.Desktop.Core.csproj`
- `desktop-app/src/Mau.Desktop.Core/Result.cs`
- `desktop-app/src/Mau.Desktop/Core/Result.cs` (dipindahkan)
- `desktop-app/Mau.Desktop.sln`

### Hasil Verifikasi/Test

- `dotnet build desktop-app/src/Mau.Desktop/Mau.Desktop.csproj -f net8.0-windows10.0.19041.0` sukses.
- `dotnet test desktop-app/tests/Mau.Desktop.Tests/Mau.Desktop.Tests.csproj` sukses (1 passed).
- Error `NU1201` pada test project tidak muncul lagi.

### Gap / Risiko

- Belum ada pemisahan lebih lanjut domain/service ke class library terpisah; saat ini baru `Result<T>` yang diekstrak untuk kebutuhan test awal.

### Next Step

- Pindahkan logic yang testable (DTO mapper, validator UX, service helper) bertahap ke `Mau.Desktop.Core` agar cakupan unit test meningkat tanpa coupling ke UI MAUI.

---

## Update Visual Studio Deploy Mapping (2026-05-02)

### Ringkasan Perubahan

- Menambahkan mapping `Deploy.0` untuk project `Mau.Desktop` di solution agar Visual Studio dapat deploy sebelum debug.

### File yang Diubah

- `desktop-app/Mau.Desktop.sln`

### Hasil Verifikasi/Test

- Verifikasi konten solution: konfigurasi `Debug|Any CPU` dan `Release|Any CPU` untuk `Mau.Desktop` kini memiliki entri `Deploy.0`.

### Gap / Risiko

- Jika startup project di Visual Studio bukan `Mau.Desktop`, error deploy masih bisa muncul.

### Next Step

- Pastikan `Mau.Desktop` dijadikan Startup Project dan target debug Windows aktif.

---

## Update Startup Exception Fix (2026-05-02)

### Ringkasan Perubahan

- Perbaikan startup crash yang memicu breakpoint `Debugger.Break()` pada debug Visual Studio.
- Menambahkan constructor parameterless pada page yang dipakai `ShellContent` agar kompatibel dengan instansiasi XAML DataTemplate.
- Constructor parameterless tetap resolve ViewModel dari DI melalui helper `ServiceResolver`.

### File yang Diubah

- `desktop-app/src/Mau.Desktop/Core/ServiceResolver.cs`
- `desktop-app/src/Mau.Desktop/Views/LoginPage.xaml.cs`
- `desktop-app/src/Mau.Desktop/Views/MainPage.xaml.cs`
- `desktop-app/src/Mau.Desktop/Views/Warehouse/WeighingPage.xaml.cs`
- `desktop-app/src/Mau.Desktop/Views/Warehouse/BuildupPage.xaml.cs`

### Hasil Verifikasi/Test

- `dotnet build desktop-app/src/Mau.Desktop/Mau.Desktop.csproj -f net8.0-windows10.0.19041.0` sukses.
- Pola constructor page kini kompatibel dengan Shell XAML dan tetap menjaga DI ViewModel.

### Gap / Risiko

- Jika masih break di startup, perlu lihat exception detail pertama di `Output > Debug` karena itu menandakan error runtime lain (mis. endpoint config/service runtime).

### Next Step

- Jalankan kembali via Visual Studio (Startup Project `Mau.Desktop`) dan verifikasi halaman login tampil normal.

---

## Update Native Debug Profile (2026-05-02)

### Ringkasan Perubahan

- Mengaktifkan native debugging untuk profile `Windows Machine` agar exception Win32 dapat ditangkap langsung di Visual Studio, bukan muncul sebagai JIT popup tanpa stack detail.

### File yang Diubah

- `desktop-app/src/Mau.Desktop/Properties/launchSettings.json`

### Hasil Verifikasi/Test

- Build target Windows tetap sukses setelah update launch profile.

### Next Step

- Jalankan debug ulang dari Visual Studio untuk mendapatkan call stack native/managed yang lengkap jika crash masih terjadi.

---

## Update XAML Startup Stability (2026-05-02)

### Ringkasan Perubahan

- Mengubah pola constructor page agar aman terhadap inisialisasi XAML/Shell di Windows.
- Constructor parameterless kini hanya menjalankan `InitializeComponent()`.
- `BindingContext` dari DI di-resolve pada `OnHandlerChanged()` saat `MauiContext` sudah siap.
- Menghapus `ServiceResolver` karena tidak lagi diperlukan.

### File yang Diubah

- `desktop-app/src/Mau.Desktop/Views/LoginPage.xaml.cs`
- `desktop-app/src/Mau.Desktop/Views/MainPage.xaml.cs`
- `desktop-app/src/Mau.Desktop/Views/Warehouse/WeighingPage.xaml.cs`
- `desktop-app/src/Mau.Desktop/Views/Warehouse/BuildupPage.xaml.cs`
- `desktop-app/src/Mau.Desktop/Core/ServiceResolver.cs` (dihapus)

### Hasil Verifikasi/Test

- `dotnet build desktop-app/src/Mau.Desktop/Mau.Desktop.csproj -f net8.0-windows10.0.19041.0` sukses.

### Next Step

- Jalankan ulang debug di Visual Studio untuk memastikan exception `Microsoft.UI.Xaml` tidak muncul lagi saat startup.

---

## Update Android Icon Build Fix (2026-05-02)

### Ringkasan Perubahan

- Memperbaiki error Android `APT2260` (`mipmap/appicon` dan `appicon_round` not found).
- Menyederhanakan konfigurasi ikon MAUI Android agar tidak bergantung pada adaptive icon foreground file yang sempat menimbulkan lock (`MAUI0000`).

### File yang Diubah

- `desktop-app/src/Mau.Desktop/Mau.Desktop.csproj`
  - `MauiIcon` diubah menjadi single source: `Resources/AppIcon/appicon.svg` tanpa `ForegroundFile`.
- `desktop-app/src/Mau.Desktop/Platforms/Android/AndroidManifest.xml`
  - Menghapus atribut `android:roundIcon`.

### Hasil Verifikasi/Test

- `dotnet build desktop-app/src/Mau.Desktop/Mau.Desktop.csproj -f net8.0-android` sukses.
- Error `APT2260` tidak muncul lagi.

### Gap / Risiko

- Android adaptive round icon sementara dinonaktifkan agar build stabil.
- Jika diperlukan branding adaptive icon penuh, perlu ditambahkan ulang setelah isu lock Resizetizer dipastikan tidak terjadi di environment dev.

### Next Step

- Jalankan ulang dari Visual Studio target Android.
- Jika build stabil, bisa lanjut set adaptive icon terkontrol pada iterasi berikutnya.

---

## Update Windows DEP0700 Fix (2026-05-02)

### Ringkasan Perubahan

- Menangani error deploy Windows `DEP0700` terkait splash screen yang tidak ditemukan saat debug.
- Mengatur mode debug Windows menjadi **unpackaged** agar F5 tidak bergantung pada instalasi MSIX.

### File yang Diubah

- `desktop-app/src/Mau.Desktop/Mau.Desktop.csproj`
  - Tambah property: `WindowsPackageType=None` untuk target Windows pada konfigurasi `Debug`.
- `desktop-app/src/Mau.Desktop/Properties/launchSettings.json`
  - Ubah `commandName` dari `MsixPackage` menjadi `Project` untuk profile `Windows Machine`.

### Hasil Verifikasi/Test

- Build debug Windows sukses setelah perubahan.
- Publish MSIX release sebelumnya juga tervalidasi berisi `splashSplashScreen.scale-*.png`, jadi error DEP0700 berasal dari alur deploy debug/package lama.

### Next Step

- Jalankan ulang dari Visual Studio dengan profile `Windows Machine` (unpackaged debug).
- Jika perlu packaging installer, gunakan publish Release (MSIX) secara terpisah.

---

## Update Desktop Migration to WPF (2026-05-02)

### Ringkasan Perubahan

- Menghapus total scaffold desktop berbasis MAUI.
- Membangun ulang desktop app menggunakan **WPF (.NET 8)**.
- Menyusun struktur MVVM + DI + service layer + API client untuk baseline desktop baru.
- Mengganti panduan desktop di root `AGENTS.md` dari MAUI menjadi WPF.
- Memperbarui `desktop-app/desktop_agent.md` agar selaras dengan stack WPF.

### File yang Diubah

- `AGENTS.md`
- `desktop-app/desktop_agent.md`
- `desktop-app/.gitignore`
- `desktop-app/Mau.Desktop.sln`
- `desktop-app/README.md`
- `desktop-app/appsettings.example.json`
- `desktop-app/docs/README.md`
- `desktop-app/src/Mau.Desktop/*` (project WPF baru: App, MainWindow, Api, Services, Core, ViewModels, Views)
- `desktop-app/src/Mau.Desktop.Core/*`
- `desktop-app/tests/Mau.Desktop.Tests/*`

### Hasil Verifikasi/Test

- `dotnet build desktop-app/Mau.Desktop.sln` sukses.
- `dotnet test desktop-app/tests/Mau.Desktop.Tests/Mau.Desktop.Tests.csproj --no-build` sukses (1 passed).

### Gap / Risiko

- Integrasi device lokal (scanner/timbangan/printer) masih placeholder dan belum diimplementasi.
- Endpoint API yang dipakai (`/auth/login`, `/warehouse/weighing`) masih baseline dan perlu sinkronisasi detail dengan backend aktual.

### Next Step

- Tambahkan modul operasional prioritas (weighing/buildup) pada WPF ViewModel + Service.
- Tambahkan abstraction device layer saat kebutuhan hardware sudah final.

---

## Update Desktop WPF UI Scaffolding (2026-05-02)

### Ringkasan Perubahan

- Refactor total `desktop-app/src/Mau.Desktop` menjadi scaffold baru berbasis **WPF UI (lepoco/wpfui)**.
- Mengganti shell utama ke `FluentWindow` + `NavigationView` dan pola page-based navigation.
- Menata ulang struktur desktop ke baseline `Api`, `Configuration`, `Models`, `Services`, `ViewModels`, `Views/Pages`.
- Menambahkan page provider untuk DI navigation (`INavigationViewPageProvider`) tanpa bypass backend.
- Memperbarui aturan desktop di root `AGENTS.md` dan `desktop-app/desktop_agent.md` agar eksplisit memakai WPF UI.

### File yang Diubah

- `AGENTS.md`
- `desktop-app/desktop_agent.md`
- `desktop-app/src/Mau.Desktop/Mau.Desktop.csproj`
- `desktop-app/src/Mau.Desktop/App.xaml`
- `desktop-app/src/Mau.Desktop/App.xaml.cs`
- `desktop-app/src/Mau.Desktop/MainWindow.xaml`
- `desktop-app/src/Mau.Desktop/MainWindow.xaml.cs`
- `desktop-app/src/Mau.Desktop/Api/*`
- `desktop-app/src/Mau.Desktop/Configuration/*`
- `desktop-app/src/Mau.Desktop/Models/*`
- `desktop-app/src/Mau.Desktop/Services/*`
- `desktop-app/src/Mau.Desktop/ViewModels/*`
- `desktop-app/src/Mau.Desktop/Views/Pages/*`

### Hasil Verifikasi/Test

- `dotnet build desktop-app/src/Mau.Desktop/Mau.Desktop.csproj` sukses.
- `dotnet test desktop-app/tests/Mau.Desktop.Tests/Mau.Desktop.Tests.csproj` gagal restore karena `PackageSourceMapping` environment (dependency test package tidak bisa di-resolve dari source yang tersedia).

### Gap / Risiko

- Scaffold masih baseline UI dan belum mengandung implementasi flow bisnis gudang penuh.
- Paket test di environment saat ini belum bisa direstore, jadi verifikasi unit test belum lengkap.

### Blocker

- Konfigurasi NuGet `PackageSourceMapping` lokal menghambat restore dependency test.

### Next Step

- Sinkronkan menu/page WPF UI dengan modul operasional prioritas (login, inbound, buildup, manifest).
- Rapikan konfigurasi source mapping NuGet agar `dotnet test` bisa berjalan normal.

---

## Update Desktop Theme Switching (2026-05-02)

### Ringkasan Perubahan

- Menambahkan runtime theme switching pada desktop WPF UI.
- Menambahkan method pengubah tema di `MainWindow` untuk `Light`, `Dark`, dan sinkron `System Theme`.
- Menambahkan kontrol tombol theme di halaman `Settings` agar pergantian tema bisa dilakukan tanpa restart aplikasi.

### File yang Diubah

- `desktop-app/src/Mau.Desktop/MainWindow.xaml.cs`
- `desktop-app/src/Mau.Desktop/Views/Pages/SettingsPage.xaml`
- `desktop-app/src/Mau.Desktop/Views/Pages/SettingsPage.xaml.cs`

### Hasil Verifikasi/Test

- `dotnet build desktop-app/src/Mau.Desktop/Mau.Desktop.csproj` sukses (0 warning, 0 error).

### Gap / Risiko

- Pilihan tema belum dipersist ke storage lokal; saat aplikasi dibuka ulang, theme kembali mengikuti konfigurasi startup saat ini.

### Blocker

- Tidak ada blocker.

### Next Step

- Tambahkan penyimpanan preferensi tema operator di local settings, lalu apply otomatis saat startup.

---

## Update Desktop Theme UI Stabilization (2026-05-02)

### Ringkasan Perubahan

- Merapikan hasil UI theme switching yang sebelumnya terlihat tidak konsisten.
- Menghapus elemen `SymbolIcon` dummy di root `MainWindow` yang tidak diperlukan.
- Menonaktifkan `ExtendsContentIntoTitleBar` untuk menghindari artefak tampilan title bar.
- Mengganti backdrop runtime theme dari `Mica` ke `None` agar surface UI tidak tampak abu-abu transparan.
- Menormalkan startup theme ke `Light` dan memperbaiki mode `System` agar dipetakan eksplisit ke `Light/Dark/HighContrast`.

### File yang Diubah

- `desktop-app/src/Mau.Desktop/MainWindow.xaml`
- `desktop-app/src/Mau.Desktop/MainWindow.xaml.cs`
- `desktop-app/src/Mau.Desktop/App.xaml`

### Hasil Verifikasi/Test

- `dotnet build desktop-app/src/Mau.Desktop/Mau.Desktop.csproj` sukses (0 warning, 0 error).

### Gap / Risiko

- Preferensi tema masih belum dipersist per user; saat aplikasi restart, default tetap `Light` kecuali ditambah storage setting.

### Blocker

- Tidak ada blocker.

### Next Step

- Simpan pilihan tema operator ke local settings agar konsisten antar sesi.

---

## Update Desktop Dashboard WPF UI Style (2026-05-02)

### Ringkasan Perubahan

- Mendesain ulang halaman `Dashboard` agar mengikuti pola visual WPF UI (hero panel, ringkasan KPI, aktivitas, quick link).
- Menjaga arsitektur MVVM: seluruh konten dashboard dibaca dari `DashboardViewModel`.
- Memastikan window tetap memakai native chrome untuk menampilkan tombol `minimize`, `maximize`, dan `close`.

### File yang Diubah

- `desktop-app/src/Mau.Desktop/MainWindow.xaml`
- `desktop-app/src/Mau.Desktop/Views/Pages/DashboardPage.xaml`
- `desktop-app/src/Mau.Desktop/ViewModels/DashboardViewModel.cs`

### Hasil Verifikasi/Test

- `dotnet build desktop-app/src/Mau.Desktop/Mau.Desktop.csproj` sukses (0 warning, 0 error).

### Gap / Risiko

- Angka KPI dan aktivitas masih placeholder; belum terhubung ke endpoint backend.

### Blocker

- Tidak ada blocker.

### Next Step

- Hubungkan data dashboard ke service backend (`app/services` di FastAPI) melalui API client desktop agar KPI realtime.

---

## Update Desktop Test Restore NU1100 Fix (2026-05-02)

### Ringkasan Perubahan

- Memperbaiki error restore test `NU1100` yang disebabkan `PackageSourceMapping` global terlalu ketat.
- Menambahkan `NuGet.Config` lokal di `desktop-app/` untuk override mapping khusus workspace desktop.
- Mapping lokal mengizinkan seluruh package (`*`) dari `nuget.org` agar paket test (`Microsoft.NET.Test.Sdk` dan dependency transitif) bisa di-restore.

### File yang Diubah

- `desktop-app/NuGet.Config`

### Hasil Verifikasi/Test

- `dotnet restore desktop-app/tests/Mau.Desktop.Tests/Mau.Desktop.Tests.csproj` sukses.
- `dotnet build desktop-app/Mau.Desktop.sln` sukses (0 warning, 0 error).
- `dotnet test desktop-app/tests/Mau.Desktop.Tests/Mau.Desktop.Tests.csproj` sukses (1 passed).

### Gap / Risiko

- Jika ada kebijakan perusahaan yang mewajibkan source privat tertentu, mapping lokal ini perlu disesuaikan ulang agar tetap patuh policy internal.

### Blocker

- Tidak ada blocker.

### Next Step

- Jika diperlukan, tambahkan source privat resmi ke `desktop-app/NuGet.Config` dan tetap pertahankan mapping eksplisit per package namespace.

---

## Update Desktop Window Close Button (2026-05-02)

### Ringkasan Perubahan

- Menambahkan `TitleBar` eksplisit pada `NavigationView` WPF UI agar tombol window (`minimize`, `maximize`, `close`) tampil konsisten.
- Mengaktifkan `ExtendsContentIntoTitleBar="True"` pada `MainWindow` agar area title bar dikelola penuh oleh kontrol `Wpf.Ui`.

### File yang Diubah

- `desktop-app/src/Mau.Desktop/MainWindow.xaml`

### Hasil Verifikasi/Test

- `dotnet build desktop-app/Mau.Desktop.sln` sukses (0 warning, 0 error).

### Gap / Risiko

- Posisi visual title bar bisa sedikit berbeda antar scaling DPI/monitor; jika ada overlap area drag-window dengan konten, perlu tuning margin/padding title bar.

### Blocker

- Tidak ada blocker.

### Next Step

- Jalankan ulang `Mau.Desktop.exe` dan verifikasi tombol `close` muncul di kanan atas serta fungsional saat diklik.

---

## Update Desktop Window Close Fallback (2026-05-02)

### Ringkasan Perubahan

- Menambahkan fallback tombol close eksplisit di area kanan `TitleBar` karena tombol caption bawaan `ShowClose` tidak tampil di runtime environment saat ini.
- Tombol `Tutup` dihubungkan ke handler `OnCloseWindowClick` untuk menutup `MainWindow` secara langsung.

### File yang Diubah

- `desktop-app/src/Mau.Desktop/MainWindow.xaml`
- `desktop-app/src/Mau.Desktop/MainWindow.xaml.cs`

### Hasil Verifikasi/Test

- `dotnet build desktop-app/Mau.Desktop.sln` sukses (0 warning, 0 error).

### Gap / Risiko

- Tombol minimize/maximize native belum ditampilkan karena issue render caption icon bawaan masih perlu investigasi lanjutan (kemungkinan dependensi font/icon environment).

### Blocker

- Tidak ada blocker untuk fungsi close window.

### Next Step

- Verifikasi runtime bahwa tombol `Tutup` terlihat di kanan atas dan dapat menutup window.

---

## Update Desktop Close Button Overlay (2026-05-02)

### Ringkasan Perubahan

- Menambahkan tombol `Tutup` sebagai overlay langsung pada `MainWindow` dengan `Panel.ZIndex` tinggi agar tetap terlihat meskipun `TitleBar` WPF UI tidak merender tombol caption.
- Tombol overlay tetap memakai handler `OnCloseWindowClick` yang menutup window.

### File yang Diubah

- `desktop-app/src/Mau.Desktop/MainWindow.xaml`

### Hasil Verifikasi/Test

- `dotnet build desktop-app/Mau.Desktop.sln` sukses (0 warning, 0 error).

### Gap / Risiko

- Solusi ini adalah fallback visual; render tombol caption native/minimize/maximize bawaan `TitleBar` masih perlu investigasi terpisah jika ingin tampilan full native.

### Blocker

- Tidak ada blocker.

### Next Step

- Verifikasi runtime bahwa tombol `Tutup` overlay terlihat dan berfungsi pada semua tema (Light/Dark) serta DPI umum.

---

## Update Desktop Caption Buttons Style (2026-05-02)

### Ringkasan Perubahan

- Mengganti tombol teks `Tutup` menjadi caption buttons bergaya Windows/WPF UI di kanan atas (`Minimize`, `Maximize/Restore`, `Close`) dengan ikon `Segoe MDL2 Assets`.
- Menambahkan style hover/pressed agar perilaku visual mendekati title bar native (hover merah khusus tombol close).
- Menambahkan logic runtime untuk toggle ikon maximize/restore sesuai `WindowState`.

### File yang Diubah

- `desktop-app/src/Mau.Desktop/MainWindow.xaml`
- `desktop-app/src/Mau.Desktop/MainWindow.xaml.cs`

### Hasil Verifikasi/Test

- `dotnet build desktop-app/Mau.Desktop.sln` sukses (0 warning, 0 error).

### Gap / Risiko

- Ini masih custom caption area (bukan tombol native OS), sehingga behavior sangat spesifik native window (mis. hit-area/pixel-perfect per OS build) bisa sedikit berbeda.

### Blocker

- Tidak ada blocker.

### Next Step

- Verifikasi visual di runtime untuk Light/Dark theme dan pastikan tombol tetap visible pada scaling DPI tinggi.

---

## Update Desktop Title Bar Full Refactor (2026-05-02)

### Ringkasan Perubahan

- Menghapus konfigurasi lama `NavigationView.TitleBar` dan seluruh fallback tombol teks sebelumnya.
- Membuat ulang title bar custom secara penuh di `MainWindow` (row khusus atas) dengan 3 tombol kontrol window ala Windows: minimize, maximize/restore, close.
- Menambahkan perilaku drag window pada area title bar custom serta double-click untuk maximize/restore.
- Menjaga behavior close/minimize/maximize seluruhnya lewat handler code-behind yang eksplisit.

### File yang Diubah

- `desktop-app/src/Mau.Desktop/MainWindow.xaml`
- `desktop-app/src/Mau.Desktop/MainWindow.xaml.cs`

### Hasil Verifikasi/Test

- `dotnet build desktop-app/Mau.Desktop.sln` sukses (0 warning, 0 error).

### Gap / Risiko

- Karena ini custom caption bar, ada kemungkinan tuning kecil tambahan pada spacing/hit area untuk beberapa kombinasi DPI/scale monitor.

### Blocker

- Tidak ada blocker.

### Next Step

- Verifikasi runtime pada monitor utama user untuk memastikan 3 tombol selalu terlihat dan close berfungsi konsisten.

---

## Update Desktop App Branding Logo (2026-05-02)

### Ringkasan Perubahan

- Menambahkan logo MAU pada area title window custom di `MainWindow`.
- Mengatur icon aplikasi desktop (`exe`/window/taskbar) menggunakan file `.ico` dari logo MAU.
- Menambahkan aset branding ke project desktop pada folder `Assets/Brand`.

### File yang Diubah

- `desktop-app/src/Mau.Desktop/MainWindow.xaml`
- `desktop-app/src/Mau.Desktop/Mau.Desktop.csproj`
- `desktop-app/src/Mau.Desktop/Assets/Brand/mau-logo.png` (baru)
- `desktop-app/src/Mau.Desktop/Assets/Brand/mau-logo.ico` (baru)

### Hasil Verifikasi/Test

- `dotnet build desktop-app/Mau.Desktop.sln` sukses (0 warning, 0 error).

### Gap / Risiko

- File `.ico` saat ini dibuat dari ukuran sumber 64x64; bila butuh kualitas optimum di semua skala Windows, bisa dibuat multi-size `.ico` (16/24/32/48/64/128/256).

### Blocker

- Tidak ada blocker.

### Next Step

- Jalankan ulang aplikasi dan verifikasi logo tampil di title window serta icon aplikasi sudah berubah pada taskbar/alt-tab.

---

## Update Desktop Startup XAML Parse Fix (2026-05-02)

### Ringkasan Perubahan

- Memperbaiki crash startup `XamlParseException` pada `MainWindow`.
- Menghapus properti `Icon` dari root `FluentWindow` karena memicu error konversi resource saat parsing XAML.
- Branding tetap dipertahankan: logo title bar masih aktif, icon aplikasi tetap berasal dari `ApplicationIcon` di `.csproj`.

### File yang Diubah

- `desktop-app/src/Mau.Desktop/MainWindow.xaml`

### Hasil Verifikasi/Test

- `dotnet build desktop-app/Mau.Desktop.sln` sukses (0 warning, 0 error).

### Next Step

- Jalankan ulang dari Visual Studio dan verifikasi startup tidak lagi crash.

---

## Update Desktop Stock Opname Menu & Page (2026-05-02)

### Ringkasan Perubahan

- Menambahkan menu baru `Stock Opname` pada navigation utama desktop.
- Menambahkan halaman `Stock Opname` dengan layout yang meniru mockup `desktop-app/docs/mockup/sto.jpg`.
- Halaman mencakup dua section utama:
  - `Input Scan Rack`: field HAWB, pemilihan rack, tabel data rack-hawb, tombol `Clear` dan `Simpan`.
  - `Preview Rack system`: input pencarian HAWB dan grid kartu rack (`RS-0001` s.d. `RS-0012`).
- Menambahkan ViewModel + model data untuk placeholder operasional stock opname serta filtering rack berdasarkan keyword HAWB.
- Mendaftarkan page/viewmodel baru ke Dependency Injection container.

### File yang Diubah

- `desktop-app/src/Mau.Desktop/App.xaml.cs`
- `desktop-app/src/Mau.Desktop/ViewModels/MainWindowViewModel.cs`
- `desktop-app/src/Mau.Desktop/Models/StockOpnameRackItem.cs` (baru)
- `desktop-app/src/Mau.Desktop/ViewModels/StockOpnameViewModel.cs` (baru)
- `desktop-app/src/Mau.Desktop/Views/Pages/StockOpnamePage.xaml` (baru)
- `desktop-app/src/Mau.Desktop/Views/Pages/StockOpnamePage.xaml.cs` (baru)

### Hasil Verifikasi/Test

- `dotnet build desktop-app/Mau.Desktop.sln` sukses (0 warning, 0 error).

### Gap / Risiko

- Data pada halaman stock opname saat ini masih placeholder UI lokal (belum terhubung endpoint backend).
- Tombol `Simpan` baru memperbarui status lokal; persist final tetap perlu endpoint backend FastAPI.

### Next Step

- Sinkronkan kontrak API stock opname di backend (`materialize-fastapi`) lalu hubungkan service desktop ke endpoint resmi.

---

## Update Desktop Stock Opname Submenu Refactor (2026-05-02)

### Ringkasan Perubahan

- Menghapus elemen menu dummy (`Stock Opname / Input Scan Rack / Preview Rack system`) dari area konten halaman.
- Mengubah struktur `StockOpnamePage` menjadi submenu nyata berbasis switcher:
  - `Input Scan Rack`
  - `Preview Rack System`
- Membuat konten terpisah untuk masing-masing submenu dan ditampilkan bergantian berdasarkan submenu aktif.
- Menambahkan state submenu di `StockOpnameViewModel` agar tampilan aktif/nonaktif bisa dikontrol dari VM.

### File yang Diubah

- `desktop-app/src/Mau.Desktop/ViewModels/ViewModelBase.cs`
- `desktop-app/src/Mau.Desktop/ViewModels/StockOpnameViewModel.cs`
- `desktop-app/src/Mau.Desktop/Views/Pages/StockOpnamePage.xaml`
- `desktop-app/src/Mau.Desktop/Views/Pages/StockOpnamePage.xaml.cs`

### Hasil Verifikasi/Test

- `dotnet build desktop-app/Mau.Desktop.sln` sukses (0 warning, 0 error).

### Gap / Risiko

- Submenu saat ini masih di level page (bukan item tree terpisah pada navigation shell). Jika nanti dibutuhkan route/URL/page terpisah per submenu, perlu split ke page berbeda.

### Next Step

- Jika diinginkan, pecah `Input Scan Rack` dan `Preview Rack System` menjadi dua page independen di navigation tree desktop.

---

## Update Desktop Sidebar Submenu for Stock Opname (2026-05-02)

### Ringkasan Perubahan

- Memindahkan submenu `Input Scan Rack` dan `Preview Rack System` dari area konten ke sidebar `NavigationView`.
- Menjadikan `Stock Opname` sebagai parent menu expandable di sidebar dengan dua child item.
- Memecah konten menjadi dua page terpisah:
  - `StockOpnamePage` untuk `Input Scan Rack`.
  - `StockOpnamePreviewRackSystemPage` untuk `Preview Rack System`.
- Menyederhanakan `StockOpnameViewModel` dengan menghapus state submenu internal karena navigasi submenu kini ditangani sidebar.

### File yang Diubah

- `desktop-app/src/Mau.Desktop/ViewModels/MainWindowViewModel.cs`
- `desktop-app/src/Mau.Desktop/App.xaml.cs`
- `desktop-app/src/Mau.Desktop/ViewModels/StockOpnameViewModel.cs`
- `desktop-app/src/Mau.Desktop/Views/Pages/StockOpnamePage.xaml`
- `desktop-app/src/Mau.Desktop/Views/Pages/StockOpnamePage.xaml.cs`
- `desktop-app/src/Mau.Desktop/Views/Pages/StockOpnamePreviewRackSystemPage.xaml` (baru)
- `desktop-app/src/Mau.Desktop/Views/Pages/StockOpnamePreviewRackSystemPage.xaml.cs` (baru)

### Hasil Verifikasi/Test

- `dotnet build desktop-app/Mau.Desktop.sln` sukses (0 warning, 0 error).

### Next Step

- Jika diinginkan, tambahkan ikon khusus pada masing-masing child submenu agar visual hierarchy di sidebar lebih jelas.
