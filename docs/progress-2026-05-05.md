# Progress 2026-05-05

## Ringkasan Perubahan
- Membangun ulang halaman `ScanXrayCheckInPage` sesuai mockup menjadi layout operasional: input `MAWB`, dua card `X-Ray Image`, card respons endpoint TPS Online, dan area progress/status.
- Menambahkan auto-lookup backend saat `MAWB` terisi (dengan debounce), plus tombol `Lookup` untuk trigger manual.
- Menambahkan integrasi endpoint backend `GET /tpsonline/imp-in?no_bl_awb=...` melalui service layer desktop (`IScanXrayCheckInService` + `ScanXrayCheckInService`) agar ViewModel tetap patuh MVVM.
- Menambahkan pembacaan gambar X-Ray dari folder `Assets` (recursive scan, filter nama file xray/x-ray, maksimal 2 gambar) dan ditampilkan pada card.
- Menambahkan DTO respons API untuk field tampilan: `no_bl_awb`, `tgl_bl_awb`, `ref_num`, `nm_angkut`, `no_voy_flight`, `uraian_brg`.

## File yang Diubah
- `desktop-app/src/Mau.Desktop/Views/Pages/ScanXrayCheckInPage.xaml`
- `desktop-app/src/Mau.Desktop/ViewModels/ScanXrayCheckInViewModel.cs`
- `desktop-app/src/Mau.Desktop/App.xaml.cs`
- `desktop-app/src/Mau.Desktop/Services/ScanXrayCheckInService.cs`
- `desktop-app/src/Mau.Desktop/Services/IScanXrayCheckInService.cs`
- `desktop-app/src/Mau.Desktop/Api/TpsOnlineImpInRecordResponse.cs`
- `desktop-app/src/Mau.Desktop/Models/ScanXrayCheckInResult.cs`
- `desktop-app/src/Mau.Desktop/Models/XrayImageCardItem.cs`

## Hasil Verifikasi
- `dotnet build desktop-app/Mau.Desktop.sln -p:UseAppHost=false` berhasil.
- Hasil: 0 warning, 0 error.

## Gap / Risiko
- Saat ini pembacaan gambar mengandalkan nama file mengandung `xray` atau `x-ray`; jika naming berbeda maka card akan tampil placeholder.
- Endpoint backend mengembalikan list; implementasi desktop saat ini menampilkan data record pertama.

## Blocker
- Tidak ada blocker.

## Next Step
- Finalisasi konvensi folder/naming file gambar X-Ray operasional (misalnya `Assets/Xray/`) agar deteksi file konsisten.
- Jika dibutuhkan multi-record display dari endpoint, ubah bagian response card menjadi tabel list.

## Update Tambahan (Fix XamlParseException ProgressBar)
- Memperbaiki crash parsing XAML pada `ScanXrayCheckInPage`:
  - Error: `Set property 'RangeBase.Value' threw an exception` di line `ProgressBar.Value`.
- Akar masalah: binding default `ProgressBar.Value` bersifat `TwoWay` (RangeBase), sementara source value di ViewModel bukan target update dari UI.
- Perbaikan: ubah binding progress menjadi explicit `Mode=OneWay` pada:
  - `Value="{Binding ViewModel.ProgressValue, Mode=OneWay}"`
  - `IsIndeterminate="{Binding ViewModel.IsProgressIndeterminate, Mode=OneWay}"`

## Verifikasi Tambahan 13
- `dotnet build desktop-app/Mau.Desktop.sln -p:UseAppHost=false` berhasil.
- Hasil: 0 warning, 0 error.

## Update Tambahan (Fix Binding Null Image + Struktur Parent Menu)
- Memperbaiki warning runtime WPF `System.Windows.Data Error: 23` pada `Image.Source` ketika nilai path gambar `null`.
- Menambahkan converter baru `SafeImagePathToImageSourceConverter` yang mengembalikan `DependencyProperty.UnsetValue` untuk path kosong/tidak valid sehingga binding `Image.Source` tidak melempar konversi error.
- Mengaplikasikan converter tersebut pada binding `Image.Source` di `ScanXrayCheckInPage.xaml`.
- Memperbaiki struktur menu navigasi agar **parent tidak memiliki halaman** (`TargetPageType` dihapus pada parent):
  - `Stock Opname`
  - `Scan X-Ray`
  - `TPS Online`
- Child menu tetap menjadi pemegang halaman (`TargetPageType`) sesuai aturan navigasi.

## Verifikasi Tambahan 14
- `dotnet build desktop-app/Mau.Desktop.sln -p:UseAppHost=false` berhasil.
- Hasil: 0 warning, 0 error.

## Update Tambahan (Fix Path Folder X-Ray)
- Mengubah sumber pembacaan gambar X-Ray agar diarahkan ke folder eksplisit:
  - `C:\Users\wisnu\Documents\Belajar\materialize-project\desktop-app\src\Mau.Desktop\Assets\X-Ray`
- Menyesuaikan service agar fallback ke jalur relatif `Assets/X-Ray` jika path absolut tidak ditemukan.
- Menghapus filter nama file `xray/x-ray`; sekarang semua file gambar valid di folder `Assets/X-Ray` akan ditampilkan (maksimal 2 sesuai desain card).

## Verifikasi Tambahan 15
- `dotnet build desktop-app/Mau.Desktop.sln -p:UseAppHost=false` berhasil.
- Hasil: 0 warning, 0 error.

## Update Tambahan (Fix Response MAWB Tidak Tampil)
- Memperkuat parser respons endpoint TPS Online di `ScanXrayCheckInService`.
- Sebelumnya parsing memakai DTO statik; kini respons dibaca sebagai `JsonElement` agar lebih toleran terhadap variasi bentuk payload dan tipe nilai.
- Mendukung dua bentuk payload backend:
  - array langsung: `[ {...} ]`
  - object wrapper: `{ "data": [ {...} ] }`
- Mapping field (`no_bl_awb`, `tgl_bl_awb`, `ref_num`, `nm_angkut`, `no_voy_flight`, `uraian_brg`) kini dibaca case-insensitive dan aman untuk tipe non-string (number/bool).
- Menghapus log debug non-produksi yang tidak diperlukan dari flow service.

## Verifikasi Tambahan 16
- `dotnet build desktop-app/Mau.Desktop.sln -p:UseAppHost=false` berhasil.
- Hasil: 0 warning, 0 error.

## Update Tambahan (Stabilkan Tampilan Response MAWB)
- Menambahkan proteksi race-condition pada `ScanXrayCheckInViewModel` agar hasil request lama tidak menimpa hasil request terbaru.
- Menambahkan `lookup request version` berbasis `Interlocked` untuk memastikan hanya respons lookup terbaru yang boleh update UI.
- Mengubah auto-lookup agar aktif saat panjang MAWB sudah minimal 8 karakter (mengurangi request parsial saat scanner masih mengetik).
- Menambahkan shortcut `Enter` pada textbox MAWB untuk trigger `LookupNowCommand` secara eksplisit.
- Menambahkan status sukses yang menyertakan `RefNum` agar mudah validasi cepat bahwa data response sudah termapping.

## Verifikasi Tambahan 17
- `dotnet build desktop-app/Mau.Desktop.sln -p:UseAppHost=false` berhasil.
- Hasil: 0 warning, 0 error.

## Update Tambahan (Fix Binding Context Response MAWB)
- Menemukan mismatch binding context pada `ScanXrayCheckInPage`:
  - Code-behind sudah menggunakan `DataContext = ViewModel`, tetapi XAML masih binding ke path `ViewModel.*`.
- Mengubah seluruh binding di halaman `ScanXrayCheckInPage.xaml` ke path langsung terhadap ViewModel aktif (contoh: `Mawb`, `RefNum`, `StatusMessage`, dst).
- Menetapkan `DataContext` page secara eksplisit ke instance `ViewModel` di code-behind.
- Dampak: field response (`no_bl_awb`, `tgl_bl_awb`, `ref_num`, `nm_angkut`, `no_voy_flight`, `uraian_brg`) kini dapat menerima update dari ViewModel setelah request backend sukses.

## Verifikasi Tambahan 18
- `dotnet build desktop-app/Mau.Desktop.sln -p:UseAppHost=false` berhasil.
- Hasil: 0 warning, 0 error.
