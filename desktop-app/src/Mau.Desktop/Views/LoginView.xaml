<UserControl x:Class="Mau.Desktop.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid>
        <Border Width="420"
                Padding="24"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                CornerRadius="10"
                Background="White">
            <StackPanel>
                <TextBlock Text="Login Operator"
                           FontSize="24"
                           FontWeight="SemiBold"
                           Margin="0,0,0,16" />

                <TextBlock Text="Username" Margin="0,0,0,4" />
                <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12" Height="29" />

                <TextBlock Text="Password" Margin="0,0,0,4" />
                <PasswordBox x:Name="PasswordInput" Margin="0,0,0,12" PasswordChanged="PasswordInput_OnPasswordChanged" Height="34" />

                <Button Content="Masuk"
                        Height="34"
                        Command="{Binding LoginCommand}" />

                <TextBlock Margin="0,12,0,0"
                           Foreground="Firebrick"
                           TextWrapping="Wrap"
                           Text="{Binding ErrorMessage}" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
