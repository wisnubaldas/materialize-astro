# Progress 2026-05-12

## Ringkasan Perubahan

- Refactor modul `EDI > FFM` agar konsisten dengan flow baru modul EDI lain:
  - aksi tabel ditata menjadi `Detail`, `Cargo-IMP`, `Cargo-XML`, dan `Send Email`;
  - `Send Email` masih placeholder (tanpa aksi kirim) sesuai pola tahap sebelumnya;
  - preview format dipusatkan pada modal message dengan tab `Cargo-IMP` dan `Cargo-XML`.
- Menambahkan output `cargo_xml` pada backend preview FFM:
  - memperluas schema response `FfmPreviewOut`;
  - menambahkan builder `Cargo-XML` preview dari data `build_up_header` + `build_up_detail`.
- Membersihkan kode frontend legacy FFM:
  - menghapus komponen lama `SendEmailFfm.jsx`;
  - menghapus generator frontend `ffmGenerator.js`;
  - menghapus route slug legacy `astro/src/pages/edi/send-email/[slug].astro`;
  - menghapus export komponen legacy dari `astro/src/components/react/edi/index.js`.

## File yang Diubah

- `astro/src/components/react/edi/ffmDatatables.jsx`
- `astro/src/components/react/edi/index.js`
- `astro/src/components/react/edi/SendEmailFfm.jsx` (dihapus)
- `astro/src/components/react/edi/ffmGenerator.js` (dihapus)
- `astro/src/pages/edi/send-email/[slug].astro` (dihapus)
- `materialize-fastapi/app/schemas/ffm_preview_schema.py`
- `materialize-fastapi/app/services/warehouse_service.py`

## Hasil Verifikasi

- `npm run build` (dari folder `astro/`) lulus tanpa error.
- `poetry run ruff check app/services/warehouse_service.py app/schemas/ffm_preview_schema.py` lulus (`All checks passed`).
- `python -m compileall app/services/warehouse_service.py app/schemas/ffm_preview_schema.py` lulus tanpa error.

## Gap / Risiko

- `Cargo-XML` pada preview FFM saat ini berupa representasi XML internal untuk kebutuhan operasional UI preview, belum claim sebagai implementasi penuh standar `XFFM` production exchange.
- Validasi sintaks `FFM/8` dan mapping `XFFM` belum diuji end-to-end ke parser eksternal pada sampel data produksi pada sesi ini.

## Blocker

- Tidak ada blocker teknis.

## Next Step

- Jika flow `Send Email FFM` siap, lanjutkan endpoint khusus kirim FFM dengan audit log pengiriman dan status retry.
- Tambahkan validasi parser eksternal (Cargo-IMP/Cargo-XML) berbasis sampel data produksi untuk memastikan konsistensi format output.

---

## Update Cleanup Komponen EDI (2026-05-12)

### Ringkasan Perubahan

- Audit ulang dependensi komponen di `astro/src/components/react/edi`.
- Konfirmasi `astro/src/components/react/edi/fwbGenerator.js` masih dipakai aktif oleh `FwbComposer` untuk preview message saat edit form FWB.
- Menghapus komponen orphan yang sudah tidak direferensikan:
  - `astro/src/components/react/edi/SendEmailFwb.jsx`.
- Normalisasi API client EDI dengan menghapus method yang hanya dipakai komponen orphan:
  - `sendEmailFwb` pada `astro/src/lib/api/edi.js`.

### File yang Diubah

- `astro/src/components/react/edi/SendEmailFwb.jsx` (dihapus)
- `astro/src/lib/api/edi.js`

### Hasil Verifikasi

- `rg` dependency check: tidak ada referensi `SendEmailFwb` / `sendEmailFwb(...)` tersisa di frontend.
- `npm run build` (dari folder `astro/`) lulus tanpa error.

### Gap / Risiko

- Endpoint backend `/edi/send-email-fwb` masih tersedia namun saat ini tidak dipakai oleh frontend flow baru.

### Blocker

- Tidak ada blocker teknis.

### Next Step

- Jika flow email FWB diaktifkan kembali, gunakan endpoint dan komponen baru yang mengikuti arsitektur FWB composer + datatable saat ini (tanpa route slug legacy).

---

## Update Preview FWB via Backend (2026-05-12)

### Ringkasan Perubahan

- Mengubah mekanisme preview pada modul `EDI > FWB`:
  - tab `Cargo-IMP` pada halaman form FWB dihapus;
  - ditambahkan tombol `Preview FWB` di samping tombol `Kembali ke DataTable`;
  - preview sekarang mengambil hasil generator backend (Cargo-IMP dan Cargo-XML) berbasis payload form saat ini, ditampilkan lewat modal frontend.
- Menambahkan endpoint backend khusus preview tanpa simpan DB:
  - `POST /edi/fwb-preview` menerima payload form FWB dan mengembalikan `cargo_imp` + `cargo_xml`.
- Membersihkan komponen frontend yang tidak lagi dipakai:
  - `astro/src/components/react/edi/fwbGenerator.js` dihapus karena generator FWB sudah dipindahkan ke backend untuk flow preview.

### File yang Diubah

- `materialize-fastapi/app/api/edi.py`
- `materialize-fastapi/app/services/edi_service.py`
- `materialize-fastapi/app/schemas/fwb_preview_request_body.py`
- `astro/src/lib/api/edi.js`
- `astro/src/components/react/edi/FwbComposer.jsx`
- `astro/src/components/react/edi/fwbGenerator.js` (dihapus)

### Hasil Verifikasi

- `poetry run ruff check app/api/edi.py app/services/edi_service.py app/schemas/fwb_preview_request_body.py` lulus (`All checks passed`).
- `python -m compileall app/api/edi.py app/services/edi_service.py app/schemas/fwb_preview_request_body.py` lulus tanpa error.
- `npm run build` (dari folder `astro/`) lulus tanpa error.

### Gap / Risiko

- Belum ada indikator visual khusus yang menandakan preview sudah sinkron dengan perubahan input terakhir jika user melakukan edit sangat cepat berturut-turut.

### Blocker

- Tidak ada blocker teknis.

### Next Step

- Jika diperlukan, tambahkan indikator UI bahwa preview yang tampil adalah hasil validasi payload terbaru sebelum proses simpan.

---

## Hotfix Save FWB Decimal Casting (2026-05-12)

### Ringkasan Perubahan

- Memperbaiki error simpan FWB `Incorrect decimal value: ''` pada kolom numerik (`volume`, `rate`, `amount`, dan field numeric lain) dengan normalisasi payload di service layer backend.
- Menambahkan parser angka aman di `EdiService`:
  - `_parse_int(...)` untuk kolom integer.
  - `_parse_decimal(...)` untuk kolom decimal.
- Seluruh field numerik pada mapping `_build_fwb_values(...)` sekarang dikonversi dulu (`''` -> `None`) sebelum insert/update ke tabel `fwb`.

### File yang Diubah

- `materialize-fastapi/app/services/edi_service.py`

### Hasil Verifikasi

- `poetry run ruff check app/services/edi_service.py` lulus (`All checks passed`).
- `python -m compileall app/services/edi_service.py` lulus tanpa error.

### Gap / Risiko

- Nilai numerik non-valid (mis. teks acak) saat ini dinormalisasi ke `None`; jika nanti dibutuhkan strict rejection, bisa ditingkatkan menjadi validasi `422` eksplisit.

### Blocker

- Tidak ada blocker teknis.

### Next Step

- Jika diinginkan, saya bisa lanjut tambah validasi backend agar field numerik wajib tertentu melempar `422` saat bukan angka valid, bukan disimpan sebagai `NULL`.

---

## Align Table `fwb` dengan Form Frontend + Migrate (2026-05-12)

### Ringkasan Perubahan

- Menormalkan struktur tabel `fwb` agar konsisten dengan aturan form `FWB` frontend (`fwbValidation.js`), terutama:
  - panjang field kode (`origin`, `destination`, `awb_prefix`, `awb_number`, `flight_carrier`, dll);
  - panjang field teks (`shipper_name`, `consignee_name`, `issue_place`, `issued_by`, dll);
  - presisi kolom numerik (`weight`, `gross_weight`, `chargeable_weight`, `volume`, `rate`, `total_charge`, dll).
- Menambahkan migration baru yang:
  - memastikan kolom FWB extended ada walau environment sebelumnya melewati branch migration lama;
  - melakukan truncation aman untuk data teks sebelum shrink tipe/length;
  - alter tipe kolom ke spesifikasi yang selaras dengan frontend.
- Menyesuaikan model SQLAlchemy `Fwb` agar setara dengan hasil migration.

### File yang Diubah

- `materialize-fastapi/app/models/BaseDB1/fwb.py`
- `materialize-fastapi/migrations/versions/8c1addb3c06c_normalize_fwb_columns_to_match_frontend_.py`

### Hasil Verifikasi

- `poetry run ruff check app/models/BaseDB1/fwb.py migrations/versions/8c1addb3c06c_normalize_fwb_columns_to_match_frontend_.py app/services/edi_service.py` lulus.
- `python -m compileall app/models/BaseDB1/fwb.py migrations/versions/8c1addb3c06c_normalize_fwb_columns_to_match_frontend_.py app/services/edi_service.py` lulus.
- `poetry run alembic upgrade head` berhasil, current revision:
  - `8c1addb3c06c (head)`.
- Verifikasi cepat struktur DB (`SHOW COLUMNS FROM fwb`) menunjukkan tipe kunci sudah sesuai target, contoh:
  - `weight/gross_weight/chargeable_weight`: `decimal(10,3)`
  - `volume`: `decimal(12,3)`
  - `rate/total_charge/amount`: `decimal(14,2)`
  - `message_type`: `varchar(3)`
  - `routing_list`: `varchar(120)`

### Gap / Risiko

- Truncation data lama dilakukan untuk kolom yang dipersempit (agar migration tidak gagal). Data historis yang melebihi batas baru akan terpotong sesuai panjang target.

### Blocker

- Tidak ada blocker teknis.

### Next Step

- Lanjut smoke test simpan FWB dari UI untuk memastikan alur end-to-end stabil setelah normalisasi tabel.

---

## Hotfix FWB Builder Parse2 FLT (2026-05-12)

### Ringkasan Perubahan

- Memperbaiki generator `FWB Cargo-IMP` backend agar menyertakan segmen `FLT` tepat setelah header AWB:
  - format yang dihasilkan: `FLT/<carrier+flight_number>/<DD>`.
- Menambahkan helper normalisasi untuk:
  - pembentukan `flight_id` dari `flight_carrier` + `flight_number/flight_no`,
  - pembentukan `flight_day` dari `flight_date`.
- Menormalkan `AGT` account menjadi alfanumerik (`max 14`) agar lebih aman untuk parser Cargo-IMP.

### File yang Diubah

- `materialize-fastapi/app/libs/fwb_message_builder.py`

### Hasil Verifikasi

- `poetry run ruff check app/libs/fwb_message_builder.py` lulus.
- `python -m compileall app/libs/fwb_message_builder.py` lulus.
- Uji sample payload menghasilkan segmen baru:
  - `FLT/FX519/15`
  sehingga urutan message tidak lagi langsung lompat dari header ke `SHP`.

### Gap / Risiko

- Validasi parse2 final tetap bergantung pada kombinasi data bisnis lain (mis. nilai field optional/conditional lain per maskapai).

### Blocker

- Tidak ada blocker teknis.

### Next Step

- Lanjutkan re-validate message terbaru di parse2; jika masih ada error rule berikutnya, lakukan penyesuaian segment per rule yang ditunjuk parser.

### Update

- Menambahkan fallback `RTG` wajib pada builder FWB setelah `FLT` untuk memenuhi rule routing parse2:
  - contoh fallback baru: `RTG/SINFX` ketika routing user kosong.
- Menyesuaikan format `LOC` pada segmen `SHP` dan `CNE` menjadi dua baris sesuai gaya Cargo-IMP:
  - `LOC/<CITY>[/<STATE>]`
  - `/<COUNTRY>`

---

## Hotfix FWB Builder Rate Description Closure (2026-05-12)

### Ringkasan Perubahan

- Memperbaiki output backend `FWB Cargo-IMP` yang sebelumnya berhenti pada segmen rate description:
  - `/NG/<goods>`
  - `/2/ND//NDA`
- Menambahkan segmen lanjutan yang dibutuhkan grammar `FWB/17` setelah `RTD`:
  - `PPD`/`COL` charge declaration berdasarkan `weight_charge_pp_cc`;
  - `CER` shipper certification;
  - `ISU` issue date/place/by;
  - `REF` sender reference dengan origin airport code.
- Menambahkan helper formatter kecil untuk normalisasi text Cargo-IMP, nilai amount, tanggal `ISU`, dan `REF`.

### File yang Diubah

- `materialize-fastapi/app/libs/fwb_message_builder.py`

### Hasil Verifikasi

- `poetry run ruff check app/libs/fwb_message_builder.py` lulus.
- `python -m compileall app/libs/fwb_message_builder.py` lulus.
- Smoke test sample AWB `023-51762830` menghasilkan lanjutan message:
  - `PPD/WT0`
  - `/CT0`
  - `CER/FEDEX EXPRESS INDONE`
  - `ISU/15MAY26/JAKARTA/FEDEX EXPRESS INDONE`
  - `REF///AGT/FEDEXEXPRESSINDON/CGK`
- Validasi parse2 `FWB17` untuk sample baru memantulkan message tanpa parser error.

### Gap / Risiko

- Nilai charge declaration saat data charge kosong tetap fallback ke `0` agar sintaks valid; jika bisnis membutuhkan rejection untuk charge kosong, perlu validasi backend `422` terpisah.
- Builder masih memakai satu rating line dasar dan belum memodelkan seluruh variasi optional Cargo-IMP seperti multi rating line, volume, dimensions detail, `OTH`, `SSR`, `OSI`, dan `OCI` secara penuh.

### Blocker

- Tidak ada blocker teknis.

### Next Step

- Tambahkan validasi backend eksplisit untuk field FWB mandatory sebelum generate/send agar data kosong tidak hanya difallback untuk kebutuhan parser.
