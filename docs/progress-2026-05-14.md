# Progress 2026-05-14

## Ringkasan Perubahan

- Memperbaiki konfigurasi NativeWind agar Metro membaca `global.css` melalui `input: './global.css'`.
- Merapikan tampilan Login, Dashboard, dan Build Up Checklist agar tidak dominan hitam dan lebih stabil di layar HP kecil.
- Menambahkan dukungan runtime Expo Web melalui dependency `react-dom`, `react-native-web`, dan `@expo/metro-runtime`.
- Menambahkan dokumentasi cara membuka mobile UI melalui browser dengan `npm run web`.
- Memperbaiki runtime error `nativewind.cssInterop is not a function` dengan memindahkan styling `LinearGradient` ke prop `style`.
- Menambahkan basic screen layout di `src/components/layout/` agar setiap screen memakai safe area, scroll, keyboard handling, dan web max-width yang konsisten.
- Memperbaiki auth boot agar kegagalan storage lokal tidak membuat loading screen stuck atau blank putih.
- Mengupdate `mobile-app/mobile_agent.md` dengan pola layout screen dan catatan kompatibilitas NativeWind saat ini.
- Menambahkan `mobile-app/adops_template.md` sebagai checklist bertahap untuk mengadopsi pola `expo-nativewind-template` ke MAU APP Mobile tanpa mengubah konsep JavaScript-first.
- Merevisi rencana adopsi agar Expo Router menjadi target migrasi routing, dengan route JavaScript tipis di `app/` dan screen tetap di `src/screens/`.
- Menyinkronkan `mobile-app/mobile_agent.md` agar `src/navigation` diperlakukan sebagai compatibility layer sementara selama migrasi dari React Navigation.
- Menyelesaikan Fase 0 Audit Awal untuk adopsi template NativeWind: branch kerja diamankan, status worktree dicatat, dependency existing didata, export baseline web dan Android diverifikasi, lalu artifact audit dibersihkan.
- Menyelesaikan Fase 1 Mapping Template Sumber ke Project Ini: komponen UI dasar yang aman diadopsi sudah dipetakan, komponen advanced ditunda, dependency minimal dan dependency tertunda sudah dicatat, serta mapping komponen existing ke target `src/components/ui/` sudah diperbarui.
- Menyelesaikan Fase 2 Dependency Minimal: dependency UI dasar dan Expo Router dipasang, range React Navigation disesuaikan dengan rekomendasi Expo SDK 54, verifikasi doctor/export berhasil, dan artifact export dibersihkan.
- Menyelesaikan Fase 2A Migrasi Routing ke Expo Router: root layout dan route JavaScript dibuat, `main` diarahkan ke `expo-router/entry`, auth gate dipindah ke route, dan navigasi Build Up memakai callback berbasis Expo Router.
- Menyelesaikan Fase 3 Utility Dasar UI: menambahkan `cn.js`, `text-context.js`, dan mulai memakai `cn()` di `AppButton` tanpa mengubah API/tampilan komponen.
- Menyelesaikan Fase 4 Komponen `Text`: menambahkan komponen text reusable dengan variant dasar dan mulai memakainya di `BuildUpChecklistScreen`.
- Menyelesaikan Fase 5-8 komponen UI: menambahkan `Button`, `Input`, `Card`, `Badge`, `Separator`, `Spinner`, barrel export `src/components/ui/index.js`, serta wrapper compatibility untuk `AppButton`, `AppInput`, dan `InfoCard`.
- Menyelesaikan Fase 9 Theme Token: menambahkan token semantik CSS-first di `global.css`, memakai token tersebut di primitive UI utama, dan menjaga project tetap tanpa `tailwind.config.js` tambahan.
- Menyelesaikan Fase 10 Refactor Screen ke UI Kit: Login, Dashboard, dan Build Up Checklist kini memakai komponen `src/components/ui` secara langsung tanpa mengubah auth, service, atau flow bisnis.
- Menyelesaikan Fase 11 Komponen Menengah: mengevaluasi `switch`, `checkbox`, `dialog`, `sheet`, dan `drawer`; hanya `drawer` yang diimplementasikan karena langsung memperbaiki bug menu Dashboard tanpa dependency native baru.
- Menyelesaikan Fase 12 Permission Components: memasang `expo-camera`, menambahkan barcode scanner untuk field AWB/MAWB dan ULD di Build Up Checklist, mempertahankan `QrScanner` untuk kebutuhan nanti, serta mendokumentasikan permission camera.
- Menyelesaikan Fase 13 Cleanup Struktur Lama: menghapus wrapper compatibility lama, menghapus `AppNavigator`/`src/navigation`, menghapus dependency langsung React Navigation, dan menjadikan UI kit + Expo Router sebagai struktur aktif.
- Menyelesaikan Fase 14 Dokumentasi dan Agent Update: menyinkronkan `README.md`, `mobile_agent.md`, checklist, dan progress report dengan struktur UI kit serta Expo Router final.
- Menjalankan Fase 15 Acceptance Review untuk web: Login, Dashboard, Build Up Checklist, tombol Scan, ukuran search field, reload route, dan console issue berhasil diverifikasi lewat headless Chromium.
- Memperbaiki kontras hamburger menu Dashboard: backdrop drawer dibuat semi-gelap, panel menu memakai background terang eksplisit, item menu diberi card surface, dan tombol hamburger dibuat biru agar terlihat konsisten dengan UI Dashboard.
- Memperkuat komponen `DrawerPanel` agar lebar, tinggi penuh, background, border, dan shadow memakai `style` React Native eksplisit; ini mencegah panel transparan/terlihat seperti item mengambang di Expo Go.

## File yang Diubah

- `mobile-app/App.js`
- `mobile-app/README.md`
- `mobile-app/adops_template.md`
- `mobile-app/metro.config.js`
- `mobile-app/global.css`
- `mobile-app/package.json`
- `mobile-app/package-lock.json`
- `mobile-app/app.json`
- `mobile-app/README.md`
- `mobile-app/app/_layout.js`
- `mobile-app/app/index.js`
- `mobile-app/app/login.js`
- `mobile-app/app/build-up-checklist.js`
- `mobile-app/src/components/ui/utils/cn.js`
- `mobile-app/src/components/ui/utils/text-context.js`
- `mobile-app/src/components/ui/text.js`
- `mobile-app/src/components/ui/button.js`
- `mobile-app/src/components/ui/input.js`
- `mobile-app/src/components/ui/card.js`
- `mobile-app/src/components/ui/drawer.js`
- `mobile-app/src/components/ui/barcode-scanner.js`
- `mobile-app/src/components/ui/qr-scanner.js`
- `mobile-app/src/components/ui/badge.js`
- `mobile-app/src/components/ui/separator.js`
- `mobile-app/src/components/ui/spinner.js`
- `mobile-app/src/components/ui/index.js`
- `mobile-app/src/styles/theme.js`
- `mobile-app/babel.config.js`
- `mobile-app/App.js`
- `mobile-app/src/components/layout/ScreenLayout.js`
- `mobile-app/src/components/layout/ScreenHeader.js`
- `mobile-app/src/contexts/AuthContext.js`
- `mobile-app/src/screens/LoginScreen.js`
- `mobile-app/src/screens/DashboardScreen.js`
- `mobile-app/src/screens/BuildUpChecklistScreen.js`
- `mobile-app/mobile_agent.md`
- `docs/progress-2026-05-14.md`

## Hasil Verifikasi

- `npm run doctor` berhasil, 17/17 checks passed.
- `npx expo export --platform web --output-dir .expo-web-check` berhasil.
- `npx expo export --platform android --output-dir .expo-android-check` berhasil.
- `npx expo export --clear --platform web --output-dir .expo-web-check` berhasil setelah cache Metro dibersihkan.
- `npx expo export --clear --platform android --output-dir .expo-android-check` berhasil setelah cache Metro dibersihkan.
- `npm run doctor` berhasil, 17/17 checks passed.
- Pencarian `cssInterop` di `src/` sudah bersih, sehingga import runtime yang memicu blank screen tidak lagi dipakai.
- Output export sementara sudah dihapus agar tidak menjadi build artifact repo.
- Branch awal `master` tracking `origin/master`; untuk audit lanjutan sudah dibuat branch lokal `mobile/adops-audit-2026-05-14` tanpa upstream.
- `git status --short` menunjukkan worktree sudah berisi banyak perubahan existing sebelum Fase 0 dilanjutkan. Perubahan mobile yang relevan berada di `mobile-app/`; catatan root/docs seperti `AGENTS.md` dan `docs/progress-2026-05-13.md` diperlakukan sebagai perubahan terpisah yang tidak boleh direvert.
- Dependency existing utama dari `mobile-app/package.json`: Expo `^54.0.0`, React `19.1.0`, React Native `0.81.5`, NativeWind `^5.0.0-preview.3`, React Navigation `^7`, AsyncStorage `2.2.0`, Expo Linear Gradient, Expo Font, Expo Status Bar, React Native Web, React Native Reanimated, dan Tailwind CSS `^4.3.0`.
- `npm run doctor` berhasil untuk audit Fase 0, 17/17 checks passed.
- `npx expo export --clear --platform web --output-dir .expo-web-check` berhasil untuk baseline Fase 0.
- `npx expo export --clear --platform android --output-dir .expo-android-check` berhasil untuk baseline Fase 0.
- Folder `.expo-web-check` dan `.expo-android-check` sudah dihapus setelah export selesai.
- Template sumber `https://github.com/chvvkrishnakumar/expo-nativewind-template` sudah diaudit melalui clone sementara di luar repo kerja. Struktur template berisi `components/ui/*.tsx`, `app/*.tsx`, `tsconfig.json`, dan `nativewind-env.d.ts`, sehingga tidak boleh dicopy mentah ke MAU APP Mobile.
- Komponen tahap pertama yang dipilih: `Text`, `Button`, `Input`, `Card`, `Badge`, `Separator`, dan `Spinner`.
- Komponen yang ditunda: `dialog`, `sheet`, `drawer`, `checkbox`, `switch`, `select`, dan `permission-requester`.
- Dependency minimal yang relevan dari template: `class-variance-authority`, `clsx`, dan `tailwind-merge`.
- Dependency yang ditunda: `lucide-react-native`, `@gorhom/bottom-sheet`, `react-native-gesture-handler`, `react-native-svg`, serta permission Expo modules seperti `expo-camera`, `expo-location`, `expo-media-library`, `expo-notifications`, dan `expo-contacts`.
- Mapping existing ke target: `AppButton.js` ke `src/components/ui/button.js`, `AppInput.js` ke `src/components/ui/input.js`, `InfoCard.js` ke `src/components/ui/card.js`, inline `Text` ke `src/components/ui/text.js`, inline divider ke `src/components/ui/separator.js`, dan loading indicator ke `src/components/ui/spinner.js`.
- Dependency UI minimal yang ditambahkan pada Fase 2: `class-variance-authority`, `clsx`, dan `tailwind-merge`.
- Dependency Expo Router yang ditambahkan pada Fase 2: `expo-router`, `expo-linking`, `expo-constants`, dan `expo-splash-screen`.
- `npx expo install` menambahkan plugin `expo-router` ke `mobile-app/app.json`.
- `package.json` `main` tetap `node_modules/expo/AppEntry.js` pada Fase 2. Perubahan ke `expo-router/entry` ditunda sampai Fase 2A ketika folder `app/` dan route JavaScript sudah dibuat, agar baseline export tidak rusak sebelum migrasi routing benar-benar dimulai.
- `npm run doctor` sempat gagal karena `expo install --check --json` meminta range package React Navigation sesuai SDK Expo 54. Range `@react-navigation/native` disesuaikan ke `^7.1.8` dan `@react-navigation/native-stack` ke `^7.3.16`; setelah itu `npm run doctor` berhasil, 17/17 checks passed.
- `npx expo export --clear --platform web --output-dir .expo-web-check` berhasil setelah dependency Fase 2.
- `npx expo export --clear --platform android --output-dir .expo-android-check` berhasil setelah dependency Fase 2.
- Folder `.expo-web-check` dan `.expo-android-check` sudah dihapus setelah verifikasi Fase 2.
- `mobile-app/package.json` `main` sudah diganti ke `expo-router/entry` pada Fase 2A setelah route JavaScript tersedia.
- `app/_layout.js` menjadi root provider baru untuk `SafeAreaProvider`, `AuthProvider`, `StatusBar`, dan Expo Router `Stack`.
- `app/index.js` menjadi auth gate dashboard: menampilkan loading saat auth boot, redirect ke `/login` jika belum authenticated, dan render `DashboardScreen` jika authenticated.
- `app/login.js` render `LoginScreen` dan redirect ke `/` jika user sudah authenticated.
- `app/build-up-checklist.js` render `BuildUpChecklistScreen`, guard user unauthenticated ke `/login`, dan memakai `router.back()` atau `router.replace('/')` untuk tombol back/close.
- `DashboardScreen.js` menerima callback `onOpenBuildUpChecklist` dari route untuk memanggil `router.push('/build-up-checklist')`; fallback React Navigation lama tetap ada selama `AppNavigator` dipertahankan.
- `BuildUpChecklistScreen.js` menerima callback `onBack` dari route untuk navigasi Expo Router; fallback `navigation.goBack()` tetap ada untuk compatibility.
- `npm run doctor` berhasil setelah Fase 2A, 17/17 checks passed.
- `npx expo export --clear --platform web --output-dir .expo-web-check` berhasil melalui `node_modules/expo-router/entry.js`; bundle web utama sekitar 1.54 MB.
- `npx expo export --clear --platform android --output-dir .expo-android-check` berhasil melalui `node_modules/expo-router/entry.js`; bundle Android utama sekitar 4.68 MB.
- Folder `.expo-web-check` dan `.expo-android-check` sudah dihapus setelah verifikasi Fase 2A.
- Expo web dev server untuk smoke test manual sudah berjalan di `http://localhost:8081` melalui `expo-router/entry.js`; log diarahkan ke `%TEMP%\\mau-mobile-expo-router-web.log` agar tidak menjadi artifact repo.
- `src/components/ui/utils/cn.js` dibuat memakai `clsx` dan `tailwind-merge` untuk merge class NativeWind/Tailwind secara konsisten.
- `src/components/ui/utils/text-context.js` dibuat untuk meneruskan class text dari komponen compound seperti `Button`, `CardTitle`, atau `Badge` pada fase berikutnya.
- `AppButton.js` mulai memakai `cn()` untuk menyusun `className` tombol, sehingga utility sudah tervalidasi minimal oleh komponen UI existing.
- `npm run doctor` berhasil setelah Fase 3, 17/17 checks passed.
- `npx expo export --clear --platform web --output-dir .expo-web-check` berhasil setelah Fase 3; bundle web utama sekitar 1.57 MB.
- `npx expo export --clear --platform android --output-dir .expo-android-check` berhasil setelah Fase 3; bundle Android utama sekitar 4.74 MB.
- Folder `.expo-web-check` dan `.expo-android-check` sudah dihapus setelah verifikasi Fase 3.
- `src/components/ui/text.js` dibuat sebagai wrapper React Native `Text` dengan variant `default`, `title`, `subtitle`, `label`, `muted`, `error`, dan `code`.
- `BuildUpChecklistScreen.js` direfactor sebagai screen pertama yang memakai `Text` baru untuk label field, heading, dan subtitle.
- `npm run doctor` berhasil setelah Fase 4, 17/17 checks passed.
- `npx expo export --clear --platform web --output-dir .expo-web-check` berhasil setelah Fase 4; bundle web utama sekitar 1.58 MB.
- `npx expo export --clear --platform android --output-dir .expo-android-check` berhasil setelah Fase 4; bundle Android utama sekitar 4.74 MB.
- Folder `.expo-web-check` dan `.expo-android-check` sudah dihapus setelah verifikasi Fase 4.
- `src/components/ui/button.js` dibuat dengan variant `default`, `secondary`, `outline`, `ghost`, `destructive`, `link` dan size `default`, `sm`, `lg`, `icon`; memakai `Pressable`, prop `android_ripple`, dan pressed opacity kecil untuk iOS.
- `AppButton.js` menjadi wrapper compatibility ke `ui/button.js`, sehingga tombol Login dan footer Build Up sudah melewati komponen UI baru.
- `src/components/ui/input.js` dibuat dengan variant `default` dan `error`, memakai React Native `TextInput`, serta `placeholderTextColor` eksplisit.
- `AppInput.js` menjadi wrapper compatibility ke `ui/input.js`; field Build Up memakai `Input` langsung.
- `src/components/ui/card.js` dibuat dengan `Card`, `CardHeader`, `CardTitle`, `CardDescription`, `CardContent`, dan `CardFooter`.
- `InfoCard.js` menjadi wrapper compatibility ke `Card`; container form Login serta kartu statistik Dashboard memakai `Card`.
- `src/components/ui/badge.js`, `separator.js`, `spinner.js`, dan `index.js` dibuat sebagai komponen tambahan ringan.
- Route auth gate `app/index.js`, `app/login.js`, dan `app/build-up-checklist.js` memakai `Spinner`.
- Dashboard memakai `Badge` untuk label kecil dan `Separator` untuk garis menu.
- `npm run doctor` berhasil setelah Fase 5-8, 17/17 checks passed.
- `npx expo export --clear --platform web --output-dir .expo-web-check` berhasil setelah Fase 5-8; bundle web utama sekitar 1.58 MB.
- `npx expo export --clear --platform android --output-dir .expo-android-check` berhasil setelah Fase 5-8; bundle Android utama sekitar 4.75 MB.
- Folder `.expo-web-check` dan `.expo-android-check` sudah dihapus setelah verifikasi Fase 5-8.
- `global.css` memakai `@theme` Tailwind CSS 4 untuk token `background`, `foreground`, `primary`, `muted`, `border`, `card`, dan `destructive`, plus token foreground pendamping untuk kontras.
- Primitive UI `Text`, `Button`, `Input`, `Card`, `Badge`, dan `Separator` mulai memakai class token semantik seperti `text-foreground`, `bg-primary`, `bg-card`, `bg-muted`, `border-border`, dan `text-destructive`.
- `tailwind.config.js` tidak dibuat pada Fase 9 karena setup NativeWind saat ini sudah stabil dengan konfigurasi CSS-first; ini menghindari copy mentah dari template sumber yang TypeScript-first.
- `npm run doctor` berhasil setelah Fase 9, 17/17 checks passed.
- `npx expo export --clear --platform web --output-dir .expo-web-check` berhasil setelah Fase 9; bundle web utama sekitar 1.58 MB dan CSS global sekitar 104 kB.
- `npx expo export --clear --platform android --output-dir .expo-android-check` berhasil setelah Fase 9; bundle Android utama sekitar 4.75 MB.
- Folder `.expo-web-check` dan `.expo-android-check` sudah dihapus setelah verifikasi Fase 9.
- `LoginScreen.js` memakai `Text`, `Button`, `Input`, `Card`, dan `CardContent` dari `src/components/ui`; wrapper `AppButton`/`AppInput` tidak lagi dipakai oleh screen.
- `DashboardScreen.js` memakai `Text` dari UI kit secara langsung bersama `Card`, `Badge`, dan `Separator`.
- `BuildUpChecklistScreen.js` memakai `Button`, `Card`, `CardContent`, `Input`, dan `Text` dari UI kit secara langsung.
- `rg "AppButton|AppInput|InfoCard" mobile-app\\src mobile-app\\app` hanya menemukan file wrapper compatibility, tidak ada import screen aktif.
- `rg "fetch\\(" mobile-app\\src\\screens mobile-app\\src\\components` kosong, sehingga screen dan komponen tetap tidak melakukan API call langsung.
- `rg "ScreenLayout" mobile-app\\src\\screens` mengonfirmasi semua screen existing tetap memakai `ScreenLayout`.
- `npm run doctor` berhasil setelah Fase 10, 17/17 checks passed.
- `npx expo export --clear --platform web --output-dir .expo-web-check` berhasil setelah Fase 10 pada retry dengan output stderr eksplisit; bundle web utama sekitar 1.58 MB.
- `npx expo export --clear --platform android --output-dir .expo-android-check` berhasil setelah Fase 10; bundle Android utama sekitar 4.75 MB.
- Folder `.expo-web-check` dan `.expo-android-check` sudah dihapus setelah verifikasi Fase 10.
- Expo web dev server berjalan di `http://localhost:8082`; smoke test `Invoke-WebRequest` ke URL tersebut mengembalikan HTTP 200.
- `src/components/ui/drawer.js` dibuat sebagai komponen menengah berbasis `Modal`, `SafeAreaView`, dan NativeWind; tidak ada dependency tambahan seperti `@gorhom/bottom-sheet` atau `react-native-gesture-handler`.
- `DashboardScreen.js` tidak lagi memiliki `SideMenu` lokal. Menu Dashboard sekarang memakai `Drawer`, `DrawerPanel`, `DrawerHeader`, `DrawerContent`, dan `DrawerFooter` dari UI kit.
- Backdrop drawer dibuat gelap eksplisit dengan panel `bg-card`, sehingga teks menu tetap terbaca dan backdrop tidak transparan saat menu dibuka.
- Search field Dashboard dibuat full width (`w-full`) dan dipakai ulang pada konten utama serta drawer.
- Padding horizontal `ScreenLayout` dinaikkan dari 20 ke 24 dan vertical content spacing dari `py-6` ke `py-7` agar konten tidak terlalu rapat ke tepi screen.
- `npm run doctor` berhasil setelah Fase 11, 17/17 checks passed.
- `npx expo export --clear --platform web --output-dir .expo-web-check` berhasil setelah Fase 11; bundle web utama sekitar 1.58 MB.
- `npx expo export --clear --platform android --output-dir .expo-android-check` berhasil setelah Fase 11; bundle Android utama sekitar 4.75 MB.
- Folder `.expo-web-check` dan `.expo-android-check` sudah dihapus setelah verifikasi Fase 11.
- Dependency `expo-camera` dipasang dengan `npx expo install expo-camera`.
- `app.json` menambahkan `scheme: "mauapp"` untuk menghilangkan warning Linking production dan plugin `expo-camera` dengan pesan permission kamera.
- `src/components/ui/qr-scanner.js` dibuat sebagai modal scanner QR berbasis `CameraView`, `useCameraPermissions`, dan `barcodeScannerSettings` dengan `barcodeTypes: ['qr']`; komponen ini dipertahankan untuk kebutuhan QR berikutnya.
- `src/components/ui/barcode-scanner.js` dibuat sebagai modal scanner barcode berbasis `CameraView`, `useCameraPermissions`, dan `barcodeScannerSettings` dengan tipe `code128`, `code39`, `ean13`, `ean8`, `upc_a`, dan `upc_e`.
- `BuildUpChecklistScreen.js` menambahkan tombol `Scan` pada field `MAWB Number` dan `ULD Number`; hasil barcode scan mengisi field aktif secara lokal.
- `README.md` diperbarui dengan dokumentasi permission camera, batas Expo Go, dan aturan bahwa hasil scan tetap harus divalidasi/disimpan melalui backend.
- `npm run doctor` berhasil setelah Fase 12, 17/17 checks passed.
- `npx expo export --clear --platform web --output-dir .expo-web-check` berhasil setelah Fase 12; bundle web utama sekitar 1.61 MB.
- `npx expo export --clear --platform android --output-dir .expo-android-check` berhasil setelah Fase 12; bundle Android utama sekitar 4.78 MB.
- Folder `.expo-web-check` dan `.expo-android-check` sudah dihapus setelah verifikasi Fase 12.
- Expo dev server di `http://localhost:8082` sudah direstart setelah pemasangan `expo-camera`; smoke test HTTP kembali mengembalikan 200.
- `src/components/AppButton.js`, `src/components/AppInput.js`, dan `src/components/InfoCard.js` dihapus karena tidak ada import aktif setelah screen memakai `src/components/ui` langsung.
- `src/navigation/AppNavigator.js` dan folder `src/navigation/` dihapus karena runtime aktif sudah memakai Expo Router.
- `App.js` dipangkas menjadi compatibility redirect ke route root Expo Router.
- Fallback prop React Navigation di `DashboardScreen.js` dan `BuildUpChecklistScreen.js` dihapus.
- Dependency langsung `@react-navigation/native` dan `@react-navigation/native-stack` dihapus dari `package.json`.
- `rg "@react-navigation|NavigationContainer|createNativeStackNavigator|AppNavigator|AppButton|AppInput|InfoCard|navigation\\?\\.|legacyRoute" mobile-app\\src mobile-app\\app mobile-app\\App.js mobile-app\\package.json` kosong.
- `rg "StyleSheet\\.create" mobile-app\\src` kosong.
- `rg "fetch\\(" mobile-app\\src\\screens mobile-app\\src\\components` kosong.
- `mobile_agent.md` diperbarui agar UI primitive baru wajib berada di `src/components/ui`, diekspor dari `src/components/ui/index.js`, memakai NativeWind, dan tetap JavaScript-only.
- `README.md` menambahkan panduan membuat UI component baru, contoh penggunaan `Button`, `Text`, `Card`, `CardContent`, dan `Input`, serta daftar dependency UI yang masih ditunda.
- Dokumentasi menegaskan template sumber TypeScript tidak boleh dicopy mentah ke project ini.
- `npm run doctor` berhasil setelah Fase 14, 17/17 checks passed.
- `npx expo export --clear --platform web --output-dir .expo-web-check` berhasil setelah Fase 14.
- `npx expo export --clear --platform android --output-dir .expo-android-check` berhasil setelah Fase 14.
- Folder `.expo-web-check` dan `.expo-android-check` sudah dihapus setelah verifikasi Fase 14.
- Dev server `http://localhost:8082` direstart setelah perbaikan scanner; Metro log terbaru bersih dari warning `CameraView` dan hanya menampilkan log standar web.
- HTTP smoke test route `/`, `/login`, dan `/build-up-checklist` mengembalikan 200.
- Headless Chromium acceptance: `LOGIN_VISIBLE=1`, `DASHBOARD_VISIBLE=1`, `SEARCH_WIDTH=345`, `BUILD_UP_VISIBLE=1`, `SCAN_BUTTONS=2`, `RELOAD_OK=1`, dan `CONSOLE_ISSUES=0`.
- Overlay scanner di `BarcodeScanner` dan `QrScanner` dipindahkan keluar dari children `CameraView` ke absolute overlay untuk mengikuti warning Expo Camera.
- Konfigurasi duplicate `scannerTitle` pada field ULD dibersihkan.
- Daftar komponen evaluasi terpisah di checklist sudah diperbarui: Drawer diimplementasikan, komponen lain ditunda dengan alasan teknis.
- `npm run doctor` berhasil setelah bugfix hamburger menu, 17/17 checks passed.
- `rg "StyleSheet\\.create|fetch\\(" src\\screens src\\components` kosong setelah bugfix, sehingga screen/components tetap NativeWind-first dan tidak ada API call langsung.
- `npx expo export --clear --platform web --output-dir .expo-web-check` berhasil setelah bugfix hamburger menu.
- `npx expo export --clear --platform android --output-dir .expo-android-check` berhasil setelah bugfix hamburger menu.
- Folder `.expo-web-check` dan `.expo-android-check` sudah dihapus setelah verifikasi bugfix hamburger menu.
- `DrawerPanel` memakai `useWindowDimensions()` untuk lebar panel responsif sampai 90% layar dengan batas maksimal 380px.
- Text menu Dashboard memakai `numberOfLines={1}` agar label seperti `Onboarding` dan `Permissions` tidak pecah di panel drawer.
- `npm run doctor` berhasil setelah perbaikan komponen drawer lanjutan, 17/17 checks passed.
- `npx expo export --clear --platform android --output-dir .expo-android-check` berhasil setelah perbaikan komponen drawer lanjutan.
- `npx expo export --platform web --output-dir .expo-web-check` berhasil setelah perbaikan komponen drawer lanjutan. Percobaan web export dengan `--clear` sempat gagal karena Metro cache sedang dikunci proses dev server/export lain, bukan karena error bundling kode.
- Folder `.expo-web-check` dan `.expo-android-check` sudah dihapus setelah verifikasi lanjutan drawer.

## Gap / Risiko

- Data dashboard masih placeholder UI dan belum terhubung ke endpoint backend.
- Menu dashboard selain Build Up masih belum memiliki route aktif.
- Preview visual manual di device fisik tetap perlu dilakukan karena perbedaan ukuran layar dan status bar Android/iOS.
- NativeWind versi saat ini tidak kompatibel dengan `nativewind/babel` atau `jsxImportSource: 'nativewind'`; gunakan cache clear saat konfigurasi styling berubah.
- Migrasi Expo Router perlu dilakukan bertahap karena project masih memiliki `AppNavigator` dari React Navigation.
- Worktree masih dirty dari perubahan sebelum audit Fase 0. Sebelum masuk fase dependency atau routing, sebaiknya review diff dan commit/simpan checkpoint agar perubahan UI, konfigurasi NativeWind, dan migrasi berikutnya tidak tercampur.
- Branch audit lokal belum punya upstream. Ini aman untuk menghindari push ke `origin/master`, tetapi perlu remote/branch tujuan yang disepakati sebelum push.
- Template sumber memakai NativeWind `^4.1.23`, Tailwind CSS `^3.4.17`, dan TypeScript. Project MAU APP Mobile saat ini memakai NativeWind `^5.0.0-preview.3`, Tailwind CSS `^4.3.0`, dan JavaScript-only, sehingga port komponen harus manual, kecil, dan diverifikasi per fase.
- `separator` tidak tersedia sebagai file standalone di template sumber; jika dibutuhkan, implementasi MAU APP harus berupa primitive `View` sederhana.
- `expo-router` sudah tersedia tetapi belum menjadi entry runtime. Fase 2A harus membuat route JavaScript terlebih dahulu sebelum mengubah `main` ke `expo-router/entry`.
- `app.json` kini memuat plugin `expo-router`; export tetap berhasil dengan entry lama, tetapi perubahan routing berikutnya perlu diverifikasi ulang di web dan Android.
- Setelah Fase 2A, bundle utama naik karena Expo Router ikut masuk ke graph: web dari sekitar 1.32 MB ke 1.54 MB dan Android dari sekitar 4.17 MB ke 4.68 MB. Ini masih diterima untuk fase migrasi routing, tetapi perlu dipantau pada fase cleanup/WPO.
- `src/navigation/AppNavigator.js` masih dipertahankan sebagai compatibility layer. Cleanup file ini dan import terkait sebaiknya dilakukan setelah manual QA memastikan login, dashboard, dan Build Up stabil di web serta device fisik.
- Bundle Fase 3 naik tipis karena dependency `clsx` dan `tailwind-merge` kini masuk graph melalui `AppButton`. Kenaikan ini perlu dipantau saat komponen UI bertambah, tetapi masih sesuai tujuan utility reusable.
- `Text` baru memakai `class-variance-authority`; bundle web naik tipis ke sekitar 1.58 MB. Masih wajar untuk fase UI foundation, tetapi tetap perlu dipantau.
- Komponen lama `AppButton`, `AppInput`, dan `InfoCard` masih ada sebagai wrapper compatibility. Ini sengaja untuk menekan risiko refactor besar; cleanup atau penggantian import langsung bisa dilakukan pada Fase 10/13.
- `AppNavigator` masih ada sebagai compatibility layer React Navigation. Karena `package.json` sudah memakai `expo-router/entry`, file ini tidak menjadi route utama, tetapi cleanup tetap perlu setelah manual QA.
- Token theme Fase 9 sudah dipakai di primitive UI, tetapi beberapa screen masih memiliki class warna utilitas langsung untuk elemen spesifik seperti ikon container dan gradient. Ini aman karena tokenisasi penuh visual detail bisa dilanjutkan bertahap tanpa mengubah flow.
- Drawer saat ini memakai React Native `Modal` bawaan, bukan bottom sheet/gesture drawer. Ini dipilih untuk menjaga Expo Go tetap sederhana; jika nanti butuh gesture drawer penuh, perlu evaluasi dependency native terpisah.
- Camera scanner perlu manual QA di HP fisik Android karena kamera/permission tidak bisa dibuktikan penuh dari export bundling saja.
- React Navigation masih mungkin muncul sebagai dependency transitif di `package-lock.json` lewat Expo Router internals, tetapi bukan dependency langsung project dan tidak ada import source aktif.
- Dokumentasi contoh UI bersifat contoh pola; komponen real tetap harus mengikuti kebutuhan screen dan tidak boleh membawa API/business logic.
- Acceptance web memakai session acceptance lokal di headless browser untuk melewati auth gate dan menguji UI route; ini tidak menggantikan QA login backend manual di browser user.
- Acceptance HP fisik Expo Go belum bisa diselesaikan oleh Codex dari terminal. Item Login, Dashboard, Build Up, orientasi portrait, keyboard, dan kamera barcode tetap membutuhkan konfirmasi user di device fisik.
- Kontras drawer sudah diperbaiki dengan warna eksplisit, tetapi review visual final di HP fisik tetap disarankan karena ukuran/status bar perangkat bisa berbeda.

## Blocker

- Tidak ada blocker teknis pada bundling Expo saat verifikasi.
- Tidak ada blocker Fase 0; catatan utama hanya disiplin branch dan pemisahan perubahan existing.
- Tidak ada blocker Fase 1; belum ada dependency baru yang dipasang.
- Tidak ada blocker Fase 2 setelah range React Navigation disesuaikan dengan rekomendasi Expo SDK.
- Tidak ada blocker Fase 2A pada doctor/export. Verifikasi visual manual masih perlu dilakukan untuk memastikan tidak ada blank screen saat runtime interaktif.
- Tidak ada blocker Fase 3.
- Tidak ada blocker Fase 4.
- Tidak ada blocker Fase 5-8.
- Tidak ada blocker Fase 9.
- Tidak ada blocker Fase 10. Catatan kecil: percobaan pertama export web setelah refactor screen keluar exit code 1 tanpa output, lalu retry dengan stderr eksplisit berhasil penuh.
- Tidak ada blocker Fase 11.
- Tidak ada blocker Fase 12 pada doctor/export. Manual QA scan barcode di device fisik masih perlu dilakukan.
- Tidak ada blocker Fase 13.
- Tidak ada blocker Fase 14.
- Tidak ada blocker untuk acceptance web Fase 15.
- Tidak ada blocker bundling setelah bugfix hamburger menu.
- Blocker tersisa Fase 15: acceptance di HP fisik Expo Go dan kamera barcode real membutuhkan pengujian user.

## Next Step

- Jalankan `npm run web` untuk review cepat di browser.
- Jalankan `npx expo start --clear --host lan` untuk cek langsung di HP fisik via Expo Go.
- Sambungkan data dashboard dan checklist ke service API setelah endpoint backend final.
- Jalankan fase migrasi Expo Router sesuai `mobile-app/adops_template.md` sebelum refactor UI kit besar-besaran.
- Lanjut manual QA di HP fisik Expo Go untuk login, dashboard, Build Up, orientation portrait, keyboard behavior, dan scanner barcode kamera.
