# Progress 2026-05-21

## Ringkasan Perubahan
- Membersihkan sisa-sisa file, class, method, endpoint, dan import dari modul legacy `Warehouse > Buildup`.
- Mempertahankan seluruh file model DB2 (`BaseDB2`) dan schema DB2 (`eks_buildup*`) agar tetap sesuai dengan database DB2 sebagai Source of Truth.
- Mempertahankan modul `Build Up Check` mobile (DB1) beserta seluruh dependensinya.

## File yang Diubah
- `astro/src/lib/api/edi.js` (Menghapus client method dan endpoint constant yang obsolete)
- `materialize-fastapi/app/api/edi.py` (Menghapus endpoint `/export-buildup` dan `/export-buildup-mawb/{buildup_number}`)
- `materialize-fastapi/app/dependencies/edi_deps.py` (Menghapus DI providers buildup)
- `materialize-fastapi/app/services/edi_service.py` (Menghapus buildup service methods)
- `materialize-fastapi/app/repositories/edi_repository.py` (Menghapus buildup repository methods dan datatable service initializations)

## Hasil Verifikasi
- Backend:
  - Linting (`poetry run ruff check`): Sukses (tidak ada error linter tersisa, import otomatis dirapikan).
  - Kompilasi (`python -m py_compile`): Sukses tanpa error.
- Frontend:
  - Build (`npm run build`): Sukses (bundling and minification berjalan lancar).

## Gap / Risiko
- Tidak ada. Seluruh fungsionalitas preview FFM yang berbasis tabel `BuildUpCheck` (DB1) tetap berjalan normal.

## Blocker
- Tidak ada.

## Next Step
- Menunggu arahan modul berikutnya dari User.
