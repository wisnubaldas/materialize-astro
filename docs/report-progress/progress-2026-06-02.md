# Progress 2026-06-02

## Ringkasan Perubahan

- Memperbaiki konfigurasi EAS mobile agar build preview dan production memakai `EXPO_PUBLIC_API_BASE_URL`, bukan `API_URL`.
- Menambahkan guard runtime agar build non-development tidak fallback ke `http://127.0.0.1:8000` saat API base URL belum dikonfigurasi.
- Memperbarui dokumentasi mobile terkait environment EAS Cloud build dan troubleshooting APK/AAB yang mengarah ke localhost.
- Menerjemahkan dan menyusun ulang `mobile-app/README.md` ke Bahasa Indonesia agar sesuai workflow project: local development, Expo Go, backend LAN, EAS build preview, production AAB, dan troubleshooting.
- Menyinkronkan version metadata Android agar EAS build berikutnya memakai `versionName 1.0.1` dan `versionCode 2`.
- Memperbaiki kegagalan `expo doctor` pada EAS build dengan menghapus field `usesCleartextTraffic` dari `app.json`, mempertahankan cleartext config di native AndroidManifest, menonaktifkan warning app config non-CNG yang memang disengaja, dan menaikkan patch dependency Expo SDK 54.

## File yang Diubah

- `mobile-app/eas.json`
- `mobile-app/app.json`
- `mobile-app/android/app/build.gradle`
- `mobile-app/package.json`
- `mobile-app/package-lock.json`
- `mobile-app/src/config/env.js`
- `mobile-app/src/services/apiService.js`
- `mobile-app/README.md`
- `docs/report-progress/progress-2026-06-02.md`

## Hasil Verifikasi

- `node -e "JSON.parse(...eas.json...)"` berhasil; `eas.json` valid JSON.
- `npx expo config --type public` berhasil membaca konfigurasi Expo.
- `npm run doctor` berhasil dengan hasil `17/17 checks passed`.

## Gap / Risiko

- URL `https://api.mitraadira.com` harus dipastikan mengarah ke backend FastAPI production yang dapat diakses dari jaringan HP dan memiliki HTTPS valid.
- APK/AAB yang sudah terlanjur dibuat sebelum perubahan ini tetap membawa konfigurasi lama dan harus dibuild ulang.
- `npm install` setelah `npx expo install` masih melaporkan 15 moderate vulnerability dari dependency tree. Belum dijalankan `npm audit fix --force` karena berisiko membawa breaking changes.

## Blocker

- Tidak ada blocker saat perubahan konfigurasi.

## Next Step

- Jalankan ulang `npm run build:android:preview` untuk menghasilkan APK baru.
- Jika memakai EAS Environment dashboard, pastikan environment `preview` dan `production` juga berisi `EXPO_PUBLIC_API_BASE_URL`.
