# Progress 2026-05-02

## Ringkasan Perubahan

- Rename folder backend dari `materialize-fastapi/app/repository/` menjadi `materialize-fastapi/app/repositories/`.
- Update seluruh import Python dari `app.repository...` menjadi `app.repositories...`.
- Update path SQL query dari `app/repository/...` menjadi `app/repositories/...`.
- Update dokumentasi struktur backend di `AGENTS.md` dan `materialize-fastapi/backend_agent.md`.

## File yang Diubah

- `AGENTS.md`
- `materialize-fastapi/backend_agent.md`
- `materialize-fastapi/app/dependencies/*.py` (file dependency yang mengimpor repository)
- `materialize-fastapi/app/services/*.py` (file service yang mengimpor repository/path SQL)
- `materialize-fastapi/app/job/*.py` (path SQL/query dan import repository)
- `materialize-fastapi/app/integrations/ceisa/*.py` (import repository terkait CEISA)
- `materialize-fastapi/app/repositories/` (hasil rename dari `app/repository/`)

## Hasil Verifikasi/Test

- Verifikasi referensi teks: tidak ada lagi `app.repository` dan `app/repository` di source backend.
- Verifikasi struktur folder: `materialize-fastapi/app/repositories/` sudah tersedia dan berisi file repository sebelumnya.
- Belum menjalankan test suite/lint penuh pada task ini.

## Gap / Risiko

- Perubahan import lintas banyak file berisiko memunculkan error runtime jika ada referensi dinamis yang tidak terdeteksi pattern pencarian.
- Dokumentasi lain di luar scope yang menyebut `repository` mungkin masih ada dan perlu audit lanjutan.

## Blocker

- Tidak ada blocker teknis saat eksekusi rename dan update referensi.

## Next Step

- Jalankan test backend dan smoke run FastAPI (`poetry run ...`) untuk memastikan seluruh import dan path query berjalan normal.

---

## Update Desktop Scaffold (2026-05-02)

### Ringkasan Perubahan

- Menambahkan scaffold awal aplikasi desktop C# .NET MAUI di `desktop-app/src/Mau.Desktop/`.
- Menyusun struktur MVVM + DI + Service Layer + API client sesuai `desktop-app/desktop_agent.md`.
- Menambahkan solusi `desktop-app/Mau.Desktop.sln`, project test dasar `desktop-app/tests/Mau.Desktop.Tests/`, konfigurasi contoh `desktop-app/appsettings.example.json`, dan dokumentasi desktop.
- Menambahkan `desktop-app/.gitignore` untuk mengabaikan artifact build/IDE/secrets lokal.

### File yang Diubah

- `desktop-app/.gitignore`
- `desktop-app/Mau.Desktop.sln`
- `desktop-app/README.md`
- `desktop-app/appsettings.example.json`
- `desktop-app/docs/README.md`
- `desktop-app/src/Mau.Desktop/` (App, Shell, MauiProgram, Core, Api, Services, Devices, ViewModels, Views, Models, Resources, Platforms)
- `desktop-app/tests/Mau.Desktop.Tests/Mau.Desktop.Tests.csproj`
- `desktop-app/tests/Mau.Desktop.Tests/ViewModels/ResultTests.cs`

### Hasil Verifikasi/Test

- Scaffold MAUI berhasil dibuat dengan `dotnet new maui`.
- Build project MAUI belum berhasil di environment ini karena restore package NuGet tidak tersedia (`NU1101`, source hanya offline packages).
- Tidak ada koneksi database langsung dari desktop; alur akses data diarahkan lewat layer API client ke backend.

### Gap / Risiko

- API endpoint yang dipanggil desktop (`/auth/login`, `/auth/me`, `/warehouse/weighing`) adalah kontrak awal dan perlu diselaraskan dengan endpoint backend aktual.
- Integrasi device (scanner/timbangan/printer) masih berupa stub dan perlu adapter vendor/driver pada iterasi berikutnya.

### Blocker

- Source NuGet online belum tersedia pada environment ini, sehingga restore/build penuh belum dapat divalidasi.

### Next Step

- Sinkronkan DTO + endpoint dengan backend FastAPI.
- Implementasi use case operasional awal (login, weighing submit, basic navigation guard) beserta unit test ViewModel/Service lebih lengkap.
- Jalankan build/test ulang setelah source NuGet dapat diakses.

---

## Update Desktop Test Compatibility (2026-05-02)

### Ringkasan Perubahan

- Perbaikan kompatibilitas test project untuk mengatasi `NU1201` saat referensi ke project MAUI.
- Memisahkan `Result<T>` ke class library baru `Mau.Desktop.Core` (target `net8.0`) agar bisa diuji tanpa mereferensikan project UI MAUI.
- Mengubah `Mau.Desktop.Tests` agar mereferensikan `Mau.Desktop.Core` alih-alih `Mau.Desktop`.
- Menambahkan `ProjectReference` dari `Mau.Desktop` ke `Mau.Desktop.Core`.

### File yang Diubah

- `desktop-app/src/Mau.Desktop/Mau.Desktop.csproj`
- `desktop-app/tests/Mau.Desktop.Tests/Mau.Desktop.Tests.csproj`
- `desktop-app/src/Mau.Desktop.Core/Mau.Desktop.Core.csproj`
- `desktop-app/src/Mau.Desktop.Core/Result.cs`
- `desktop-app/src/Mau.Desktop/Core/Result.cs` (dipindahkan)
- `desktop-app/Mau.Desktop.sln`

### Hasil Verifikasi/Test

- `dotnet build desktop-app/src/Mau.Desktop/Mau.Desktop.csproj -f net8.0-windows10.0.19041.0` sukses.
- `dotnet test desktop-app/tests/Mau.Desktop.Tests/Mau.Desktop.Tests.csproj` sukses (1 passed).
- Error `NU1201` pada test project tidak muncul lagi.

### Gap / Risiko

- Belum ada pemisahan lebih lanjut domain/service ke class library terpisah; saat ini baru `Result<T>` yang diekstrak untuk kebutuhan test awal.

### Next Step

- Pindahkan logic yang testable (DTO mapper, validator UX, service helper) bertahap ke `Mau.Desktop.Core` agar cakupan unit test meningkat tanpa coupling ke UI MAUI.

---

## Update Visual Studio Deploy Mapping (2026-05-02)

### Ringkasan Perubahan

- Menambahkan mapping `Deploy.0` untuk project `Mau.Desktop` di solution agar Visual Studio dapat deploy sebelum debug.

### File yang Diubah

- `desktop-app/Mau.Desktop.sln`

### Hasil Verifikasi/Test

- Verifikasi konten solution: konfigurasi `Debug|Any CPU` dan `Release|Any CPU` untuk `Mau.Desktop` kini memiliki entri `Deploy.0`.

### Gap / Risiko

- Jika startup project di Visual Studio bukan `Mau.Desktop`, error deploy masih bisa muncul.

### Next Step

- Pastikan `Mau.Desktop` dijadikan Startup Project dan target debug Windows aktif.

---

## Update Startup Exception Fix (2026-05-02)

### Ringkasan Perubahan

- Perbaikan startup crash yang memicu breakpoint `Debugger.Break()` pada debug Visual Studio.
- Menambahkan constructor parameterless pada page yang dipakai `ShellContent` agar kompatibel dengan instansiasi XAML DataTemplate.
- Constructor parameterless tetap resolve ViewModel dari DI melalui helper `ServiceResolver`.

### File yang Diubah

- `desktop-app/src/Mau.Desktop/Core/ServiceResolver.cs`
- `desktop-app/src/Mau.Desktop/Views/LoginPage.xaml.cs`
- `desktop-app/src/Mau.Desktop/Views/MainPage.xaml.cs`
- `desktop-app/src/Mau.Desktop/Views/Warehouse/WeighingPage.xaml.cs`
- `desktop-app/src/Mau.Desktop/Views/Warehouse/BuildupPage.xaml.cs`

### Hasil Verifikasi/Test

- `dotnet build desktop-app/src/Mau.Desktop/Mau.Desktop.csproj -f net8.0-windows10.0.19041.0` sukses.
- Pola constructor page kini kompatibel dengan Shell XAML dan tetap menjaga DI ViewModel.

### Gap / Risiko

- Jika masih break di startup, perlu lihat exception detail pertama di `Output > Debug` karena itu menandakan error runtime lain (mis. endpoint config/service runtime).

### Next Step

- Jalankan kembali via Visual Studio (Startup Project `Mau.Desktop`) dan verifikasi halaman login tampil normal.

---

## Update Native Debug Profile (2026-05-02)

### Ringkasan Perubahan

- Mengaktifkan native debugging untuk profile `Windows Machine` agar exception Win32 dapat ditangkap langsung di Visual Studio, bukan muncul sebagai JIT popup tanpa stack detail.

### File yang Diubah

- `desktop-app/src/Mau.Desktop/Properties/launchSettings.json`

### Hasil Verifikasi/Test

- Build target Windows tetap sukses setelah update launch profile.

### Next Step

- Jalankan debug ulang dari Visual Studio untuk mendapatkan call stack native/managed yang lengkap jika crash masih terjadi.

---

## Update XAML Startup Stability (2026-05-02)

### Ringkasan Perubahan

- Mengubah pola constructor page agar aman terhadap inisialisasi XAML/Shell di Windows.
- Constructor parameterless kini hanya menjalankan `InitializeComponent()`.
- `BindingContext` dari DI di-resolve pada `OnHandlerChanged()` saat `MauiContext` sudah siap.
- Menghapus `ServiceResolver` karena tidak lagi diperlukan.

### File yang Diubah

- `desktop-app/src/Mau.Desktop/Views/LoginPage.xaml.cs`
- `desktop-app/src/Mau.Desktop/Views/MainPage.xaml.cs`
- `desktop-app/src/Mau.Desktop/Views/Warehouse/WeighingPage.xaml.cs`
- `desktop-app/src/Mau.Desktop/Views/Warehouse/BuildupPage.xaml.cs`
- `desktop-app/src/Mau.Desktop/Core/ServiceResolver.cs` (dihapus)

### Hasil Verifikasi/Test

- `dotnet build desktop-app/src/Mau.Desktop/Mau.Desktop.csproj -f net8.0-windows10.0.19041.0` sukses.

### Next Step

- Jalankan ulang debug di Visual Studio untuk memastikan exception `Microsoft.UI.Xaml` tidak muncul lagi saat startup.

---

## Update Android Icon Build Fix (2026-05-02)

### Ringkasan Perubahan

- Memperbaiki error Android `APT2260` (`mipmap/appicon` dan `appicon_round` not found).
- Menyederhanakan konfigurasi ikon MAUI Android agar tidak bergantung pada adaptive icon foreground file yang sempat menimbulkan lock (`MAUI0000`).

### File yang Diubah

- `desktop-app/src/Mau.Desktop/Mau.Desktop.csproj`
  - `MauiIcon` diubah menjadi single source: `Resources/AppIcon/appicon.svg` tanpa `ForegroundFile`.
- `desktop-app/src/Mau.Desktop/Platforms/Android/AndroidManifest.xml`
  - Menghapus atribut `android:roundIcon`.

### Hasil Verifikasi/Test

- `dotnet build desktop-app/src/Mau.Desktop/Mau.Desktop.csproj -f net8.0-android` sukses.
- Error `APT2260` tidak muncul lagi.

### Gap / Risiko

- Android adaptive round icon sementara dinonaktifkan agar build stabil.
- Jika diperlukan branding adaptive icon penuh, perlu ditambahkan ulang setelah isu lock Resizetizer dipastikan tidak terjadi di environment dev.

### Next Step

- Jalankan ulang dari Visual Studio target Android.
- Jika build stabil, bisa lanjut set adaptive icon terkontrol pada iterasi berikutnya.

---

## Update Windows DEP0700 Fix (2026-05-02)

### Ringkasan Perubahan

- Menangani error deploy Windows `DEP0700` terkait splash screen yang tidak ditemukan saat debug.
- Mengatur mode debug Windows menjadi **unpackaged** agar F5 tidak bergantung pada instalasi MSIX.

### File yang Diubah

- `desktop-app/src/Mau.Desktop/Mau.Desktop.csproj`
  - Tambah property: `WindowsPackageType=None` untuk target Windows pada konfigurasi `Debug`.
- `desktop-app/src/Mau.Desktop/Properties/launchSettings.json`
  - Ubah `commandName` dari `MsixPackage` menjadi `Project` untuk profile `Windows Machine`.

### Hasil Verifikasi/Test

- Build debug Windows sukses setelah perubahan.
- Publish MSIX release sebelumnya juga tervalidasi berisi `splashSplashScreen.scale-*.png`, jadi error DEP0700 berasal dari alur deploy debug/package lama.

### Next Step

- Jalankan ulang dari Visual Studio dengan profile `Windows Machine` (unpackaged debug).
- Jika perlu packaging installer, gunakan publish Release (MSIX) secara terpisah.

---

## Update Desktop Migration to WPF (2026-05-02)

### Ringkasan Perubahan

- Menghapus total scaffold desktop berbasis MAUI.
- Membangun ulang desktop app menggunakan **WPF (.NET 8)**.
- Menyusun struktur MVVM + DI + service layer + API client untuk baseline desktop baru.
- Mengganti panduan desktop di root `AGENTS.md` dari MAUI menjadi WPF.
- Memperbarui `desktop-app/desktop_agent.md` agar selaras dengan stack WPF.

### File yang Diubah

- `AGENTS.md`
- `desktop-app/desktop_agent.md`
- `desktop-app/.gitignore`
- `desktop-app/Mau.Desktop.sln`
- `desktop-app/README.md`
- `desktop-app/appsettings.example.json`
- `desktop-app/docs/README.md`
- `desktop-app/src/Mau.Desktop/*` (project WPF baru: App, MainWindow, Api, Services, Core, ViewModels, Views)
- `desktop-app/src/Mau.Desktop.Core/*`
- `desktop-app/tests/Mau.Desktop.Tests/*`

### Hasil Verifikasi/Test

- `dotnet build desktop-app/Mau.Desktop.sln` sukses.
- `dotnet test desktop-app/tests/Mau.Desktop.Tests/Mau.Desktop.Tests.csproj --no-build` sukses (1 passed).

### Gap / Risiko

- Integrasi device lokal (scanner/timbangan/printer) masih placeholder dan belum diimplementasi.
- Endpoint API yang dipakai (`/auth/login`, `/warehouse/weighing`) masih baseline dan perlu sinkronisasi detail dengan backend aktual.

### Next Step

- Tambahkan modul operasional prioritas (weighing/buildup) pada WPF ViewModel + Service.
- Tambahkan abstraction device layer saat kebutuhan hardware sudah final.
