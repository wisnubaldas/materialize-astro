---
title: 'Diagram'
description: 'Lorem ipsum dolor sit amet - 2'
---

import MermaidClient from '../../../components/MermaidClient.astro'
import MermaidZoomSupport from '../../../components/MermaidZoomSupport.astro'

<MermaidClient />
<MermaidZoomSupport />

## <iconify-icon icon="line-md:briefcase-check-filled" width="24" height="24"></iconify-icon> Ringkasan Teknis
APP MAU adalah sistem manajemen gudang lini 1 Bandara Soekarno-Hatta, mencakup pelaporan ke AP2, HUBNET, Bea Cukai, dan operasional CTOS. Sistem bekerja 24/7 menggunakan FastAPI, Astro, Redis dan APScheduler, dengan keamanan berbasis SSL, JWT, firewall, dan role-based access.

---

## <iconify-icon icon="line-md:cloud-alt-tags-twotone" width="24" height="24"></iconify-icon> Diagram Arsitektur Jaringan
<div class="mermaid zoomable">
{`graph TD
    subgraph Client
        A[User Browser]
    end

    subgraph Frontend
        B[Astro Frontend - Node.js]
    end

    subgraph Backend
        C[FastAPI Backend]
        D[Redis Cache / Queue]
        E[APScheduler Jobs]
    end

    subgraph External Systems
        F[Angkasa Pura II API]
        G[Kemenhub HUBNET API]
        H[Bea Cukai TPS Online API]
        I[Airlines Manifest API]
    end

    A --> B --> C
    C --> D
    C --> E
    C --> F
    C --> G
    C --> H
    C --> I`}
</div>

---

## <iconify-icon icon="line-md:calendar" width="24" height="24"></iconify-icon> ERD Database
<div class="mermaid zoomable">
{`erDiagram
    USERS ||--o{ ROLES : has
    USERS {
        int id PK
        string nama
        string email
        string password_hash
        int role_id FK
    }
    ROLES {
        int id PK
        string nama_role
    }

    MANIFEST ||--o{ INVOICE : generates
    MANIFEST {
        int id PK
        string awb_no
        string flight_no
        datetime flight_date
        string shipper
        string consignee
    }

    INVOICE {
        int id PK
        int manifest_id FK
        decimal total_billing
        datetime created_at
    }

    MOVEMENT ||--o{ MANIFEST : tracks
    MOVEMENT {
        int id PK
        int manifest_id FK
        string status
        datetime timestamp
    }`}
</div>

---

## <iconify-icon icon="line-md:briefcase-plus-twotone" width="24" height="24"></iconify-icon> BPMN - Workflow Bisnis
<div class="mermaid zoomable">
{`flowchart LR
    A[Cargo Masuk Gudang] --> B[Input Data Manifest]
    B --> C[Penimbangan Barang]
    C --> D{Data Valid?}
    D -- Ya --> E[Generate Invoice]
    E --> F[Kirim Status ke Airlines / AP2 / HUBNET]
    D -- Tidak --> G[Perbaikan Data]
    F --> H[Monitoring Barang Keluar]`}
</div>

---

## <iconify-icon icon="line-md:turn-sharp-left" width="24" height="24"></iconify-icon> Sequence Diagram (Proses Pembuatan Invoice)
<div class="mermaid zoomable">
{`sequenceDiagram
    participant User
    participant Frontend
    participant Backend
    participant Database
    participant AP2

    User ->> Frontend: Input data billing
    Frontend ->> Backend: Kirim data invoice
    Backend ->> Database: Simpan invoice
    Database -->> Backend: OK
    Backend ->> AP2: Kirim laporan pendapatan
    AP2 -->> Backend: Response Success
    Backend -->> Frontend: Tampilkan hasil`}
</div>

---

## <iconify-icon icon="line-md:speed-twotone-loop" width="24" height="24"></iconify-icon> State Machine Diagram (Status Barang)
<div class="mermaid zoomable">
{`stateDiagram-v2
    [*] --> Received
    Received --> Stored
    Stored --> Weighed
    Weighed --> Invoiced
    Invoiced --> Released
    Released --> [*]`}
</div>

---

## <iconify-icon icon="line-md:person-search-twotone" width="24" height="24"></iconify-icon> SOP Downtime / Disaster Recovery
1. Sistem monitoring mendeteksi service mati �+' Kirim notifikasi
2. Supervisor auto-restart semua service
3. Jika gagal:
   ```bash
   sudo supervisorctl restart all
   sudo systemctl status redis
   sudo systemctl status nginx
   ```
4. Jika server rusak total:
   - Restore snapshot VM
   - Restore database dari backup harian

RTO: 5�?"10 menit  
RPO: backup harian

---

## <iconify-icon icon="line-md:security-twotone" width="24" height="24"></iconify-icon> Security Policy
- SSL aktif, HTTPS only
- Firewall hanya membuka 80/443/22
- Login server via SSH Key
- API pakai JWT
- Hak akses berdasarkan role
- Semua aktivitas dicatat di log
