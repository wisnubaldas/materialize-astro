---
title: 'Page 2'
description: 'Lorem ipsum dolor sit amet - 2'
---

import MermaidClient from '../../../components/MermaidClient.astro';

<MermaidClient />



# APP MAU - Technical Documentation (ISO Standard)

## ✅ Technical Summary
APP MAU is a warehouse management system for Soekarno-Hatta Airport Line 1. It handles AP2 revenue reports, HUBNET tracking submissions, Customs monitoring, and CTOS warehouse operations. The system runs 24/7 using FastAPI, Astro, Redis, APScheduler, and secure authentication with SSL, JWT, firewall rules and role-based access.

---

## ✅ Network Architecture Diagram
```mermaid
graph TD
    Client[User Browser] --> Frontend[Astro Frontend - Node.js]
    Frontend --> Backend[FastAPI Server]
    Backend --> Redis[Redis Cache / Queue]
    Backend --> APS[APScheduler Jobs]

    Backend --> AP2[Angkasa Pura II API]
    Backend --> HUBNET[Kemenhub API]
    Backend --> TPS[Bea Cukai TPS Online API]
    Backend --> Airlines[Airline Manifest API]
```

---

## ✅ ERD Database
```mermaid
erDiagram
    USERS ||--o{ ROLES : has
    USERS {
        int id PK
        string name
        string email
        string password_hash
        int role_id FK
    }
    ROLES {
        int id PK
        string role_name
    }

    MANIFEST ||--o{ INVOICE : generates
    MANIFEST {
        int id PK
        string awb_no
        string flight_no
        datetime flight_date
        string shipper
        string consignee
    }

    INVOICE {
        int id PK
        int manifest_id FK
        decimal total_billing
        datetime created_at
    }

    MOVEMENT ||--o{ MANIFEST : tracks
    MOVEMENT {
        int id PK
        int manifest_id FK
        string status
        datetime timestamp
    }
```

---

## ✅ BPMN Business Flow
```mermaid
flowchart LR
    A[Cargo Arrives] --> B[Input Manifest]
    B --> C[Weigh Cargo]
    C --> D{Data Valid?}
    D -- Yes --> E[Generate Invoice]
    E --> F[Send to Airline / AP2 / HUBNET]
    D -- No --> G[Correction Required]
    F --> H[Cargo Exit Monitoring]
```

---

## ✅ Sequence Diagram (Invoice Creation)
```mermaid
sequenceDiagram
    participant User
    participant Frontend
    participant Backend
    participant Database
    participant AP2

    User ->> Frontend: Enter invoice data
    Frontend ->> Backend: Submit invoice
    Backend ->> Database: Save record
    Database -->> Backend: OK
    Backend ->> AP2: Send daily revenue report
    AP2 -->> Backend: Success
    Backend -->> Frontend: Display confirmation
```

---

## ✅ State Machine (Cargo Process)
```mermaid
stateDiagram-v2
    [*] --> Received
    Received --> Stored
    Stored --> Weighed
    Weighed --> Invoiced
    Invoiced --> Released
    Released --> [*]
```

---

## ✅ Downtime / Disaster Recovery SOP
1. Monitoring detects service outage
2. Supervisor automatically restarts all services
3. If failed:
   ```bash
   sudo supervisorctl restart all
   sudo systemctl status redis
   sudo systemctl status nginx
   ```
4. If server fails completely:
   - Restore VM snapshot
   - Restore database backup

RTO: 5–10 minutes  
RPO: Daily backups

---

## ✅ Security Policy
- SSL, HTTPS enforced
- UFW firewall (80/443/22 only)
- SSH key authentication
- JWT authentication
- Role-based access
- Logging and auditing of all actions
