# Progress 2026-05-10

## Ringkasan Perubahan

- Memperbaiki konflik deklarasi `Buildup` pada halaman warehouse buildup Astro dengan mengganti alias import komponen React.
- Menyesuaikan konfigurasi `astro/jsconfig.json` untuk warning deprecasi TypeScript terkait `baseUrl` dengan menambahkan `ignoreDeprecations: "6.0"`.
- Menambahkan konfigurasi `moduleResolution: "bundler"` pada `jsconfig` agar `astro check` dapat resolve `astro/config` dengan benar.
- Memasang dependency verifikasi `@astrojs/check` dan `typescript` (dev dependency) agar pemeriksaan lint/type dapat dijalankan.
- Memperbaiki mismatch prop pada homepage dengan menghapus prop `name` yang tidak didukung oleh komponen `WelcomeBanner`.
- Refactor modul `Buildup` agar draft dapat diedit dari tab Draft menggunakan form yang sama dengan pembuatan draft.
- Menambahkan kemampuan tambah Master AWB saat proses edit draft melalui aksi append (`Tambah AWB`) tanpa menghapus data draft yang sudah ada.
- Menambahkan alur update draft di parent state agar perubahan draft tersimpan kembali ke local storage.
- Menetapkan konsistensi agent frontend:
  - form/detail table wajib Bootstrap modal bawaan template UI;
  - konfirmasi aksi boleh SweetAlert;
  - notifikasi wajib Toasts.
- Mengubah modal detail tabel Build Up dari SweetAlert menjadi Bootstrap modal bawaan template agar konsisten dan tidak tertutup sidebar.
- Menyusun gap analysis pemakaian SweetAlert di frontend untuk backlog migrasi modal form.
- Memperbaiki tampilan modal edit draft buildup agar elemen heading/description dari `BuildupForm` tidak dirender saat mode edit draft.
- Memperbaiki validasi generator FHL (`fhlGenerator.js`) dengan sanitasi karakter Cargo-IMP, normalisasi MAWB/HAWB, validasi kode IATA origin-destination, dan guard validasi pieces/weight per HAWB sebelum membentuk pesan.
- Memperbaiki ketidaksesuaian DataTables FFM (`ffmDatatables.jsx`) dengan menghapus kolom kontrol pertama, menyederhanakan kolom menjadi `BuildUp No`, `MAWB`, `airlines`, `flight date`, `ULD Type`, `ULD Number`, serta menambahkan kolom tombol `Detail` yang menampilkan Bootstrap modal bawaan template UI.
- Memperbaiki warning DataTables `Requested unknown parameter 'number'` pada tabel FFM dengan fallback mapping field row (`number`/`number_build_up`) agar kompatibel dengan variasi payload backend.
- Menyesuaikan kolom `MAWB`, `ULD Type`, `ULD Number` pada tabel FFM agar mengikuti kontrak backend: data header di `/warehouse/manifest-flight` diperkaya dengan ringkasan dari endpoint detail `/warehouse/manifest-flight/{id}/details` per baris.
- Menambahkan generator preview FFM Cargo-IMP berbasis tabel `build_up_header` + `build_up_detail` di backend endpoint baru `GET /warehouse/manifest-flight/{header_id}/ffm-preview`.
- Menambahkan validasi kelengkapan data pada generator FFM dengan output `missing_fields` dan `warnings` agar field yang belum lengkap bisa diketahui jelas sebelum kirim EDI.
- Menambahkan tombol `Cargo-IMP` pada FFM datatables frontend untuk menampilkan hasil generator FFM dalam Bootstrap modal, termasuk daftar data yang kurang jika generator belum bisa menghasilkan pesan.
- Memperbaiki validasi format ULD pada generator FFM agar kompatibel parser Cargo-IMP (`type + serial numeric 4/5 digit + owner code`) dan mencegah keluaran invalid seperti serial ULD non-numerik/terlalu pendek.
- Menambahkan validasi Cargo-IMP pada form Buildup (`buildupForm.jsx`) meliputi format MAWB, airline code, flight number, IATA origin/destination, struktur ULD (type/serial/owner), serta validasi pieces/weight per detail dengan penandaan error per field dan blokir simpan draft jika tidak valid.
- Perbaikan lanjutan kompatibilitas parse2 FFM: owner code ULD dipaksa selalu 2 karakter valid pada generator backend/frontend (termasuk fallback otomatis dari carrier) dan validasi form ULD owner diperketat menjadi tepat 2 karakter.
- Perbaikan final error parse2 DE801: owner code ULD kini hanya boleh berasal dari huruf (`A-Z`) dan tidak lagi diambil dari suffix numerik `uld_number`; fallback default selalu huruf dari carrier/`XX`.
- Menambahkan dukungan volume (`MC`) end-to-end untuk FFM:
  - frontend form Buildup menambah field `Volume (MC)` per detail ULD dan validasi wajib > 0;
  - payload draft mengirim `volume` per MAWB detail;
  - query backend `masterwaybill/bulk` menambahkan `total_volume`;
  - parser submit manifest memvalidasi volume dan menyimpan metadata volume per detail;
  - generator FFM menyusun segmen `...K<weight>MC<volume>/...` agar sesuai contoh valid parse2.
- Finalisasi persistensi volume ke tabel `buildup_*`:
  - menambahkan kolom ORM/schema `build_up_detail.volume` dan `build_up_header.total_volume`;
  - menambahkan migration Alembic `aa0000000032_add_volume_columns_to_build_up_tables.py`;
  - parser submit manifest menyimpan `total_volume` ke `build_up_header` dan `volume` ke `build_up_detail` (tanpa fallback metadata `VOL=` di remark).
- Menyesuaikan generator preview FFM backend agar membaca `MC` langsung dari `build_up_detail.volume` dan menandai baris invalid jika volume kosong/non-positif.
- Menyesuaikan payload `flight_manifest` frontend agar menyertakan agregasi `total_volume` per flight saat submit buildup.
- Perbaikan rule pembuatan buildup untuk nilai volume nol:
  - backend `submit_manifest` kini otomatis mengonversi `volume <= 0` menjadi `1` pada detail MAWB;
  - backend otomatis mengonversi `total_volume <= 0` menjadi hasil agregasi detail, atau `1` jika agregasi tetap nol;
  - frontend form buildup menormalkan input `volume = 0` menjadi `1.00` saat membentuk payload.

## File yang Diubah

- `astro/src/pages/warehouse/buildup.astro`
- `astro/src/pages/index.astro`
- `astro/jsconfig.json`
- `astro/package.json`
- `astro/package-lock.json`
- `astro/src/components/react/warehouse/buildup.jsx`
- `astro/src/components/react/warehouse/buildupDrafts.jsx`
- `astro/src/components/react/warehouse/buildupForm.jsx`
- `astro/src/components/react/warehouse/buildupDatatables.jsx`
- `astro/src/components/react/edi/fhlGenerator.js`
- `astro/src/components/react/edi/ffmDatatables.jsx`
- `astro/src/lib/api/warehouse.js`
- `materialize-fastapi/app/api/warehouse.py`
- `materialize-fastapi/app/services/warehouse_service.py`
- `materialize-fastapi/app/repositories/warehouse_repositrory.py`
- `materialize-fastapi/app/schemas/ffm_preview_schema.py`
- `materialize-fastapi/app/models/BaseDB1/build_up_header.py`
- `materialize-fastapi/app/models/BaseDB1/build_up_detail.py`
- `materialize-fastapi/app/schemas/build_up_detail_schema.py`
- `materialize-fastapi/app/schemas/exp_manifest_flight_schema.py`
- `materialize-fastapi/migrations/versions/aa0000000032_add_volume_columns_to_build_up_tables.py`
- `astro/frontend_agent.md`
- `docs/progress-2026-05-10.md`

## Hasil Verifikasi

- `npx astro check` di folder `astro/`: berhasil dengan `0 errors`, `0 warnings`, tersisa `2 hints` non-blocking.
- `python -m compileall` untuk file backend yang diubah (`warehouse.py`, `warehouse_service.py`, `warehouse_repositrory.py`, `ffm_preview_schema.py`): berhasil.
- `npm run build` di folder `astro/`: berhasil tanpa error.

## Gap / Risiko

- Tersisa 2 hints non-blocking dari `astro check`:
  - `src/assets/fonts/iconify/iconify.js` masih CommonJS style.
  - Parameter `id` pada `resolveManualChunk` di `astro.config.mjs` terdeteksi implicit `any`.
- Gap konsistensi modal frontend yang masih perlu migrasi dari SweetAlert ke Bootstrap modal (karena berisi form):
  - `astro/src/components/react/edi/DiscrepancyCodeDatatables.jsx` (edit form).
  - `astro/src/components/react/edi/FsuMessageDatatables.jsx` (edit form).
  - `astro/src/components/react/setting/MenuManagement.jsx` (edit form).
  - `astro/src/components/react/setting/RoleManagement.jsx` (edit form).
  - `astro/src/components/react/setting/UserManagement.jsx` (edit user/password/role form).
  - Catatan: dialog konfirmasi hapus pada file-file tersebut tetap boleh menggunakan SweetAlert sesuai aturan baru.

## Blocker

- Tidak ada blocker.

## Next Step

- (Opsional) Rapikan 2 hint tersisa jika ingin diagnostik sepenuhnya bersih.
