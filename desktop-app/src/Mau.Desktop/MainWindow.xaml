<Window x:Class="Mau.Desktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MAU APP Desktop"
        Height="620"
        Width="980"
        MinHeight="560"
        MinWidth="900"
        WindowStartupLocation="CenterScreen">
    <Grid Background="#F2F4F8" Margin="0,0,0,-51">
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="56" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Background="Coral" Padding="16,8">
            <DockPanel LastChildFill="True">
                <TextBlock Text="MAU APP Desktop (WPF)" Foreground="White" FontSize="18" FontWeight="SemiBold" />
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
                    <TextBlock Margin="0,0,12,0"
                               VerticalAlignment="Center"
                               Foreground="White"
                               Text="{Binding CurrentUsername, StringFormat=User: {0}}" />
                    <Button Content="Logout"
                            Padding="12,4"
                            Command="{Binding LogoutCommand}" />
                </StackPanel>
            </DockPanel>
        </Border>

        <ContentControl Grid.Row="1" Content="{Binding CurrentViewModel}" Margin="20,20,20,20" />
    </Grid>
</Window>
