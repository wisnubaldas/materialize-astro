# Progress 2026-05-15

## Ringkasan Perubahan

- Menyamakan daftar hamburger menu Dashboard dengan `serviceItems` yang dipakai pada shortcut layanan Dashboard.
- Menghapus sumber data menu terpisah agar label, icon, description, dan route service tidak perlu dipelihara ganda.
- Menampilkan `description` service pada item drawer agar konsisten dengan kartu service di Dashboard.

## File yang Diubah

- `mobile-app/src/screens/DashboardScreen.js`
- `docs/progress-2026-05-15.md`

## Hasil Verifikasi

- `node --check src\screens\DashboardScreen.js` berhasil tanpa error sintaks.
- `npm run doctor` berhasil, 17/17 checks passed.

## Gap / Risiko

- Service `Weighing`, `EDI`, dan `Warehouse` belum memiliki `href`, sehingga item drawer tampil tetapi belum melakukan navigasi.
- Visual drawer tetap perlu dicek di device/browser untuk memastikan label dan description nyaman dibaca pada layar kecil.

## Blocker

- Tidak ada blocker teknis.

## Next Step

- Tambahkan route `href` pada masing-masing service saat screen/flow backend dan frontend untuk service tersebut sudah tersedia.

---

## Update Mobile APK Build Documentation

## Ringkasan Perubahan

- Memperbarui bagian build di `mobile-app/README.md` dengan panduan Android build yang lebih lengkap.
- Menambahkan cara build preview APK via EAS Cloud.
- Menambahkan cara build APK lokal memakai `eas build --local` dengan profile `preview`.
- Menambahkan catatan prasyarat local build, setup `.env`, lokasi output artifact, install APK via `adb`, dan troubleshooting umum.

## File yang Diubah

- `mobile-app/README.md`
- `docs/progress-2026-05-15.md`

## Hasil Verifikasi

- Review manual struktur README: section `Build Android` sudah memuat APK cloud, APK lokal, production AAB, dan troubleshooting.

## Gap / Risiko

- Local EAS build resmi didukung di macOS/Linux. Untuk Windows, README mengarahkan penggunaan WSL2 karena Windows native tidak resmi didukung oleh Expo untuk `eas build --local`.
- Build lokal belum dijalankan dari Codex karena task hanya meminta update dokumentasi.

## Blocker

- Tidak ada blocker teknis.

## Next Step

- Jalankan `npx eas-cli build --platform android --profile preview --local` dari environment yang sudah memiliki Android SDK/NDK untuk memvalidasi APK lokal end-to-end.

---

## Update Backend Build Up Check Model dan Migration

## Ringkasan Perubahan

- Menambahkan model SQLAlchemy `BuildUpCheckHeader` untuk tabel `build_up_check_header`.
- Menambahkan model SQLAlchemy `BuildUpCheckDetail` untuk tabel `build_up_check_detail`.
- Menambahkan migration Alembic `buc000000001_create_build_up_check_tables.py`.
- Menambahkan relasi `header -> details` dengan cascade delete melalui FK `build_up_check_detail.header_id`.
- Menambahkan index awal untuk filter operasional: `uld`, `flight_date`, `header_id`, dan `mawb`.

## File yang Diubah

- `materialize-fastapi/app/models/BaseDB1/build_up_check_header.py`
- `materialize-fastapi/app/models/BaseDB1/build_up_check_detail.py`
- `materialize-fastapi/migrations/versions/buc000000001_create_build_up_check_tables.py`
- `docs/progress-2026-05-15.md`

## Hasil Verifikasi

- `poetry run python -m py_compile app\models\BaseDB1\build_up_check_header.py app\models\BaseDB1\build_up_check_detail.py migrations\versions\buc000000001_create_build_up_check_tables.py` berhasil.
- `poetry run ruff check app\models\BaseDB1\build_up_check_header.py app\models\BaseDB1\build_up_check_detail.py` berhasil.
- `poetry run alembic heads` berhasil dan menampilkan `buc000000001 (head)`.
- `poetry run alembic history -r bud000000001:buc000000001` berhasil menampilkan migration baru setelah `bud000000001`.

## Gap / Risiko

- Kolom timestamp baru memakai `created_at` dan `updated_at` sesuai skema yang diminta, sementara beberapa tabel existing backend masih memakai `create_at` dan `update_at`.
- Belum ada repository, service, schema, atau endpoint untuk CRUD build-up check; perubahan saat ini hanya model dan migration.

## Blocker

- Tidak ada blocker teknis.

## Next Step

- Tambahkan schema, repository, service, dependency, dan endpoint build-up check jika flow input/output sudah disepakati.

---

## Revisi Backend Build Up Check Rincian

## Ringkasan Perubahan

- Merevisi struktur `build_up_check_detail` agar tidak lagi menyimpan `pieces` dan `weight`.
- Menambahkan model dan tabel `build_up_check_rincian` untuk menyimpan rincian `pieces` dan `weight` per detail checklist.
- Menambahkan relasi `BuildUpCheckDetail.rincian` dengan cascade delete ke `BuildUpCheckRincian`.
- Menyelaraskan model `BuildUpCheckHeader.uld` menjadi wajib sesuai migration saat ini.

## File yang Diubah

- `materialize-fastapi/app/models/BaseDB1/build_up_check_header.py`
- `materialize-fastapi/app/models/BaseDB1/build_up_check_detail.py`
- `materialize-fastapi/app/models/BaseDB1/build_up_check_rincian.py`
- `materialize-fastapi/migrations/versions/buc000000001_create_build_up_check_tables.py`
- `docs/progress-2026-05-15.md`

## Hasil Verifikasi

- `poetry run python -m py_compile app\models\BaseDB1\build_up_check_header.py app\models\BaseDB1\build_up_check_detail.py app\models\BaseDB1\build_up_check_rincian.py migrations\versions\buc000000001_create_build_up_check_tables.py` berhasil.
- `poetry run ruff check app\models\BaseDB1\build_up_check_header.py app\models\BaseDB1\build_up_check_detail.py app\models\BaseDB1\build_up_check_rincian.py` berhasil.
- `poetry run alembic heads` berhasil dan tetap menampilkan `buc000000001 (head)`.
- `configure_mappers()` SQLAlchemy berhasil untuk relasi header, detail, dan rincian.

## Gap / Risiko

- Migration ini direvisi langsung karena migration sebelumnya belum diterapkan ke database oleh Codex. Jika sudah pernah dijalankan di environment lain, perlu dibuat migration alter terpisah.
- Belum ada repository, service, schema, atau endpoint untuk flow build-up check.

## Blocker

- Tidak ada blocker teknis.

## Next Step

- Tambahkan schema dan endpoint CRUD build-up check dengan payload nested detail/rincian jika kontrak API sudah disepakati.
