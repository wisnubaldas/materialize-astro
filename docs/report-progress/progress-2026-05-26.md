# Progress 2026-05-26

## Ringkasan Perubahan

- Menambahkan test untuk memastikan error server 500 dari exception tidak terduga tetap dimasking dari response API.
- Memastikan detail error teknis tidak dikirim ke client, tetapi tetap tercatat pada log backend.
- Menambahkan perbaikan deploy agar folder runtime storage backend dapat ditulis oleh user Supervisor.

## File yang Diubah

- `materialize-fastapi/tests/api/middleware/test_exception_handler.py`
- `deploy-development.sh`
- `deploy-production.sh`
- `docs/report-progress/progress-2026-05-26.md`

## Hasil Verifikasi

- `poetry run pytest tests/api/middleware/test_exception_handler.py -q` berhasil.
- Hasil: 1 test passed, 1 warning dari dependency `python_multipart`/Starlette.
- `poetry run ruff check tests/api/middleware/test_exception_handler.py` berhasil.
- `git diff --check` berhasil.
- Pemeriksaan manual: error production menunjukkan `PermissionError` saat membuat `app/storage/generated_pdf`.
- `bash -n deploy-development.sh; bash -n deploy-production.sh` belum bisa dijalankan lokal karena WSL tidak memiliki distro terpasang.

## Gap / Risiko

- Response `errors: []` pada 500 adalah perilaku aman untuk production.
- Root cause PDF Build Up adalah permission folder `app/storage/generated_pdf` karena proses aplikasi berjalan sebagai user Supervisor.

## Blocker

- Tidak ada blocker saat penambahan test.

## Next Step

- Jalankan deploy ulang atau perbaiki permission folder storage secara manual di server.
