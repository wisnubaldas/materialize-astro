# Progress 2026-05-07

## Ringkasan Perubahan

- Sinkronisasi `AGENTS.md` dengan kondisi aktual frontend Astro berbasis JavaScript (`middleware.js`, `jsconfig.json`, `.env.example`).
- Menambahkan aturan wajib referensi desain UI Materialize ke dokumentasi resmi Pixinvent.
- Menambahkan daftar folder asset UI yang boleh dipakai pada aturan agent frontend.
- Menambahkan governance struktur folder frontend untuk meningkatkan maintainability dan mencegah duplikasi.
- Menambahkan dokumen rencana perapihan struktur frontend secara bertahap di `astro/docs/frontend-structure-plan.md`.
- Menyelesaikan audit dan standardisasi import path lintas modul frontend:
  - `admin`, `angkasapura`, `auth`, `edi`, `hub-net`, `setting`, `warehouse`.
- Menambahkan barrel export per modul komponen untuk menjaga konsistensi import dan mempermudah maintenance.
- Merapikan frontmatter/page syntax legacy pada route Astro yang terdampak.
- Menambahkan laporan audit modul di `astro/docs/module-audit-2026-05-07.md`.
- Perbaikan error runtime dev:
  - `(node) Warning: To load an ES module...`
  - `Unexpected token 'export'`
  - Akar masalah: import `sweetalert2/dist/sweetalert2.esm.all.js` pada komponen React yang dirender di SSR dev.
  - Solusi: ganti import statis ke `sweetalert2` pada komponen terkait.
- Update aturan AGENTS/frontend agent untuk modul EDI:
  - Jika ada perubahan Cargo-IMP pada `astro/src/pages/edi`, wajib validasi ke `https://www.parse2.com/service-cargoimp.shtml`.
- Validasi Cargo-IMP untuk route `/edi/send-email/fwb@023-51762830` (tab Cargo-IMP) berhasil dijalankan via submit form parse2 (`FWB17`), dan ditemukan error sintaks.

## File yang Diubah

- `AGENTS.md`
- `astro/frontend_agent.md`
- `astro/docs/frontend-structure-plan.md`
- `astro/docs/module-audit-2026-05-07.md`
- `astro/src/components/angkasapura/index.js`
- `astro/src/components/auth/index.js`
- `astro/src/components/edi/index.js`
- `astro/src/components/hubnet/index.js`
- `astro/src/components/setting/index.js`
- `astro/src/components/warehouse/index.js`
- `astro/src/components/edi/DiscrepancyCodeDatatables.jsx`
- `astro/src/components/edi/FsuMessageDatatables.jsx`
- `astro/src/components/edi/FsuSearch.jsx`
- `astro/src/components/edi/SendEmailFfm.jsx`
- `astro/src/components/edi/SendEmailFhl.jsx`
- `astro/src/components/setting/MenuManagement.jsx`
- `astro/src/components/setting/RoleManagement.jsx`
- `astro/src/components/setting/UserManagement.jsx`
- `astro/src/components/warehouse/buildupDatatables.jsx`
- `astro/src/js/sweet-alert.js`
- `astro/astro.config.mjs`
- `astro/src/components/Nav.jsx`
- `astro/src/pages/admin/index.astro`
- `astro/src/pages/angkasapura/data-invoice.astro`
- `astro/src/pages/angkasapura/report.astro`
- `astro/src/pages/angkasapura/response-invoice.astro`
- `astro/src/pages/angkasapura/upload-invoice.astro`
- `astro/src/pages/auth/login.astro`
- `astro/src/pages/edi/discrepancy-code.astro`
- `astro/src/pages/edi/ffm.astro`
- `astro/src/pages/edi/fhl.astro`
- `astro/src/pages/edi/fsu-message.astro`
- `astro/src/pages/edi/fsu.astro`
- `astro/src/pages/edi/fwb.astro`
- `astro/src/pages/edi/send-email/[slug].astro`
- `astro/src/pages/hub-net/dashboard.astro`
- `astro/src/pages/hub-net/data-tracking.astro`
- `astro/src/pages/hub-net/logging.astro`
- `astro/src/pages/hub-net/report.astro`
- `astro/src/pages/hub-net/upload-excel-outgoing.astro`
- `astro/src/pages/hub-net/upload-excel-sending-export.astro`
- `astro/src/pages/setting/airlines-management.astro`
- `astro/src/pages/setting/menu-setting.astro`
- `astro/src/pages/setting/role-management.astro`
- `astro/src/pages/setting/user-management.astro`
- `astro/src/pages/warehouse/buildup.astro`
- `docs/progress-2026-05-07.md`

## Hasil Verifikasi

- Verifikasi manual isi dokumen menunjukkan aturan root dan agent frontend sudah konsisten dengan struktur aktual frontend.
- Link referensi UI dan daftar folder asset yang disepakati sudah tertulis eksplisit di agent frontend.
- Build frontend berhasil:
  - Jalankan `npm run build` di folder `astro/` pada 2026-05-07.
  - Status: sukses.
  - Catatan: ada warning ukuran chunk besar (>500KB), tetapi bukan error build.
- Retest `npm run dev` untuk route:
  - `/edi/send-email/ffm@123456`
  - `/edi/send-email/fhl@123456`
  - `/edi/send-email/fwb@123456`
  - Hasil: tidak ada lagi warning/error `Unexpected token 'export'`.
- Validasi parse2 untuk pesan `FWB/17` hasil generator route `/edi/send-email/fwb@023-51762830`:
  - Endpoint validasi: `https://www.parse2.com/cargo-imp/cargo-imp.php` (`message=FWB17`, `rule=FWB17`).
  - Hasil: `parser error: rule "Numeric" failed`.
  - Potongan indikator error dari parse2:
    - `FWB/17 023-51762830CGKSIN/T1/K23.7 ...`
    - caret (`^`) menunjuk area setelah segmen quantity detail.

## Gap / Risiko

- Struktur fisik folder codebase frontend saat ini masih legacy dan belum direlokasi massal.
- Potensi risiko regression tinggi jika pemindahan asset dilakukan sekaligus tanpa batch kecil.
- Modul `tps` belum memiliki page/komponen aktif, sehingga belum ada objek yang bisa distandarisasi pada batch ini.
- Format header FWB generator saat ini menghasilkan `.../T1/K23.7` (terdeteksi invalid di parse2); perlu penyesuaian format quantity detail agar sesuai Cargo-IMP (`FWB/17`).

## Blocker

- Tidak ada blocker teknis untuk tahap dokumentasi dan penetapan aturan.

## Next Step

- Lanjutkan refactor fisik asset secara bertahap (bukan massal), mulai dari modul dengan dependency vendor paling besar (`angkasapura`, `edi`).
- Tambahkan strategi code-splitting/dynamic import untuk menurunkan ukuran chunk besar pada build.
- Tambahkan regression check parse2 untuk sampel AWB lain agar perubahan generator FWB aman untuk data produksi yang lebih variatif.

## Update Lanjutan 2026-05-07

### Ringkasan Perubahan Tambahan

- Perbaikan `astro/src/components/edi/fwbGenerator.js` untuk menyesuaikan format `FWB/17` ke grammar parse2:
  - Header quantity-weight menjadi satu segmen (`T{pieces}K{weight}`).
  - Struktur party `SHP/CNE` menjadi multi-line sesuai Cargo-IMP (`SHP`, `NAM`, `ADR`, `LOC`, `/CC`).
  - Segment `AGT` dinormalisasi ke numeric IATA agent code (7 digit) + multi-line name/place.
  - Segment `RTD` disusun ulang ke kolom standar (`P/K/C/W/R/T`) dan goods description dipindah ke line `/NG/...`.
  - Segment `PPD` disusun ulang ke dua baris (`PPD/WT...` lalu `/OA.../OC.../CT...`).
  - Segment `CER` dan `ISU` dipotong ke batas panjang field Cargo-IMP.
  - Segment `REF` diubah ke pola sender participant (`REF///AGT/{participant}/{origin}`).

### Hasil Verifikasi Tambahan

- Validasi ulang payload route `/edi/send-email/fwb@023-51762830` ke parse2 (`FWB17`) berhasil.
- Hasil parse2 terbaru: tidak ada `parser error`; payload ditampilkan utuh oleh parser.

### Gap / Risiko Terkini

- Belum ada blocker untuk payload contoh `023-51762830`.
- Risiko residual: variasi data AWB lain (nama/alamat panjang, charge non-zero, line rate multipel) tetap perlu regression check parse2 per sampel produksi.

## Update Struktur Agent Frontend 2026-05-07

### Ringkasan Perubahan Tambahan

- Update `AGENTS.md` untuk menetapkan struktur target frontend best-practice Astro:
  - `src/assets`
  - `src/components/react`
  - `src/components/astro`
  - `src/layouts`
  - `src/pages`
  - `src/utils`
  - `src/hooks`
  - `src/lib`
- Update `astro/frontend_agent.md` agar aturan operasional frontend mengikuti struktur target yang sama.
- Menambahkan aturan migrasi bertahap dari folder legacy (`src/js`, `src/libs`, `src/vendor`, `src/scss`, `src/fonts`) agar refactor tidak memicu regression besar.
- Menjaga konsistensi rule JavaScript-first (tanpa file `.ts`/`.tsx` baru kecuali diminta eksplisit).

### Hasil Verifikasi Tambahan

- Verifikasi manual isi `AGENTS.md` dan `astro/frontend_agent.md` menunjukkan tidak ada konflik rule terhadap:
  - JavaScript-first policy.
  - Referensi UI Materialize.
  - Larangan bypass backend sebagai source of truth.

## Refactor Frontend + WPO 2026-05-07

### Ringkasan Perubahan Tambahan

- Refactor struktur frontend Astro mengikuti target best-practice:
  - Membuat `astro/src/components/react`, `astro/src/components/astro`, `astro/src/assets`, `astro/src/hooks`, `astro/src/utils`.
  - Memindahkan seluruh komponen React dari `astro/src/components/*` legacy ke `astro/src/components/react/*`.
  - Memindahkan asset font iconify dari `astro/src/fonts` ke `astro/src/assets/fonts`.
- Refactor shared layer:
  - Menambahkan `astro/src/lib/auth/token.js` untuk token helper terpusat.
  - Memperbarui `astro/src/js/auth.js` agar menggunakan helper token baru.
  - Memperbarui `astro/src/lib/api/client.js` agar tidak bergantung langsung ke `@js/auth.js`.
  - Menambahkan util baru: `astro/src/utils/formatters.js`, `astro/src/utils/toast.js`, `astro/src/utils/index.js`.
  - Menjaga kompatibilitas transisi melalui `astro/src/js/utils.js` sebagai re-export.
- Update alias path di `astro/astro.config.mjs` dan `astro/jsconfig.json` untuk struktur target + legacy compatibility.
- Cleanup file yang tidak terpakai:
  - `astro/src/js/bootstrap.js`
  - `astro/src/js/scroll-anchor.js`
  - `astro/src/lib/parse_ffm.js`
  - folder `astro/src/fonts/` lama.
- Web Performance Optimization (WPO):
  - Menambahkan `manualChunks` Rollup di `astro.config.mjs` untuk pemisahan vendor bundle (`framework-react`, `vendor-datatables`, `vendor-ui-core`, `vendor-ui-widgets`, `vendor-chartjs`, `vendor-apexcharts`, `vendor-misc`).
  - Menambahkan aturan WPO wajib di `AGENTS.md` dan `astro/frontend_agent.md`.

### Checklist Eksekusi Refactor

- [x] Freeze baseline verifikasi build sebelum refactor.
- [x] Define target map struktur frontend.
- [x] Tambah alias + compatibility window untuk migrasi.
- [x] Refactor shared layer (`auth token`, `utils`, API client dependency cleanup).
- [x] Split komponen ke `components/react` dan menyiapkan `components/astro`.
- [x] Migrasi import ke util baru (`@utils`) pada modul aktif.
- [x] Konsolidasi awal assets ke `src/assets`.
- [x] Cleanup file/script yang tidak dipakai.
- [x] WPO pass (manual chunking + evaluasi ukuran chunk).
- [x] Verifikasi ulang build setelah setiap batch utama.
- [x] Update dokumentasi dan aturan agent.

### Hasil Verifikasi Tambahan

- `npm run build` di `astro/` sukses setelah refactor struktur.
- `npm run build` di `astro/` sukses setelah penyesuaian WPO/manual chunks.
- Warning chunk >500KB masih tersisa pada `vendor-apexcharts` (sekitar 580KB) karena ukuran library upstream; sudah dipisahkan dari `chart.js` agar blast radius initial load lebih terkendali.

### Gap / Risiko Terkini

- Chunk `apexcharts` tetap besar; mitigasi lanjutan perlu lazy loading yang lebih agresif di halaman chart yang relevan.
- Struktur legacy (`src/js`, `src/libs`, `src/vendor`, `src/scss`) masih ada untuk kompatibilitas dan perlu batch migrasi lanjutan.

## Batch Lanjutan WPO 2026-05-07

### Ringkasan Perubahan Tambahan

- Refactor chart `Report Invoice`:
  - Ganti implementasi dari `react-apexcharts` ke `react-chartjs-2` + `chart.js` (dynamic import/lazy load).
  - Tetap mempertahankan behavior chart area, tooltip, dan label periode.
- Hapus dependency tidak terpakai:
  - `react-apexcharts`
  - `apexcharts`
- Update `astro.config.mjs`:
  - Hapus optimizeDeps untuk apexcharts.
  - Hapus manual chunk rule khusus apexcharts.
  - Pertahankan chunking vendor lain (`framework-react`, `vendor-chartjs`, `vendor-datatables`, `vendor-ui-*`, `vendor-misc`).

### Hasil Verifikasi Tambahan

- `npm run build` di `astro/` sukses setelah migrasi chart.
- Warning chunk >500KB hilang.
- Peringkat chunk terbesar (hasil build terbaru):
  - `framework-react` ~411KB
  - `vendor-misc` ~274KB
  - `vendor-datatables` ~206KB
  - `vendor-chartjs` ~198KB

### Dampak WPO

- Eliminasi chunk `vendor-apexcharts` (sebelumnya >500KB).
- Initial dependency footprint untuk modul report invoice menjadi lebih kecil dan terkontrol.

## Batch Refactor Modul Admin 2026-05-07

### Ringkasan Perubahan Tambahan

- Eksekusi refactor modul `admin` terlebih dahulu sesuai prioritas:
  - Menambahkan komponen domain `astro/src/components/react/admin/AdminDashboard.jsx`.
  - Menambahkan barrel export `astro/src/components/react/admin/index.js`.
  - Memperbarui route `astro/src/pages/admin/index.astro` agar memakai komponen domain admin.
- Struktur modul `admin` sekarang mengikuti pola target `components/react/<module>`.
- Tidak ada dependensi legacy tambahan (`@js`, `@libs`, `@vendor`) yang diperkenalkan pada modul admin.

### Hasil Verifikasi Tambahan

- `npm run build` di `astro/` sukses setelah refactor modul admin.
- Route `/admin` tetap terbangun normal pada SSR build.

## Batch Refactor Modul Setting 2026-05-07

### Ringkasan Perubahan Tambahan

- Refactor internal modul `setting` untuk maintainability tanpa mengubah kontrak API:
  - Tambah helper bersama `astro/src/components/react/setting/shared.js`:
    - `escapeHtml`
    - `normalizeCollection`
    - `resolveErrorMessage`
  - Terapkan helper bersama pada:
    - `MenuManagement.jsx`
    - `RoleManagement.jsx`
    - `UserManagement.jsx`
- Stabilkan callback untuk aksi tabel/modal (`useCallback`) agar listener tabel tidak re-bind berlebihan setiap render.
- Standarisasi penanganan error toast/validation agar konsisten dan lebih mudah dirawat.

### Hasil Verifikasi Tambahan

- `npm run build` di `astro/` sukses setelah refactor modul `setting`.
- Tidak ditemukan import legacy baru (`@js`, `@libs`, `@vendor`) pada komponen `setting`.

## Batch Refactor Modul Warehouse 2026-05-07

### Ringkasan Perubahan Tambahan

- Refactor internal modul `warehouse` untuk meningkatkan reusability helper dan konsistensi error handling:
  - Menambahkan helper bersama `astro/src/components/react/warehouse/shared.js`:
    - `DRAFT_STORAGE_KEY`
    - `createDraftId`
    - `readStoredDrafts` / `writeStoredDrafts`
    - `emitManifestUploaded`
    - `resolveErrorMessage`
    - `isBrowser`
  - Integrasi helper ke:
    - `buildup.jsx`
    - `buildupDrafts.jsx`
    - `buildupDatatables.jsx`
    - `buildupForm.jsx`
- Menambahkan guard browser eksplisit untuk event binding polling di `buildupDatatables` agar lebih aman terhadap konteks non-browser.
- Menstandarkan pembentukan pesan error agar seragam lintas komponen warehouse.

### Hasil Verifikasi Tambahan

- `npm run build` di `astro/` sukses setelah refactor modul `warehouse`.
- Tidak ada perubahan API contract pada endpoint warehouse; refactor murni internal frontend.

## Batch Refactor Modul Hub-net 2026-05-07

### Ringkasan Perubahan Tambahan

- Refactor internal modul `hub-net` untuk mengurangi duplikasi logic dan memperjelas error handling:
  - Menambahkan helper bersama `astro/src/components/react/hubnet/shared.js`:
    - `resolveErrorMessage`
    - `resolveApiErrorMessage`
    - `collectDropzoneErrors`
    - `formatFileSize`
    - `isBrowser`
- Integrasi helper bersama ke komponen:
  - `ChartDataTerkirim.jsx`
  - `DataTerkirim.jsx`
  - `DataTracking.jsx`
  - `SseDataTracking.jsx`
  - `UploadExcel.jsx`
  - `UploadExcelOutgoing.jsx`
- Standarisasi import flatpickr ke package resmi (menghapus ketergantungan ke wrapper legacy `@libs/flatpickr`):
  - `flatpickr`
  - `flatpickr/dist/flatpickr.min.css`
  - `flatpickr/dist/plugins/monthSelect/style.css`
- Perbaikan lifecycle flatpickr di komponen report/dashboard agar inisialisasi dan cleanup instance lebih aman.
- Minor cleanup state pada `Dashboard.jsx` (hapus state yang tidak dipakai).

### Hasil Verifikasi Tambahan

- `npm run build` di `astro/` sukses setelah refactor modul `hub-net`.
- Tidak ada warning chunk `>500KB` pada hasil build terbaru.
- Chunk modul `hubnet` terpisah dan tetap ringan (`hubnet.*.js` sekitar 25KB sebelum minify post-process).

### Dampak WPO

- Penghapusan ketergantungan wrapper flatpickr legacy mengurangi kompleksitas bundling dan menghindari duplikasi jalur style/plugin.
- Error handling upload + SSE + data fetch menjadi konsisten antar halaman `hub-net`, mempermudah debugging dan maintenance batch berikutnya.

### Gap / Risiko Terkini

- Struktur folder legacy (`src/js`, `src/libs`, `src/vendor`, `src/scss`) masih dipertahankan untuk kompatibilitas dan perlu migrasi bertahap lanjutan.
- Beberapa page `hub-net` masih menggunakan tabel/plugin lama; optimasi lanjutan bisa dilakukan lewat lazy load plugin per halaman jika diperlukan.

## Batch Refactor Modul Angkasapura 2026-05-07

### Ringkasan Perubahan Tambahan

- Refactor internal modul `angkasapura` dengan helper shared baru di `astro/src/components/react/angkasapura/shared.js`:
  - `escapeHtml`
  - `resolveErrorMessage`
  - `resolveApiErrorMessage`
  - `collectDropzoneErrors`
  - `formatFileSize`
  - `parseUploadError`
  - `isBrowser`
- Integrasi helper shared pada:
  - `upload-invoice-excel.js`
  - `data-invoice.js`
  - `ResponseInvoiceGrid.jsx`
  - `ReportInvoice.jsx`
- Perbaikan khusus `upload-invoice-excel.js`:
  - Hilangkan duplikasi parser error upload.
  - Standarisasi pesan error SSE/polling.
  - Standarisasi parse error `dropzone`.
  - Standarisasi formatter ringkasan file upload.
  - Tambah guard browser (`isBrowser`) sebelum inisialisasi SSE/polling.
- Perbaikan khusus `data-invoice.js`:
  - Sanitasi HTML atribut/detail baris pakai helper tunggal.
  - Error toast void invoice pakai `resolveApiErrorMessage` agar detail backend lebih konsisten.

### Hasil Verifikasi Tambahan

- `npm run build` di `astro/` sukses setelah refactor modul `angkasapura`.
- Tidak ada warning chunk `>500KB` pada hasil build terbaru.
- Chunk modul `angkasapura` tetap stabil sekitar `36KB` (pre-minify post-build).

### Dampak WPO

- Tidak menambah dependency baru dan tidak menambah beban vendor chunk.
- Deduplikasi helper menurunkan kompleksitas kode modul dan memudahkan optimasi lanjutan per halaman tanpa duplikasi logic.

## Batch Refactor Modul EDI 2026-05-07

### Ringkasan Perubahan Tambahan

- Menambahkan helper bersama modul EDI di `astro/src/components/react/edi/shared.js`:
  - `resolveErrorMessage`
  - `escapeHtml`
  - `boolBadge`
  - `promptEmailAddress`
- Refactor komponen pengiriman email EDI:
  - `SendEmailFfm.jsx`
  - `SendEmailFhl.jsx`
  - `SendEmailFwb.jsx`
- Refactor komponen FSU dan datatable EDI:
  - `FsuSearch.jsx`
  - `FsuMessageDatatables.jsx`
  - `DiscrepancyCodeDatatables.jsx`

### Perbaikan Kualitas Kode

- Menghapus debug log (`console.log`) pada komponen EDI yang direfactor.
- Menstandarkan penanganan error API ke `resolveErrorMessage` agar detail response backend lebih konsisten.
- Menstandarkan prompt input email lewat helper `promptEmailAddress` (termasuk perbaikan typo placeholder email).
- Menstandarkan sanitasi HTML pada modal edit (`escapeHtml`) dan badge boolean (`boolBadge`) untuk mengurangi duplikasi fungsi lokal.

### Hasil Verifikasi Tambahan

- `npm run build` di `astro/` sukses setelah refactor batch EDI.
- Tidak ada warning chunk `>500KB` pada build terbaru.
- Tidak ditemukan sisa `console.log` pada `astro/src/components/react/edi`.

### Dampak WPO

- Tidak ada dependency baru yang ditambahkan.
- Chunk modul EDI tetap terkontrol (sekitar `41.7KB` pre-minify) dan sedikit turun dibanding baseline batch sebelumnya.

## Batch Refactor Auth + Guard Flow 2026-05-07

### Ringkasan Perubahan Tambahan

- Hardening middleware auth di `astro/src/middleware.js`:
  - Menambahkan dukungan redirect `next` untuk mempertahankan tujuan halaman saat user belum login.
  - Menambahkan validasi path internal aman (`isSafeInternalPath`) untuk mencegah open redirect.
  - Menambahkan helper redirect login terpusat (`buildLoginRedirectPath`) untuk alur unauthorized.
  - Menambahkan pembacaan `next` pada route login saat token valid.
  - Menambahkan invalidasi cache profile token saat cookie auth dihapus.
- Update konfigurasi auth di `astro/src/lib/auth/config.js`:
  - Menambahkan konstanta `LOGIN_NEXT_PARAM = "next"`.
- Perbaikan login page flow di:
  - `astro/src/js/pages/login-page.js`
  - `astro/public/js/login-page.js`
  - Perubahan: redirect setelah login sekarang membaca query `next` yang tervalidasi, dan tidak lagi redirect prematur hanya berdasarkan keberadaan token di browser.
- Perbaikan API client unauthorized flow di `astro/src/lib/api/client.js`:
  - Saat HTTP `401`, token frontend dibersihkan (`clearAccessToken`) sebelum redirect ke `/auth/login`.
- Perbaikan error handling login di `astro/src/js/auth.js`:
  - Menambahkan parser pesan error auth (`resolveAuthErrorMessage`) agar detail backend lebih konsisten.
  - Membersihkan token lama jika login gagal.

### Hasil Verifikasi Tambahan

- `npm run build` di `astro/` sukses setelah refactor auth + guard flow.
- Tidak ada warning chunk `>500KB` pada build terbaru.
- Chunk login script tetap kecil (`login-page.*.js` sekitar `1.3KB` pre-minify).

### Dampak WPO

- Tidak menambah dependency baru.
- Perubahan auth flow bersifat logic-level dengan dampak ukuran bundle sangat kecil.

## Batch Cleanup Final Legacy Lintas Modul 2026-05-07

### Ringkasan Perubahan Tambahan

- Menghapus file/artefak legacy yang tidak direferensikan aktif:
  - `astro/src/components/react/angkasapura/CreateInvoice.jsx`
  - `astro/src/components/react/hubnet/DataCard.jsx`
  - `astro/public/js/login-page.js`
  - `astro/public/js/template-customizer.js`
  - folder kosong `astro/public/js/`
- Merapikan komponen `WelcomeBanner`:
  - Menghapus blok komentar dead code lama.
  - Menghapus prop `name` yang tidak digunakan.

### Hasil Verifikasi Tambahan

- Audit referensi menunjukkan file yang dihapus tidak dipakai oleh route aktif maupun import komponen aktif.
- `npm run build` di `astro/` sukses setelah cleanup.
- Tidak ada warning chunk `>500KB`.

### Dampak WPO

- Output JS yang diproses minifier turun dari 42 file ke 40 file.
- Total ukuran JS sebelum minify menurun ke kisaran ~1589KB (dari baseline ~1640KB pada batch awal refactor besar).

### Catatan

- Setelah penghapusan static JS legacy, folder `dist/client/js` dapat tidak terbentuk pada build tertentu; script minify sudah menangani kondisi ini dengan mode skip aman.

## Tahap Penutupan Frontend 2026-05-07

### Ringkasan Perubahan Tambahan

- Menyelesaikan audit akhir import/alias frontend Astro:
  - Verifikasi alias utama (`@components`, `@lib`, `@utils`, `@js`) berjalan konsisten di route dan komponen aktif.
  - Tidak ditemukan referensi tersisa ke file static legacy `astro/public/js/login-page.js` dan `astro/public/js/template-customizer.js`.
- Hardening script minify build di `astro/minify-public-js.js`:
  - Rewrite log output ke format ASCII yang konsisten.
  - Menambahkan mode skip aman untuk folder output yang tidak selalu terbentuk (`dist/client/js`).
  - Menambahkan ringkasan minify terstruktur (`[minify][summary]`) untuk CSS/JS.
  - Menjaga fungsi utama tetap sama: minify CSS + minify JS hasil build.

### Hasil Verifikasi Tambahan

- `npm run build` di `astro/` sukses setelah hardening script.
- Output log minify lebih bersih dan mudah dibaca.
- Tidak ada warning chunk `>500KB` pada build terbaru.

### Dampak WPO

- Tidak ada dependency baru.
- Operasional pipeline build lebih stabil karena penanganan kondisi output folder yang dinamis.
- Monitoring ukuran hasil minify lebih konsisten untuk baseline batch pengembangan berikutnya.

## Final Smoke Test Route Utama 2026-05-07

### Ringkasan Pengujian

- Menjalankan preview server lokal (`npm run preview -- --host 127.0.0.1 --port 4321`) dan melakukan smoke test HTTP pada route utama modul frontend.
- Validasi fokus pada status route login publik (`200`) dan redirect route terproteksi (`302`) termasuk query `next`.

### Hasil Smoke Test

- `/auth/login` -> `200`
- `/` -> `302` ke `/auth/login?next=%2F`
- `/admin` -> `302` ke `/auth/login?next=%2Fadmin`
- `/setting/menu-setting` -> `302` ke `/auth/login?next=%2Fsetting%2Fmenu-setting`
- `/warehouse/buildup` -> `302` ke `/auth/login?next=%2Fwarehouse%2Fbuildup`
- `/hub-net/dashboard` -> `302` ke `/auth/login?next=%2Fhub-net%2Fdashboard`
- `/angkasapura/report` -> `302` ke `/auth/login?next=%2Fangkasapura%2Freport`
- `/edi/ffm` -> `302` ke `/auth/login?next=%2Fedi%2Fffm`

### Kesimpulan

- Guard route dan redirect `next` berjalan sesuai target untuk kondisi belum login.
- Route publik login tetap dapat diakses normal.
