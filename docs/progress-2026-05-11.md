# Progress 2026-05-11

## Ringkasan Perubahan

- Menjalankan Phase 1 quick wins refactor backend:
  - memperbaiki Ruff `B012` pada middleware request logging (menghilangkan `return` di blok `finally`);
  - menghapus `print()` runtime pada `main.py`, `buildup_service.py`, dan SSE stream handler;
  - memperbaiki pola raise exception (`B904`) dengan `raise ... from ...` pada SSE dan proses generate PDF build up.
- Menuntaskan sisa warning Ruff hingga `0` temuan:
  - refactor `ceisa_log_repository` dengan object payload/metrik untuk menghilangkan warning `too-many-arguments`;
  - refactor `warehouse_repositrory.get_masterwaybill_by_awbs` ke helper methods untuk menurunkan kompleksitas cabang (`too-many-branches`);
  - refactor `warehouse_service.generate_ffm_preview` + context object untuk menghilangkan `too-many-branches` dan `too-many-arguments`;
  - normalisasi schema CEISA X-Ray ke `snake_case` internal + alias camelCase agar lolos rule naming (`N815`) tanpa mengubah kontrak payload eksternal.
- Penyesuaian format header FWB generator di frontend EDI: segmen volume `MC` digabung ke quantity/weight (contoh `T1K12MC10.45`), tidak lagi dipisah dengan `/`.
- Perbaikan formatter segmen `RTG` di FWB generator agar sesuai grammar Cargo-IMP:
  - token routing pertama otomatis dipastikan mengandung carrier code (contoh input `CGK/DPS` menjadi `RTG/CGKII/DPS`);
  - mendukung input user-friendly (dipisah slash/spasi/koma) dan fallback aman ke `RTG/{destination}II`.
- Perbaikan formatter segmen `CVD` di FWB generator:
  - menambahkan normalisasi indikator prepaid/collect (`weight_charge_pp_cc`) agar hanya menghasilkan kode valid (`PP`, `CC`, `PC`, `CP`);
  - input tidak valid seperti `0` otomatis fallback ke `PP` untuk mencegah parser error pada `DE403_PCInd`.
- Penyesuaian formatter party segment (`SHP/CNE`) di FWB generator agar postal code mengikuti pola coded location Cargo-IMP:
  - postal digabung pada baris country (`/{COUNTRY}/{POSTAL}`), bukan segmen terpisah `ZIP/...`;
  - contoh: `/ID/1435` menggantikan kombinasi `/ID` lalu `ZIP/1435`.
- Hardening validasi FWB untuk lolos parse2 pada kondisi data form tidak lengkap:
  - normalisasi airport code (`origin/destination`) ke 3 alpha dengan fallback aman;
  - normalisasi country code ke 2 alpha, city/place dipotong ke panjang aman, dan postal code dibersihkan ke alfanumerik;
  - segmen `SHP/CNE` dipastikan selalu membentuk blok wajib (`NAM`, `ADR`, `LOC`, country line), termasuk fallback nilai default;
  - merge `buildFwbDefaults` + data `fwb` saat render form dan saat submit, agar field penting otomatis terisi saat proses validasi/generate pesan.
- Memperbarui pedoman backend agent pada `materialize-fastapi/backend_agent.md` dengan aturan wajib performa berbasis Ruff.
- Menambahkan aturan wajib async safety: larangan mencampur `async def` dengan kode blocking (contoh `time.sleep()`), serta panduan penggunaan `await asyncio.sleep(...)` dan threadpool untuk library blocking.
- Menambahkan query baru untuk pencarian MAWB domestic outgoing saat proses create build up.
- Mengubah alur lookup MAWB di repository warehouse agar:
  - query export tetap dijalankan lebih dulu;
  - query domestic outgoing dijalankan untuk MAWB yang belum ditemukan;
  - hasil akhir tetap digabung tanpa duplikasi dan mengikuti urutan input MAWB.
- Memperbarui ringkasan endpoint warehouse masterwaybill bulk agar mencerminkan sumber data export + domestic outgoing.
- Rename template PDF build up dari `air_cargo_manifest2.html` menjadi `pdf_build_up.html` dan update referensi di service.
- Perbaikan layout kolom tabel PDF manifest (`ULD / NUMBER MAWB`, `WEIGHT`, `NUMBER OF PIECES`) agar teks tidak terpotong dan kolom lebih proporsional.
- Menjalankan Phase 2 refactor backend untuk modul `auth` dan `setting`:
  - route/controller dirapikan agar fokus pada endpoint definition + dependency injection + pemanggilan service;
  - query database dan validasi bisnis dipindahkan ke layer `repository` dan `service` baru;
  - menambah dependency factory khusus read/write untuk `SettingService` dan dependency service untuk `AuthService`.
- Menjalankan Phase 3 standardisasi response error backend:
  - menambahkan schema error envelope baru (`status`, `message`, `errors[]`) untuk konsistensi lintas endpoint;
  - menambahkan util mapper error validasi/request agar format error tidak tersebar per route;
  - mengaktifkan global exception handler di `main.py` (HTTPException, RequestValidationError, unhandled Exception);
  - menyelaraskan response unauthorized di JWT middleware agar konsisten dengan envelope error global.

## File yang Diubah

- `materialize-fastapi/backend_agent.md`
- `materialize-fastapi/app/api/middleware/request_logging.py`
- `materialize-fastapi/app/main.py`
- `materialize-fastapi/app/api/sse.py`
- `materialize-fastapi/app/repositories/query/get_domestic_outgoing_buildup.sql`
- `materialize-fastapi/app/repositories/warehouse_repositrory.py`
- `materialize-fastapi/app/api/warehouse.py`
- `materialize-fastapi/app/services/buildup_service.py`
- `materialize-fastapi/app/repositories/ceisa_log_repository.py`
- `materialize-fastapi/app/integrations/ceisa/log_service.py`
- `materialize-fastapi/app/integrations/ceisa/sync_job.py`
- `materialize-fastapi/app/services/warehouse_service.py`
- `materialize-fastapi/app/schemas/ceisa_xray_photo_schema.py`
- `materialize-fastapi/app/integrations/ceisa/xray_photo_service.py`
- `materialize-fastapi/app/integrations/ceisa/xray_photo_get_service.py`
- `astro/src/components/react/edi/fwbGenerator.js`
- `materialize-fastapi/app/templates/pdf_build_up.html`
- `materialize-fastapi/app/templates/air_cargo_manifest.css`
- `materialize-fastapi/app/api/auth.py`
- `materialize-fastapi/app/api/setting.py`
- `materialize-fastapi/app/dependencies/auth_deps.py`
- `materialize-fastapi/app/dependencies/setting_deps.py`
- `materialize-fastapi/app/repositories/auth_repository.py`
- `materialize-fastapi/app/repositories/setting_repository.py`
- `materialize-fastapi/app/services/auth_service.py`
- `materialize-fastapi/app/services/setting_service.py`
- `materialize-fastapi/app/main.py`
- `materialize-fastapi/app/api/middleware/exception_handler.py`
- `materialize-fastapi/app/api/middleware/auth_middleware.py`
- `materialize-fastapi/app/utils/api_response.py`
- `materialize-fastapi/app/schemas/api_response_schema.py`

## Hasil Verifikasi

- `poetry run ruff check app/api/middleware/request_logging.py app/main.py app/services/buildup_service.py app/api/sse.py` (dijalankan dari folder `materialize-fastapi/`) lulus (`All checks passed`).
- `poetry run ruff check app` (dijalankan dari folder `materialize-fastapi/`) lulus (`All checks passed`) setelah refactor lanjutan.
- Review manual konten `materialize-fastapi/backend_agent.md` memastikan dua aturan wajib baru sudah tercantum: acuan performa Ruff dan larangan blocking call di `async def`.
- `python -m compileall materialize-fastapi/app/repositories/warehouse_repositrory.py materialize-fastapi/app/api/warehouse.py` berhasil tanpa error.
- `python -m compileall materialize-fastapi/app/services/buildup_service.py` berhasil tanpa error.
- Review `git diff` menunjukkan perubahan terfokus pada penambahan query domestic outgoing dan integrasi lookup MAWB build up.
- `poetry run ruff check app` (dijalankan dari folder `materialize-fastapi/`) lulus (`All checks passed`) setelah implementasi Phase 2.
- `python -m compileall app/api/auth.py app/api/setting.py app/dependencies/auth_deps.py app/dependencies/setting_deps.py app/repositories/auth_repository.py app/repositories/setting_repository.py app/services/auth_service.py app/services/setting_service.py` lulus tanpa error.
- `poetry run ruff check app` (dijalankan dari folder `materialize-fastapi/`) lulus (`All checks passed`) setelah implementasi Phase 3 error envelope + exception handler.
- `python -m compileall app/api/middleware/exception_handler.py app/api/middleware/auth_middleware.py app/utils/api_response.py app/schemas/api_response_schema.py app/main.py` lulus tanpa error.

## Gap / Risiko

- Query domestic outgoing bergantung pada konsistensi field `MasterAWB`, `Pieces`, `VolumeCargo`, `Netto`, `KindOfNature`, dan relasi `id_header -> _id` pada tabel outgoing.
- Jika ditemukan variasi data historis (mis. null/format tidak standar), perlu validasi sample data produksi/staging untuk memastikan agregasi total sesuai ekspektasi bisnis.

## Blocker

- Tidak ada blocker teknis pada implementasi kode.

## Next Step

- Uji regresi endpoint utama di environment dev/staging untuk memastikan frontend sudah menyesuaikan format error baru (tidak lagi mengandalkan key `detail` tunggal).
- Lanjutkan rollout bertahap envelope sukses (`status: success`) bila disetujui, agar kontrak response menjadi simetris antara sukses dan error.

---

## Update Sesi EDI FHL (2026-05-11)

### Ringkasan Perubahan

- Mengubah alur modul `EDI > FHL` agar tidak lagi menggunakan slug route untuk preview/generate pesan.
- Mengubah tabel FHL:
  - kolom `MasterAWB` tidak lagi menjadi hyperlink;
  - menambahkan kolom aksi berisi tombol `Cargo-IMP`, `Cargo-XML`, dan `Send Email` (tanpa aksi).
- Menambahkan modal preview FHL di halaman datatable untuk menampilkan hasil format:
  - `Cargo-IMP`;
  - `Cargo-XML`.
- Memindahkan logic generator FHL dari frontend (`astro/src/components/react/edi/fhlGenerator.js`) ke backend FastAPI.
- Menambahkan endpoint backend baru untuk generate preview FHL dari MAWB:
  - `GET /edi/fhl-message/{mawb}`.
- Menambahkan rujukan agent tambahan di `AGENTS.md` ke folder `docs/fleet_master/` untuk task EDI/Cargo message.

### File yang Diubah

- `AGENTS.md`
- `astro/src/components/react/edi/FhlDatatables.jsx`
- `astro/src/components/react/edi/index.js`
- `astro/src/lib/api/edi.js`
- `astro/src/pages/edi/send-email/[slug].astro`
- `astro/src/components/react/edi/SendEmailFhl.jsx` (dihapus)
- `astro/src/components/react/edi/fhlGenerator.js` (dihapus)
- `materialize-fastapi/app/api/edi.py`
- `materialize-fastapi/app/services/edi_service.py`
- `materialize-fastapi/app/libs/fhl_message_builder.py`
- `materialize-fastapi/app/schemas/fhl_message_schema.py`

### Hasil Verifikasi

- `poetry run ruff check app/libs/fhl_message_builder.py app/schemas/fhl_message_schema.py app/services/edi_service.py app/api/edi.py` lulus (`All checks passed`).
- `python -m compileall app/libs/fhl_message_builder.py app/schemas/fhl_message_schema.py app/services/edi_service.py app/api/edi.py` lulus tanpa error.
- `npm run build` (dari folder `astro/`) lulus tanpa error.

### Gap / Risiko

- Format `Cargo-XML` FHL saat ini adalah representasi XML internal untuk kebutuhan preview operasional; jika diperlukan kompatibilitas penuh ke standar eksternal tertentu, perlu validasi lanjutan terhadap referensi implementasi target.
- Tombol `Send Email` pada FHL sengaja belum memiliki aksi sesuai arahan; flow pengiriman email perlu ditentukan di iterasi berikutnya.

### Blocker

- Tidak ada blocker teknis pada implementasi ini.

### Next Step

- Definisikan flow final untuk aksi `Send Email` pada FHL (single/multi recipient, audit log, dan retry policy).
- Validasi sampel output `Cargo-IMP`/`Cargo-XML` FHL terhadap dokumen rujukan di `docs/fleet_master/` bersama user bisnis.

---

## Update Sesi EDI FWB (2026-05-11)

### Ringkasan Perubahan

- Mengubah alur modul `EDI > FWB` mengikuti pola baru seperti FHL (tanpa slug route pada flow utama FWB).
- Menambahkan alur pembuatan FWB langsung dari datatable:
  - tombol `Buat FWB` pada datatable membuka komponen React form FWB yang menggantikan konten datatable.
- Menghapus sesi ke-12 `Alamat Email Tujuan` pada form FWB.
- Menambahkan aturan status tombol aksi di datatable:
  - `Send Email`, `Cargo-IMP`, dan `Cargo-XML` otomatis nonaktif jika MAWB belum memiliki data FWB tersimpan.
- Menambahkan tombol `Detail FWB` pada datatable untuk menampilkan isi data FWB dari database (menggantikan kebutuhan tab `FWB DB` lama).
- Menambahkan endpoint backend baru untuk memisahkan create/save dari email flow:
  - `POST /edi/fwb` untuk simpan payload FWB tanpa kirim email.
- Menambahkan endpoint backend preview message FWB:
  - `GET /edi/fwb-message/{mawb}` untuk payload `cargo_imp` dan `cargo_xml`.
- Menambahkan builder backend `FWB message` agar preview di frontend tidak bergantung slug flow lama.

### File yang Diubah

- `astro/src/components/react/edi/fwbDatatables.jsx`
- `astro/src/components/react/edi/FwbComposer.jsx` (baru)
- `astro/src/components/react/edi/fwbForm.jsx`
- `astro/src/lib/api/edi.js`
- `astro/src/pages/edi/send-email/[slug].astro`
- `astro/src/components/react/edi/index.js`
- `materialize-fastapi/app/api/edi.py`
- `materialize-fastapi/app/services/edi_service.py`
- `materialize-fastapi/app/libs/fwb_message_builder.py` (baru)
- `materialize-fastapi/app/schemas/fwb_message_schema.py` (baru)
- `materialize-fastapi/app/schemas/fwb_save_request_body.py` (baru)

### Hasil Verifikasi

- `poetry run ruff check app/api/edi.py app/services/edi_service.py app/libs/fwb_message_builder.py app/schemas/fwb_message_schema.py app/schemas/fwb_save_request_body.py` lulus (`All checks passed`).
- `python -m compileall app/api/edi.py app/services/edi_service.py app/libs/fwb_message_builder.py app/schemas/fwb_message_schema.py app/schemas/fwb_save_request_body.py` lulus tanpa error.
- `npm run build` (dari folder `astro/`) lulus tanpa error.

### Gap / Risiko

- Endpoint `GET /edi/fwb-message/{mawb}` menggunakan `raw_message` jika tersedia; jika tidak ada, fallback generator backend digunakan. Perlu validasi bisnis apakah fallback ini sudah cukup untuk seluruh varian data historical.
- Tombol `Send Email` di flow FWB masih placeholder (belum mengeksekusi kirim email) sesuai kebutuhan tahap saat ini.

### Blocker

- Tidak ada blocker teknis pada implementasi ini.

### Next Step

- Finalisasi flow `Send Email` FWB (UI confirmation + backend retry/audit) setelah requirement operasional disepakati.
- Tambahkan validasi sintaks output `FWB` terhadap parser Cargo-IMP pada sampel produksi untuk memastikan konsistensi hasil builder backend/frontend.

---

## Update Sesi EDI FWB - Mandatory & Masking (2026-05-11)

### Ringkasan Perubahan

- Membaca ulang referensi `docs/fleet_master/CIMP-34thEdition2014.pdf` (section `7. FWB Message Specification`) untuk menyelaraskan format field FWB terhadap pola karakter Cargo-IMP (`aaa`, `n[...8]`, `n[...7]p`, `PP/CC`, dsb).
- Menambahkan validator dan sanitizer khusus FWB di frontend:
  - masking real-time per field (kode IATA/ISO, AWB digit, numeric decimal, enum PP/CC/PC/CP);
  - validasi mandatory field pada submit `Simpan FWB`;
  - validasi format penting agar payload tidak menyimpan nilai yang melanggar pola element FWB.
- Menandai ulang field wajib pada form FWB dan menambahkan helper format pada field inti.
- Menambahkan tampilan error per field (`is-invalid`) pada form FWB.
- Menyesuaikan default `charge_code` menjadi `PX` agar mengikuti pola umum segment CVD dan mengurangi invalid payload kosong.

### File yang Diubah

- `astro/src/components/react/edi/fwbValidation.js` (baru)
- `astro/src/components/react/edi/fwbForm.jsx`
- `astro/src/components/react/edi/FwbComposer.jsx`

### Hasil Verifikasi

- `npm run build` (dari folder `astro/`) lulus tanpa error.
- Review manual pada form FWB memastikan:
  - field wajib menampilkan indikator `*`;
  - masking bekerja saat mengetik;
  - submit diblokir dan menampilkan error bila mandatory/format belum valid.

### Gap / Risiko

- Status `M/C/O` detail per-elemen pada PDF CIMP tidak seluruhnya terbaca sempurna dari ekstraksi teks layout, sehingga penetapan mandatory difokuskan pada elemen inti pembentuk FWB/17 yang dipakai pada flow saat ini.
- Validasi ini masih di level frontend (UX). Validasi final di backend masih perlu diperketat jika ingin enforcement penuh di sisi API.

### Blocker

- Tidak ada blocker teknis.

### Next Step

- Tambahkan validasi final di backend schema/service FWB dengan rule inti yang sama agar konsisten lintas client.
- Uji sampel payload produksi ke validator Cargo-IMP (`FWB/17`) untuk memastikan seluruh varian data operasional lolos parser.

---

## Update Sesi Frontend Notifikasi (2026-05-11)

### Ringkasan Perubahan

- Memperbaiki arsitektur toaster global frontend agar notifikasi selalu tampil statis pada layar (viewport), tidak ikut area konten yang bisa ter-scroll.
- Mengganti render toast dari posisi `absolute` menjadi viewport-level `fixed` dan dirender via `portal` langsung ke `document.body`.
- Menambahkan hardening event toast global:
  - queue sementara untuk notifikasi yang terjadi sebelum toaster listener siap;
  - flush queue saat provider mount;
  - flag readiness agar notifikasi konsisten lintas modul.
- Menambahkan normalisasi tipe notifikasi agar semua pemanggilan frontend konsisten:
  - alias `error` otomatis dipetakan ke `danger`;
  - fallback tipe tak dikenal ke `info`.
- Menyempurnakan tampilan toast:
  - default title berdasarkan tipe (`Sukses`, `Error`, `Peringatan`, `Informasi`);
  - ikon per tipe;
  - responsif mobile;
  - `pointer-events` aman supaya hanya toast yang interaktif.

### File yang Diubah

- `astro/src/components/react/ToasterProvider.jsx`
- `astro/src/utils/toast.js`
- `astro/src/assets/scss/_bootstrap-extended/_toasts.scss`

### Hasil Verifikasi

- `npm run build` (dari folder `astro/`) lulus tanpa error.
- Review implementasi memastikan seluruh `showToast(...)` di modul frontend kini keluar pada viewport-level toaster yang sama.

### Gap / Risiko

- Verifikasi visual interaktif lintas seluruh halaman dilakukan via build + review code; untuk memastikan perilaku final di semua resolusi dan skenario modal, tetap disarankan smoke test manual di browser.

### Blocker

- Tidak ada blocker teknis.

### Next Step

- Jalankan smoke test cepat pada halaman dengan konten panjang (scroll dalam) dan halaman dengan modal aktif untuk memastikan overlap/z-index sudah sesuai ekspektasi operasional.
