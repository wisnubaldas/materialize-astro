# Progress 2026-05-04

## Ringkasan Perubahan
- Memperbaiki visibilitas tombol `Sign In` pada login window desktop WPF.
- Menambahkan fallback text pada binding konten tombol agar label tetap muncul jika binding null/gagal.
- Menetapkan `Background` dan `BorderBrush` tombol ke warna primary lokal agar kontras konsisten.

## File yang Diubah
- desktop-app/src/Mau.Desktop/LoginWindow.xaml
- docs/progress-2026-05-04.md

## Hasil Verifikasi/Test
- `dotnet build desktop-app/Mau.Desktop.sln` sukses.
- Hasil: 0 warning, 0 error.

## Gap / Risiko
- Patch ini fokus ke visibilitas tombol login. Belum ada uji visual manual lintas tema (light/dark/high-contrast) di runtime.

## Blocker
- Tidak ada.

## Next Step
- Jalankan verifikasi manual UI login pada environment operasional untuk memastikan tombol `Sign In` tetap terlihat pada semua state (enabled/disabled/busy).

## Update Tambahan (Button Spacing)
- Layout tombol `Close` dan `Sign In` di login window diubah dari kolom fleksibel (`*`) menjadi kolom `Auto` dengan container `HorizontalAlignment="Center"` agar jarak antar tombol lebih rapat dan konsisten.
- Menambahkan `MinWidth="150"` pada tombol `Close` agar ukuran visual seimbang dengan tombol `Sign In`.

## Verifikasi Tambahan
- `dotnet build desktop-app/Mau.Desktop.sln` sukses (0 error).
- Terdapat warning file lock `Mau.Desktop.exe` karena aplikasi sedang berjalan (`Mau.Desktop (14368)`).

## Update Tambahan (Button Group Left Align)
- Group tombol aksi login diubah menjadi rata kiri dengan `HorizontalAlignment="Left"` pada container tombol.
- Struktur kolom `Auto` tetap dipertahankan agar jarak antar tombol konsisten dan tidak melebar.

## Verifikasi Tambahan 2
- Build standar sempat gagal karena file output dikunci proses `Mau.Desktop` yang sedang berjalan.
- Build verifikasi compile berhasil dengan `dotnet build -p:UseAppHost=false` (0 error, warning lock file non-blocking).

## Update Tambahan (Desktop AGENTS)
- Memperbarui `desktop-app/AGENTS.md` dengan aturan wajib bahwa komponen kompleks harus dibuat sebagai `UserControl` dan expose properti via `DependencyProperty` (gaya props React).
- Menambahkan aturan wajib penggunaan style bawaan WPF UI untuk pembuatan komponen (contoh `ui:Button Appearance="Primary" ...`).
- Menambahkan ketentuan bahwa custom style harus dibuat reusable (shared `ResourceDictionary`/style bersama), bukan inline duplikatif per halaman.

## Verifikasi Tambahan 3
- Perubahan bersifat dokumentasi (`AGENTS.md`), tidak ada perubahan runtime code.
- Validasi dilakukan melalui review konten dokumen dan konsistensi dengan arahan arsitektur desktop.

## Update Tambahan (Login Button Always Visible)
- Memperbaiki bug visual saat login gagal di mana tombol `Sign In` terlihat hilang karena state disabled.
- Menambahkan style reusable `LoginPrimaryActionButtonStyle` pada `LoginWindow.xaml` agar tombol primary tetap kontras ketika `IsEnabled=False`.
- Menambahkan brush `PrimaryButtonDisabledBrush` untuk state disabled yang masih terbaca jelas.

## Verifikasi Tambahan 4
- `dotnet build desktop-app/Mau.Desktop.sln -p:UseAppHost=false` berhasil.
- Hasil: 0 warning, 0 error.

## Update Tambahan (Login Retry + HTTP Contract Fix)
- Memperbaiki perilaku tombol login agar selalu terlihat dan tetap aktif untuk percobaan login berulang setelah gagal.
- `LoginWindowViewModel.CanLogin()` diubah hanya bergantung pada `!IsBusy` sehingga tombol tidak lagi terkunci karena validasi input awal.
- `LoginWindow.xaml.cs` disesuaikan agar state visual `LoginButton` tidak di-disable oleh code-behind.
- Request DTO login diperjelas dengan `[JsonPropertyName("email")]` dan `[JsonPropertyName("password")]` agar payload JSON konsisten dengan kontrak backend.
- `ApiOptions` default base URL diperbaiki ke `http://localhost:8000` dan ditambahkan normalisasi URL untuk mencegah mismatch `https` lokal yang memicu `Invalid HTTP request received`.

## Verifikasi Tambahan 5
- `dotnet build desktop-app/Mau.Desktop.sln -p:UseAppHost=false` berhasil.
- Hasil: 0 warning, 0 error.

## Update Tambahan (Login 200 Tapi Gagal Masuk Dashboard)
- Ditemukan mismatch kontrak JSON respons login: backend mengirim `access_token`/`token_type` (snake_case), sedangkan model desktop sebelumnya membaca `AccessToken`/`TokenType` tanpa mapping eksplisit.
- `AuthLoginResponse` diperbaiki dengan atribut `[JsonPropertyName("access_token")]` dan `[JsonPropertyName("token_type")]` agar deserialisasi token sukses dan dapat dipakai untuk request `/auth/me`.

## Verifikasi Tambahan 6
- `dotnet build desktop-app/Mau.Desktop.sln -p:UseAppHost=false` berhasil.
- Hasil: 0 warning, 0 error.

## Update Tambahan (Hapus AGENTS Desktop Lokal)
- Menghapus file desktop-app/AGENTS.md untuk menghilangkan ambiguitas instruksi agent pada project desktop.
- Menetapkan bahwa instruksi yang digunakan hanya AGENTS.md di root repository.

## Verifikasi Tambahan 7
- Validasi file: desktop-app/AGENTS.md sudah tidak ada (Test-Path = False).
- Perubahan bersifat dokumentasi/struktur instruksi, tidak ada perubahan runtime code.

## Update Tambahan (Referensi Resmi WPF UI)
- Menambahkan referensi resmi WPF UI https://github.com/lepoco/wpfui/tree/main pada desktop-app/desktop_agent.md sebagai acuan pembuatan UI desktop.

## Verifikasi Tambahan 8
- Validasi konten dokumen: referensi WPF UI sudah tercantum pada section Desktop Technology Standard.
- Perubahan bersifat dokumentasi panduan engineering, tidak ada perubahan runtime code.

## Update Tambahan (Hardening Desktop + Kebijakan Tanpa Testing)
- Memperbarui AGENTS.md root untuk menghapus requirement testing eksplisit, termasuk perubahan wording verifikasi menjadi lint/build/pemeriksaan manual.
- Menghapus section Testing Rules dari desktop-app/desktop_agent.md sesuai kebijakan project tidak menggunakan unit test.
- Menambahkan shared resource dictionary Resources/SemanticColors.xaml dan merge ke App.xaml agar token warna UI desktop terpusat.
- MainWindow.xaml direfaktor memakai DynamicResource semantic color, serta tombol logout dipindah ke Command (ViewModel.LogoutCommand) untuk kepatuhan MVVM.
- MainWindowViewModel menambahkan LogoutCommand + event LogoutRequested; MainWindow.xaml.cs menangani flow UI logout setelah command dipicu.
- LoginWindow.xaml direfaktor: tombol Sign In kini memakai Command + CommandParameter (password), handler click login dihapus.
- LoginWindow.xaml.cs membersihkan code-behind dari pemaksaan LoginButton.IsEnabled = true.
- App.xaml.cs diperbaiki agar popup startup error tidak menampilkan stack trace mentah ke user.
- Menambahkan BackendApiException dan memperbarui BackendApiClient untuk parsing error backend + throw terstruktur.
- AuthService diperbarui dengan mapping status code eksplisit: 400, 401, 403, 404, 409, 422, 429, 500.

## Verifikasi Tambahan 9
- dotnet build desktop-app/Mau.Desktop.sln -p:UseAppHost=false berhasil.
- Hasil: 0 warning, 0 error.
- Verifikasi dilakukan via build/compile dan review alur runtime (tanpa penambahan unit test sesuai kebijakan terbaru).

## Update Tambahan (Fix Login Request Tidak Terkirim)
- Memperbaiki alur login di `LoginWindowViewModel` agar password tidak hanya bergantung ke `CommandParameter` dari `PasswordBox.Password`.
- Menambahkan cache `_latestPasswordInput` dari event `OnPasswordChanged` sebagai fallback ketika binding command parameter tidak ter-update.
- Menambahkan blok komentar debug pada fungsi `OnPasswordChanged` dan `LoginAsync` untuk memudahkan tracing akar masalah login request.
- Menyesuaikan pesan error agar menampilkan `result.ErrorMessage` dari service ketika login gagal.

## Verifikasi Tambahan 10
- `dotnet build desktop-app/Mau.Desktop.sln -p:UseAppHost=false` berhasil.
- Hasil: 0 warning, 0 error.
- Dampak: klik `Sign In` kini tetap mengeksekusi flow login dengan password terbaru dari input user.

## Update Tambahan (MainWindow Default Maximized + Logout Top Right)
- Mengatur `MainWindow` agar default terbuka dalam state `Maximized` dengan menambahkan `WindowState="Maximized"` pada root `ui:FluentWindow`.
- Memindahkan tombol `Logout` dari header `NavigationView` ke title bar (baris atas) dan menempatkannya sejajar dengan title `MAU APP Desktop` di area kanan atas.
- Mengubah style tombol `Logout` menjadi `Appearance="Danger"` agar tampil merah sesuai kebutuhan.
- Menghapus tombol `Logout` lama pada `NavigationView.Header` untuk menghindari duplikasi aksi logout.

## Verifikasi Tambahan 11
- `dotnet build desktop-app/Mau.Desktop.sln -p:UseAppHost=false` berhasil.
- Hasil: 0 warning, 0 error.

## Update Tambahan (Environment Dev/Prod Endpoint Desktop)
- Menambahkan konfigurasi environment endpoint di aplikasi desktop melalui file:
  - `desktop-app/src/Mau.Desktop/appsettings.json`
  - `desktop-app/src/Mau.Desktop/appsettings.Development.json`
  - `desktop-app/src/Mau.Desktop/appsettings.Production.json`
- Integrasi loader konfigurasi pada startup (`App.xaml.cs`) dengan prioritas environment:
  1. `MAU_DESKTOP_ENVIRONMENT`
  2. `DOTNET_ENVIRONMENT`
  3. default `Development`
- `ApiOptions` direfaktor agar membaca konfigurasi dari gabungan JSON + environment variables.
- Override endpoint tetap didukung via env var:
  - `MAU_DESKTOP_API_BASE_URL`
  - `MAU_DESKTOP_API_TIMEOUT_SECONDS`
- `Mau.Desktop.csproj` diperbarui agar file `appsettings*.json` otomatis ikut ke output build dan package konfigurasi JSON/environment aktif.

## Verifikasi Tambahan 12
- `dotnet build desktop-app/Mau.Desktop.sln -p:UseAppHost=false` berhasil.
- Hasil: 0 warning, 0 error.
