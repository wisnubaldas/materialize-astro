# Progress 2026-05-13

## Ringkasan Perubahan

- Menghubungkan login `mobile-app` ke backend FastAPI:
  - form login mobile memakai kontrak `email` + `password`;
  - request login diarahkan ke `POST /auth/login`;
  - profil user diambil dari `GET /auth/me` memakai Bearer token hasil login.
- Menambahkan timeout request API mobile dan logging `console.error` untuk error response server.
- Menambahkan route dan halaman protected `Build Up Checklist`.
- Menambahkan menu `Build Up Checklist` dari dashboard mobile.
- Menambahkan origin CORS dev mobile `localhost:8100` dan `127.0.0.1:8100` pada backend.
- Memperbaiki CORS debug backend agar origin Ionic dev di IP LAN lokal ikut diizinkan saat `APP_DEBUG=True`.
- Menyesuaikan dokumentasi environment mobile untuk mode backend auth.

## File yang Diubah

- `mobile-app/.env.example`
- `mobile-app/README.md`
- `mobile-app/src/App.jsx`
- `mobile-app/src/config/env.js`
- `mobile-app/src/pages/LoginPage.jsx`
- `mobile-app/src/pages/DashboardPage.jsx`
- `mobile-app/src/pages/BuildUpChecklistPage.jsx`
- `mobile-app/src/services/apiService.js`
- `mobile-app/src/services/authService.js`
- `mobile-app/src/styles/global.css`
- `mobile-app/src/utils/validators.js`
- `materialize-fastapi/app/main.py`

## Hasil Verifikasi

- `npm run build` pada `mobile-app/` lulus.
- `npm audit --audit-level=moderate` pada `mobile-app/` lulus (`found 0 vulnerabilities`).
- `poetry run ruff check app/main.py` pada `materialize-fastapi/` lulus.
- `python -m compileall app/main.py` pada `materialize-fastapi/` lulus.
- `rg --files -g "*.ts" -g "*.tsx" mobile-app` tidak menemukan file TypeScript.
- Smoke test backend lokal:
- `POST http://127.0.0.1:8000/auth/login` berhasil mengembalikan `access_token`.
- `GET http://127.0.0.1:8000/auth/me` dengan Bearer token berhasil mengembalikan profil user.
- Smoke test CORS backend:
  - preflight `OPTIONS /auth/login` dari `Origin: http://localhost:8100` berhasil (`200`).
  - preflight `OPTIONS /auth/login` dari `Origin: http://192.168.1.12:8100` berhasil (`200`).
  - `POST /auth/login` dari kedua origin mobile dev tersebut berhasil (`200`).

## Gap / Risiko

- Untuk akses dari HP fisik, backend tetap harus dijalankan dengan host yang bisa diakses LAN, misalnya `0.0.0.0`, dan `VITE_API_BASE_URL` mobile diarahkan ke IP LAN backend.
- Halaman `Build Up Checklist` baru berupa shell form awal; field input detail menunggu susunan checklist final.

## Blocker

- Tidak ada blocker teknis.

## Next Step

- Jalankan backend FastAPI, lalu buka `mobile-app` dan uji login dengan user backend.
- Susun field operasional untuk `Build Up Checklist`, lalu tambahkan komponen input dan service submit sesuai endpoint backend yang disepakati.

---

## Migrasi Mobile ke React Native Expo (2026-05-13)

## Ringkasan Perubahan

- Mematikan dev server yang listen pada port mobile `8100`.
- Mengganti stack `mobile-app` dari Ionic React + Capacitor menjadi React Native + Expo JavaScript.
- Mengisi ulang `mobile-app` dengan struktur Expo:
  - `App.js`
  - `app.json`
  - `babel.config.js`
  - `eas.json`
  - `assets/`
  - `src/components/`
  - `src/contexts/`
  - `src/navigation/`
  - `src/screens/`
  - `src/services/`
  - `src/styles/`
  - `src/utils/`
- Menghapus folder sumber `react-native-expo-js-agent-starter` setelah isi starter dipindahkan.
- Menyesuaikan root `AGENTS.md` agar mobile resmi memakai React Native + Expo.
- Menyiapkan baseline auth React Native yang tetap memakai backend FastAPI:
  - `POST /auth/login`
  - `GET /auth/me`
  - token disimpan melalui AsyncStorage.
- Menambahkan screen `Build Up Checklist` sebagai shell form awal.
- Mengganti script EAS agar memakai `npx eas-cli` saat dibutuhkan, sehingga `eas-cli` tidak menjadi dependency lokal.
- Mem-pin Expo SDK line ke `expo@53.0.27` dan `react-native@0.79.6` agar kompatibel dengan Node lokal `20.18.0`.

## File yang Diubah

- `AGENTS.md`
- `mobile-app/.env.example`
- `mobile-app/.gitignore`
- `mobile-app/README.md`
- `mobile-app/App.js`
- `mobile-app/app.json`
- `mobile-app/babel.config.js`
- `mobile-app/eas.json`
- `mobile-app/mobile_agent.md`
- `mobile-app/package.json`
- `mobile-app/package-lock.json`
- `mobile-app/assets/*`
- `mobile-app/src/**`

## Hasil Verifikasi

- Semua server dev pada port `8000`, `8100`, `8081`, `4321`, `19000`, `19001`, dan `19002` tidak ada yang listen setelah proses stop.
- `npm install` pada `mobile-app/` berhasil.
- `npm audit --audit-level=moderate` pada `mobile-app/` lulus (`found 0 vulnerabilities`).
- `npx expo-doctor` pada `mobile-app/` lulus (`17/17 checks passed`).
- `rg --files -g "*.ts" -g "*.tsx" mobile-app` tidak menemukan file TypeScript.
- Folder `react-native-expo-js-agent-starter` sudah terhapus.

## Gap / Risiko

- Asset icon/splash saat ini berupa placeholder sederhana MAU APP dan perlu diganti desain final sebelum rilis.
- Build APK/AAB belum dijalankan karena membutuhkan konfigurasi EAS/akun Expo atau Android toolchain sesuai workflow rilis.

## Blocker

- Tidak ada blocker teknis untuk development React Native Expo.

## Next Step

- Jalankan `npm start` dari `mobile-app/`, lalu buka melalui Expo Go.
- Jika testing di HP fisik, arahkan `EXPO_PUBLIC_API_BASE_URL` ke IP LAN backend, misalnya `http://192.168.1.12:8000`.

---

## Upgrade Mobile Expo SDK 54 (2026-05-13)

## Ringkasan Perubahan

- Meng-upgrade `mobile-app` dari Expo SDK 53 ke Expo SDK 54 agar kompatibel dengan Expo Go yang terpasang di HP.
- Menjalankan `npx expo install expo@^54.0.0 --fix` agar native package mengikuti versi yang direkomendasikan Expo SDK 54.
- Dependency utama setelah upgrade:
  - `expo@^54.0.0`
  - `react-native@0.81.5`
  - `react@19.1.0`
  - `expo-status-bar@~3.0.9`
  - `@react-native-async-storage/async-storage@2.2.0`

## File yang Diubah

- `mobile-app/package.json`
- `mobile-app/package-lock.json`
- `docs/progress-2026-05-13.md`

## Hasil Verifikasi

- `npm audit --audit-level=moderate` pada `mobile-app/` lulus (`found 0 vulnerabilities`).
- `npx expo-doctor` pada `mobile-app/` lulus (`17/17 checks passed`).
- `rg --files -g "*.ts" -g "*.tsx" mobile-app` tidak menemukan file TypeScript.

## Gap / Risiko

- Expo SDK 54 / React Native `0.81.5` menampilkan warning engine karena Node lokal saat ini `20.18.0`, sementara beberapa package meminta `>=20.19.4`.
- Upgrade Node ke minimal `20.19.4` direkomendasikan sebelum build rilis agar toolchain Metro/React Native sepenuhnya sesuai requirement.

## Blocker

- Tidak ada blocker untuk menjalankan project dengan Expo Go SDK 54.

## Next Step

- Jalankan `npm start -- --clear` dari `mobile-app/`, lalu scan ulang QR dengan Expo Go terbaru.

---

## Hotfix Expo Go Bundling (2026-05-13)

## Ringkasan Perubahan

- Memperbaiki error Metro bundler `Cannot find module 'babel-preset-expo'`.
- Menambahkan `babel-preset-expo` versi kompatibel Expo SDK 54 (`~54.0.10`) di `devDependencies`.
- Membersihkan duplikasi dependency `babel-preset-expo` agar `expo-doctor` membaca satu versi yang benar.
- Restart Metro dengan cache bersih melalui `npx expo start --clear --host lan`.

## File yang Diubah

- `mobile-app/package.json`
- `mobile-app/package-lock.json`
- `docs/progress-2026-05-13.md`

## Hasil Verifikasi

- `npm install` pada `mobile-app/` berhasil.
- `npm audit --audit-level=moderate` pada `mobile-app/` lulus (`found 0 vulnerabilities`).
- `npx expo-doctor` pada `mobile-app/` lulus (`17/17 checks passed`).
- `rg --files -g "*.ts" -g "*.tsx"` pada `mobile-app/` tidak menemukan file TypeScript.
- Metro berjalan di port `8081`.
- Bundling Android berhasil: `Android Bundled ... node_modules\expo\AppEntry.js`.

## Gap / Risiko

- Node lokal masih `20.18.0`; Expo SDK 54/React Native `0.81.5` merekomendasikan minimal `20.19.4`.

## Blocker

- Tidak ada blocker teknis setelah hotfix bundling.

## Next Step

- Scan ulang QR dari Expo Go.
- Upgrade Node ke minimal `20.19.4` untuk menghilangkan warning engine dan menyamakan toolchain dengan requirement React Native.

---

## Hotfix Login Mobile Loading Tanpa Request (2026-05-13)

## Ringkasan Perubahan

- Menambahkan timeout eksplisit pada semua request API mobile melalui `apiRequest`.
- Menambahkan log diagnostik request:
  - mode login `mock` atau `api`;
  - method dan URL request sebelum `fetch`;
  - status sukses;
  - detail error response atau network error.
- Mengubah default `EXPO_PUBLIC_USE_MOCK_AUTH` menjadi `false` jika env tidak terbaca, agar mobile tidak diam-diam masuk mode mock saat targetnya backend.
- Menambahkan konfigurasi `EXPO_PUBLIC_API_TIMEOUT_MS` di `.env.example`.
- Memperkuat parser response API agar response non-JSON tetap menghasilkan error yang bisa dibaca, bukan error parsing mentah.

## File yang Diubah

- `mobile-app/.env.example`
- `mobile-app/src/config/env.js`
- `mobile-app/src/services/apiService.js`
- `mobile-app/src/services/authService.js`
- `docs/progress-2026-05-13.md`

## Hasil Verifikasi

- `npx expo-doctor` pada `mobile-app/` lulus (`17/17 checks passed`).
- `http://127.0.0.1:8000/` dari mesin lokal berhasil (`200`).
- `http://192.168.1.12:8000/` dari mesin lokal gagal dengan connection refused, sehingga device Expo yang memakai IP LAN tersebut tidak akan mencapai backend sampai FastAPI listen ke jaringan LAN.

## Gap / Risiko

- Untuk HP fisik via Expo Go, backend perlu dijalankan dengan host LAN, misalnya `0.0.0.0`, lalu `EXPO_PUBLIC_API_BASE_URL` tetap diarahkan ke IP LAN komputer backend.
- Jika testing lewat Android emulator, API base URL biasanya perlu memakai `http://10.0.2.2:8000`, bukan `127.0.0.1`.

## Blocker

- Tidak ada blocker kode mobile.
- Akses login dari HP fisik masih bergantung pada backend yang listen di LAN dan firewall Windows yang mengizinkan port `8000`.

## Next Step

- Jalankan backend dengan host yang bisa diakses device, misalnya `poetry run uvicorn app.main:app --host 0.0.0.0 --port 8000`.
- Restart Expo dengan cache bersih setelah perubahan env: `npx expo start --clear --host lan`.

---

## Material Line Icon Dashboard Mobile (2026-05-13)

## Ringkasan Perubahan

- Menambahkan dependency Expo-compatible `@expo/vector-icons` untuk Material icon set.
- Menambahkan peer dependency `expo-font` melalui `npx expo install` agar icon font aman untuk Expo Go dan native build.
- Menambahkan config plugin `expo-font` pada `app.json`.
- Mengubah header native stack `Dashboard` agar memakai icon `view-dashboard-outline` di sebelah kiri teks `Dashboard`.

## File yang Diubah

- `mobile-app/app.json`
- `mobile-app/package.json`
- `mobile-app/package-lock.json`
- `mobile-app/src/navigation/AppNavigator.js`
- `docs/progress-2026-05-13.md`

## Hasil Verifikasi

- `node --check src/navigation/AppNavigator.js` lulus.
- `npx expo-doctor` pada `mobile-app/` lulus (`17/17 checks passed`).
- `npm audit --audit-level=moderate` pada `mobile-app/` lulus (`found 0 vulnerabilities`).
- `rg --files -g "*.ts" -g "*.tsx" mobile-app` tidak menemukan file TypeScript.

## Gap / Risiko

- Icon menggunakan `@expo/vector-icons`/MaterialCommunityIcons karena ini pendekatan resmi dan kompatibel untuk React Native Expo; bukan file icon font web dari Materialize HTML template.

## Blocker

- Tidak ada blocker teknis.

## Next Step

- Refresh Expo Go atau restart Metro jika icon belum langsung muncul karena cache.

---

## Refactor UI Mobile ke NativeWind (2026-05-13)

## Ringkasan Perubahan

- Menambahkan NativeWind sebagai sistem styling utama mobile berdasarkan `mobile-app/nativewind_agent.md` dan dokumentasi resmi NativeWind/Expo.
- Menambahkan setup NativeWind:
  - `global.css`
  - `metro.config.js`
  - `postcss.config.mjs`
  - import `global.css` di `App.js`
  - `web.bundler` Metro di `app.json`
- Menambahkan dependency terkait:
  - `nativewind@preview`
  - `react-native-css`
  - `react-native-reanimated`
  - `react-native-worklets`
  - `tailwindcss`
  - `@tailwindcss/postcss`
  - `prettier-plugin-tailwindcss`
  - `expo-linear-gradient`
- Menambahkan aturan wajib NativeWind di `AGENTS.md` dan `mobile-app/mobile_agent.md`.
- Refactor komponen reusable `AppButton`, `AppInput`, dan `InfoCard` agar memakai `className` NativeWind.
- Refactor `LoginScreen` menjadi UI dark mobile yang konsisten dengan dashboard.
- Membuat `DashboardScreen` bergaya mockup:
  - header dark dengan hamburger, logo, dan bell;
  - search bar;
  - horizontal popular services;
  - card gradient utama;
  - recently viewed cards;
  - bottom navigation;
  - drawer menu samping dengan item Profile, Onboarding, Welcome, Permissions, Chat, dan Sign out.
- Mengisi `BuildUpChecklistScreen` dengan form dark seperti referensi:
  - back dan close action;
  - title/subtitle;
  - field MAWB Number, ULD Number, Gross Weight;
  - tombol `Next` besar di bawah.

## File yang Diubah

- `AGENTS.md`
- `mobile-app/App.js`
- `mobile-app/app.json`
- `mobile-app/global.css`
- `mobile-app/metro.config.js`
- `mobile-app/postcss.config.mjs`
- `mobile-app/mobile_agent.md`
- `mobile-app/package.json`
- `mobile-app/package-lock.json`
- `mobile-app/src/components/AppButton.js`
- `mobile-app/src/components/AppInput.js`
- `mobile-app/src/components/InfoCard.js`
- `mobile-app/src/navigation/AppNavigator.js`
- `mobile-app/src/screens/LoginScreen.js`
- `mobile-app/src/screens/DashboardScreen.js`
- `mobile-app/src/screens/BuildUpChecklistScreen.js`
- `docs/progress-2026-05-13.md`

## Hasil Verifikasi

- `node --check` untuk file JS yang diubah lulus.
- `npx expo-doctor` pada `mobile-app/` lulus (`17/17 checks passed`).
- `npm audit --audit-level=moderate` pada `mobile-app/` lulus (`found 0 vulnerabilities`).
- `rg --files -g "*.ts" -g "*.tsx" mobile-app` tidak menemukan file TypeScript.
- `npx expo export --platform android --output-dir dist` berhasil membundle Android (`1388 modules`), lalu folder `dist` hasil verifikasi dihapus karena build artifact.

## Gap / Risiko

- NativeWind yang dipakai adalah jalur modern Expo SDK 54 (`nativewind@preview`/v5) karena sesuai dokumentasi resmi terbaru untuk Expo SDK 54.
- Visual sudah mengikuti struktur mockup, tetapi gambar ilustrasi mockup diganti dengan gradient/card native agar tidak menambah aset eksternal.
- Tombol `Next` pada Build Up Checklist belum submit ke backend karena kontrak endpoint checklist belum ditentukan.

## Blocker

- Tidak ada blocker teknis.

## Next Step

- Jalankan `npx expo start --clear --host lan` agar Metro membaca konfigurasi NativeWind baru.
- Tentukan kontrak endpoint backend untuk submit Build Up Checklist.
